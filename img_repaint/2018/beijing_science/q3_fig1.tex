% Source PDF: source/普通高考/2018/2018北京理.pdf, page 1.
% PDF embedded-bitmap attempt: pdfimages -list found no embedded image objects; the flowchart is vector content.
% Flowchart elements: start/end rounded boxes, initialization/update/increment rectangles,
% decision diamond k>=3, output trapezium, directed arrows, and the yes/no branch labels.
% Node-edge list from the source PDF: start -> initialize; initialize -> update;
% update -> increment; increment -> decision; decision --是--> output; output -> end;
% decision --否--> update (the return arrow ends at the update connector).
% Relations: the main sequence is start -> initialize -> update -> increment -> decision -> output -> end;
% the no branch returns from the decision to the update step, while yes continues to output.
% solid segments: all box outlines and directed connectors; dashed segments: none.
% labels: Chinese labels stay inside their source nodes; branch labels sit beside the decision connectors.

\begin{tikzpicture}[
    >=stealth,
    font=\normalsize,
    node distance=0pt,
    line width=0.55pt,
    every node/.style={
        inner xsep=4pt,
        inner ysep=2pt,
        anchor=center,
        align=center,
        execute at begin node={\setlength{\parindent}{0pt}},
    },
]
    % The vertical layout follows the source flowchart; all relations are semantic, not pixel traced.
    \node[draw, rounded corners=5pt, minimum width=1.18cm, minimum height=0.58cm] (start) at (0,9.05) {开始};
    \node[draw, minimum width=2.55cm, minimum height=0.62cm] (init) at (0,7.85) {$k=1,s=1$};
    \node[draw, minimum width=4.65cm, minimum height=1.08cm] (update) at (0,6.45)
        {$s=s+(-1)^k\cdot\dfrac{1}{1+k}$};
    \node[draw, minimum width=1.95cm, minimum height=0.62cm] (increment) at (0,5.05) {$k=k+1$};
    \node[draw, diamond, aspect=2.35, inner sep=0pt,
        minimum width=3.45cm, minimum height=1.05cm] (decision) at (0,3.65) {$k\geq 3$};
    \node[draw, trapezium, trapezium left angle=75, trapezium right angle=105,
        minimum width=1.85cm, minimum height=0.72cm] (output) at (0,2.15) {输出 $s$};
    \node[draw, rounded corners=5pt, minimum width=1.18cm, minimum height=0.58cm] (finish) at (0,0.88) {结束};

    % Main directed sequence.
    \draw[->] (start.south) -- (init.north);
    \draw[->] (init.south) -- (update.north);
    \draw[->] (update.south) -- (increment.north);
    \draw[->] (increment.south) -- (decision.north);
    \draw[->] (decision.south) -- node[pos=0.50, anchor=west] {是} (output.north);
    \draw[->] (output.south) -- (finish.north);

    % No branch loops rightward and back to the update connector; the arrow points left at the return.
    \draw[->] (decision.east) -- ++(1.25,0) |- (0,7.48);
    \node[anchor=south] at (1.38,3.83) {否};
\end{tikzpicture}
