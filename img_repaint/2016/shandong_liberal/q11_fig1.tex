% Source PDF: source/普通高考/2016/2016山东文.pdf, page 1 (question 11).
% PDF embedded-bitmap attempt: pdfimages -list found no image objects; the program
% flowchart is vector line art. Comparison crop is derived from a no-grid page render.
% Elements: rounded start/end terminals, input/output parallelograms, initialization
% rectangles, accumulation/update rectangles, one decision diamond, directed arrows,
% and 是/否 branch labels.
% Relations (directed edges): 开始→输入 n→i=1→S=0→累加式→i>=n?;
% (是)→输出 S→结束; (否)→i=i+1→累加式.
% solid segments: all node outlines and directed connectors; dashed segments: none.
% labels: node text is locally zero-indented and centered; 是/否 labels sit beside
% the corresponding decision branches without touching arrow shafts or borders.

\begin{tikzpicture}[
    >=Stealth,
    line width=0.45pt,
    line cap=round,
    line join=round,
    font=\normalsize,
    flowbox/.style={draw, minimum height=0.62cm, inner sep=0.6pt, align=center,
        execute at begin node={\setlength{\parindent}{0pt}}},
    terminal/.style={flowbox, rounded corners=0.18cm, minimum width=1.45cm},
    io/.style={flowbox, trapezium, trapezium left angle=72, trapezium right angle=108},
    process/.style={flowbox},
    decision/.style={flowbox, diamond, aspect=2.8, inner sep=0pt},
]
    \node[terminal] (start) at (0,9.65) {开始};
    \node[io, minimum width=2.55cm, minimum height=0.72cm] (input) at (0,8.35)
        {输入 \(n\)};
    \node[process, minimum width=2.00cm, minimum height=0.72cm] (init) at (0,7.05)
        {\(i=1\)};
    \node[process, minimum width=2.00cm, minimum height=0.72cm] (zero) at (0,5.85)
        {\(S=0\)};
    \node[process, minimum width=5.50cm, minimum height=0.82cm] (accumulate) at (0,4.35)
        {\(S=S+\sqrt{i+1}-\sqrt{i}\)};
    \node[decision, minimum width=3.95cm, minimum height=1.10cm] (limit) at (0,2.65)
        {\(i\geq n\)};
    \node[io, minimum width=2.55cm, minimum height=0.72cm] (output) at (0,1.15)
        {输出 \(S\)};
    \node[terminal] (finish) at (0,-0.05) {结束};
    \node[process, minimum width=2.75cm, minimum height=0.72cm] (increment) at (4.40,3.70)
        {\(i=i+1\)};

    % Main downward flow and terminating branch.
    \draw[->] (start.south) -- (input.north);
    \draw[->] (input.south) -- (init.north);
    \draw[->] (init.south) -- (zero.north);
    \draw[->] (zero.south) -- (accumulate.north);
    \draw[->] (accumulate.south) -- (limit.north);
    \draw[->] (limit.south) -- node[right=2pt] {是} (output.north);
    \draw[->] (output.south) -- (finish.north);

    % The 否 branch increments i, then returns to the accumulation step.
    \coordinate (branch-right) at (4.40,2.65);
    \draw[->] (limit.east) -- (branch-right) -- (increment.south);
    \node[anchor=west, inner sep=1pt] at (2.05,2.78) {否};
    \coordinate (loop-top) at (4.40,5.02);
    % The loop arrow runs to the central-stem junction; the stem itself
    % remains the independent zero->accumulate main edge.
    \draw[->] (increment.north) -- (loop-top) -- (0.55,5.02) -- (0,5.02);
\end{tikzpicture}
