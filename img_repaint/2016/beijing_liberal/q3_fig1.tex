% Source PDF: source/普通高考/2016/2016北京文.pdf, page 1 (question 3).
% PDF embedded-bitmap attempt: pdfimages -list found no image objects; vector flowchart.
% Node-edge list: start -> k=0,s=0 -> k<=2; (是) -> s=s+k^3 -> k=k+1
% -> merge back to the central connector; (否) -> 输出 s -> 结束.
% solid segments: all node outlines and directed connectors; dashed segments: none.
% labels: node text is centered with zero paragraph indent; 是/否 sit beside their
% corresponding branches. The branch geometry is schematic, not to scale.
\begin{tikzpicture}[
    >=Stealth,
    line width=0.55pt,
    line cap=round,
    line join=round,
    every node/.style={
        inner xsep=4pt,
        inner ysep=2pt,
        anchor=center,
        align=center,
        execute at begin node={\setlength{\parindent}{0pt}},
    },
]
    \node[draw, rounded corners=5pt, minimum width=1.2cm, minimum height=0.58cm]
      (start) at (0,8.65) {开始};
    \node[draw, minimum width=2.45cm, minimum height=0.62cm]
      (init) at (0,7.35) {$k=0,s=0$};
    \node[draw, diamond, aspect=2.45, inner sep=0pt,
      minimum width=3.55cm, minimum height=1.05cm]
      (check) at (0,3.95) {$k\leq 2$};
    \node[draw, minimum width=2.35cm, minimum height=0.62cm]
      (update) at (-3.40,4.62) {$s=s+k^3$};
    \node[draw, minimum width=2.20cm, minimum height=0.62cm]
      (increment) at (-3.40,5.95) {$k=k+1$};
    \node[draw, trapezium, trapezium left angle=75, trapezium right angle=105,
      minimum width=2.05cm, minimum height=0.70cm]
      (output) at (0,2.05) {输出 $s$};
    \node[draw, rounded corners=5pt, minimum width=1.2cm, minimum height=0.58cm]
      (finish) at (0,0.75) {结束};

    % Main flow; the side loop rejoins the central connector above the decision.
    \draw[->] (start.south) -- (init.north);
    \draw[->] (init.south) -- (check.north);
    \draw[->] (check.south) -- (output.north);
    \node[anchor=west, inner sep=0pt] at (0.18,2.95) {否};
    \draw[->] (output.south) -- (finish.north);

    % 是 branch: update then increment, returning by a distinct horizontal arrow.
    \coordinate (yesCorner) at (-3.40,3.95);
    \draw[->] (check.west) -- (yesCorner) -- (update.south);
    \node[anchor=south] at (-1.92,4.10) {是};
    \draw[->] (update.north) -- (increment.south);
    \coordinate (mergeLeft) at (-3.40,6.55);
    \coordinate (merge) at (0,6.55);
    \draw (increment.north) -- (mergeLeft);
    \draw[->] (mergeLeft) -- (merge);
\end{tikzpicture}
