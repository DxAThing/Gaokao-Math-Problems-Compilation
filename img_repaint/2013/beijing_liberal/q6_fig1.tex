% Source: source/普通高考/2013/2013北京文.pdf, page 1, question 6.
% pdfimages -list found no embedded image objects; the source flowchart is vector artwork.
% Grid evidence: tmp/review_2013_beijing_liberal/grids/page1_grid100.png; q6_grid50.png.
% Comparison crop: tmp/review_2013_beijing_liberal/crops/q6_original.png.
% Program topology: 开始 -> i=0,S=1 -> S=(S^2+1)/(2S+1) -> i=i+1 -> i>=2;
% 是 -> 输出 S -> 结束; 否 -> return to the shared stem above the update.
% All connectors are solid directed edges; branch labels are placed beside exits.

\begin{tikzpicture}[
    >=Stealth,
    x=1cm,
    y=0.82cm,
    line width=0.45pt,
    line cap=round,
    line join=round,
    font=\normalsize,
    flowtext/.style={anchor=center, align=center,
        execute at begin node={\setlength{\parindent}{0pt}}},
    flowbox/.style={draw, flowtext, minimum height=0.68cm,
        inner xsep=5pt, inner ysep=3pt},
    terminal/.style={flowbox, rounded corners=0.16cm, minimum width=0.98cm},
    process/.style={flowbox, minimum width=1.60cm},
    decision/.style={flowbox, diamond, aspect=3.0, minimum width=2.95cm,
        minimum height=1.04cm},
    branchlabel/.style={flowtext, inner sep=0pt},
    io/.style={flowbox, trapezium, trapezium left angle=72,
        trapezium right angle=108, minimum height=0.78cm},
]
    % Include the full right-side return leg and a small safety margin.
    \path[use as bounding box] (-2.05,0.05) rectangle (3.25,9.45);

    \node[terminal] (start) at (0,9.10) {开始};
    \node[process, minimum width=2.20cm] (init) at (0,7.78) {$i=0,S=1$};
    \node[process, minimum width=2.10cm, minimum height=1.05cm] (update)
        at (0,6.18) {$S=\dfrac{S^2+1}{2S+1}$};
    \node[process, minimum width=1.70cm] (increment) at (0,4.62)
        {$i=i+1$};
    \node[decision] (test) at (0,3.18) {$i\geq 2$};
    \node[io, minimum width=1.60cm] (output) at (0,1.75) {输出 $S$};
    \node[terminal] (finish) at (0,0.45) {结束};

    % Main sequence and affirmative branch.
    \draw[->] (start.south) -- (init.north);
    \draw[->] (init.south) -- (update.north);
    \draw[->] (update.south) -- (increment.north);
    \draw[->] (increment.south) -- (test.north);
    \draw[->] (test.south) -- node[right=2pt] {是} (output.north);
    \draw[->] (output.south) -- (finish.north);

    % Negative branch: a right-side return arrow joins the stem above the
    % update; the main stem carries the only downward arrow into update.
    \draw[->] (test.east) -- node[above=2pt] {否} (1.85,3.18) -- (1.85,7.38) -- (0,7.38);
\end{tikzpicture}
