% original_pdf: source/普通高考/2014/2014湖南文.pdf
% page: 1 (question 7)
% embedded_bitmap_attempt: pdfimages -list returned no embedded images; redraw from rendered page
% evidence: tmp/independent_gate_2014_hunan_liberal/page1_grid.png; q7_original.png (no-grid crop)
% elements: rounded 开始/结束 terminals; input parallelogram 输入 t; condition diamond t<0?;
% assignment rectangles t=2t^2+1 and S=t-3; output parallelogram 输出 S; solid arrows and 是/否 labels.
% node-edge list: 开始→输入 t→t<0?; 是→t=2t^2+1→left loop-back to the connector before
% t<0?; 否→S=t−3→输出 S→结束. No other connecting segments are present.
% solid segments: all node borders and directed connectors; dashed segments: none.
% Labels are centered in nodes with local zero paragraph indentation and compact padding.

\begin{tikzpicture}[
  baseline,
  >=stealth,
  x=1cm,
  y=1.3cm,
  line width=0.55pt,
  line cap=round,
  line join=round,
  font=\normalsize,
  every node/.style={anchor=center,align=center,
    execute at begin node={\setlength{\parindent}{0pt}}},
  flowbox/.style={draw,minimum height=0.68cm,inner xsep=4pt,inner ysep=2pt,
    align=center,execute at begin node={\setlength{\parindent}{0pt}}},
  terminal/.style={flowbox,rounded corners=0.20cm,minimum width=1.24cm},
  io/.style={flowbox,shape=trapezium,trapezium left angle=72,trapezium right angle=108,
    minimum height=0.76cm},
  decision/.style={flowbox,shape=diamond,aspect=2.8,inner xsep=4pt,inner ysep=2pt,
    minimum height=1.00cm}
]
  % Main path and branch nodes.
  \node[terminal] (start) at (0,9.00) {开始};
  \node[io,minimum width=2.35cm] (input) at (0,7.55) {输入 $t$};
  \node[decision,minimum width=3.75cm] (test) at (0,5.90) {$t<0?$};
  \node[flowbox,minimum width=3.05cm] (leftassign) at (-3.08,4.25) {$t=2t^{2}+1$};
  \node[flowbox,minimum width=2.85cm] (rightassign) at (3.08,4.25) {$S=t-3$};
  \node[io,minimum width=2.70cm] (output) at (0,2.35) {输出 $S$};
  \node[terminal] (finish) at (0,0.88) {结束};

  \draw[->] (start.south) -- (input.north);
  \draw[->] (input.south) -- (test.north);
  \draw[->] (test.west) -- (-3.08,5.90) -- (leftassign.north);
  \draw[->] (test.east) -- (3.08,5.90) -- (rightassign.north);
  \draw[->] (rightassign.south) -- (3.08,3.45) -- (0,3.45) -- (output.north);
  \draw[->] (output.south) -- (finish.north);

  % The yes branch loops left and returns to the connector immediately before the test.
  \draw[->] (leftassign.west) -- (-4.80,4.25) -- (-4.80,7.05) -- (0,7.05);

  \node[anchor=south east,inner sep=1pt] at (-1.55,5.98) {是};
  \node[anchor=south west,inner sep=1pt] at (1.55,5.98) {否};

  \path[use as bounding box] (-5.05,9.42) rectangle (4.35,0.46);
\end{tikzpicture}
