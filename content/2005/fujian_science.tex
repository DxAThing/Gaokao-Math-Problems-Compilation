\chapter{2005年福建卷（理）}
\section{单选题}
\begin{problem}
复数 \(z = \frac{1}{1 - \i}\) 的共轭复数是（\quad）

\choices
    {\(\frac{1}{2} + \frac{1}{2}\i\)}
    {\(\frac{1}{2} - \frac{1}{2}\i\)}
    {\(1 - \i\)}
    {\(1 + \i\)}
\end{problem}

\begin{problem}
已知等差数列 \(\{a_n\}\) 中，\(a_7 + a_9 = 16\)，\(a_4 = 1\)，则 \(a_{12}\) 的值是（\quad）

\choices
    {15}
    {30}
    {31}
    {64}
\end{problem}

\begin{problem}
在 \(\triangle ABC\) 中，\(\angle C = 90^{\circ}\)，\(\overrightarrow{AB} = (k, 1)\)，\(\overrightarrow{AC} = (2, 3)\)，则 \(k\) 的值是（\quad）

\choices
    {5}
    {\(-5\)}
    {\(\frac{3}{2}\)}
    {\(-\frac{3}{2}\)}
\end{problem}

\begin{problem}
已知直线 \(m\)，\(n\) 与平面 \(\alpha, \beta\)，给出下列三个命题：

\begin{circlelist}
\item 若 \(m \parallel \alpha\)，\(n \parallel \alpha\)，则 \(m \parallel n\)；
\item 若 \(m \parallel \alpha\)，\(n \perp \alpha\)，则 \(n \perp m\)；
\item 若 \(m \perp \alpha\)，\(m \parallel \beta\)，则 \(\alpha \perp \beta\).
\end{circlelist}

其中真命题的个数是（\quad）

\choices
    {0}
    {1}
    {2}
    {3}
\end{problem}

\begin{problem}
若函数 \(f(x)=a^{x-b}\) 的图象如图，其中 \(a, b\) 为常数，则下列结论正确的是（\quad）

\choices
    {\(a > 1, b < 0\)}
    {\(a > 1, b > 0\)}
    {\(0 < a < 1, b > 0\)}
    {\(0 < a < 1, b < 0\)}
\end{problem}

\begin{problem}
若函数 \(y = \sin(\omega x + \varphi)\) \((x \in \R, \omega > 0, 0 \leqslant \varphi < 2\pi)\) 的部分图象如图，则（\quad）

\choices
    {\(\omega = \frac{\pi}{2},\varphi = \frac{\pi}{4}\)}
    {\(\omega = \frac{\pi}{3},\varphi = \frac{\pi}{6}\)}
    {\(\omega = \frac{\pi}{4},\varphi = \frac{\pi}{4}\)}
    {\(\omega = \frac{\pi}{4},\varphi = \frac{5\pi}{4}\)}
\end{problem}

\begin{problem}
已知 \(p: |2x - 3| < 1\)，\(q: x(x - 3) < 0\)，则 \(p\) 是 \(q\) 的（\quad）

\choices
    {充分不必要条件}
    {必要不充分条件}
    {充要条件}
    {既不充分也不必要条件}
\end{problem}

\begin{problem}
如图，长方体 \(ABCD - A_{1}B_{1}C_{1}D_{1}\) 中，\(AA_{1} = AB = 2\)，\(AD = 1\)，点 \(E\)，\(F\)，\(G\) 分别是 \(DD_{1}\)，\(AB\)，\(CC_{1}\) 的中点，则异面直线 \(A_{1}E\) 与 \(GF\) 所成的角是（\quad）

\bitmapfigure[max width=0.62\linewidth,max totalheight=6.8cm]{img/2005/fujian_science/q8_fig1.png}

\choices
    {\(\arccos \frac{\sqrt{15}}{5}\)}
    {\(\frac{\pi}{4}\)}
    {\(\arccos \frac{\sqrt{10}}{5}\)}
    {\(\frac{\pi}{2}\)}
\end{problem}

\begin{problem}
从 6 人中选 4 人分别到巴黎，伦敦，悉尼，莫斯科四个城市游览. 要求每个城市有一人游览，每人只游览一个城市，且这 6 人中甲，乙两人不去巴黎游览，则不同的选择方案共有（\quad）

\choices
    {300 种}
    {240 种}
    {144 种}
    {96 种}
\end{problem}

\begin{problem}
已知 \(F_{1}\)，\(F_{2}\) 是双曲线 \(\frac{x^{2}}{a^{2}} - \frac{y^{2}}{b^{2}} = 1\) \((a > 0, b > 0)\) 的两焦点，以线段 \(F_{1}F_{2}\) 为边作正三角形 \(MF_{1}F_{2}\)，若边 \(MF_{1}\) 的中点在双曲线上，则双曲线的离心率是（\quad）

\choices
    {\(4 + 2\sqrt{3}\)}
    {\(\sqrt{3} - 1\)}
    {\(\frac{\sqrt{3} + 1}{2}\)}
    {\(\sqrt{3} + 1\)}
\end{problem}

\begin{problem}
设 \(a, b \in \R\)，\(a^2 + 2b^2 = 6\)，则 \(a + b\) 的最小值是（\quad）

\choices
    {\(-2\sqrt{2}\)}
    {\(-\frac{5\sqrt{3}}{3}\)}
    {\(-3\)}
    {\(-\frac{7}{2}\)}
\end{problem}

\begin{problem}
\(f(x)\) 是定义在 \(\R\) 上的以 3 为周期的奇函数，且 \(f(2) = 0\)，则方程 \(f(x) = 0\) 在区间 \((0,6)\) 内解的个数的最小值是（\quad）

\choices
    {2}
    {3}
    {5}
    {7}
\end{problem}

\section{填空题}
\begin{problem}
\(\left(2\sqrt{x} - \frac{1}{x}\right)^6\) 展开式中的常数项是\fillinblank{}.（用数字作答）
\end{problem}

\begin{problem}
非负实数 \(x, y\) 满足 \(\begin{cases}2x + y \leqslant 0,\\ x + y - 3 \leqslant 0.\end{cases}\)，则 \(x + 3y\) 的最大值为\fillinblank{}.
\end{problem}

\begin{problem}
若常数 \(b\) 满足 \(|b| > 1\)，则 \(\lim_{n \to \infty} \frac{1 + b + b^2 + \cdots + b^{n-1}}{b^n} = \)\fillinblank{}.
\end{problem}

\begin{problem}
把下面不完整的命题补充完整，并使之成为真命题：若函数 \(f(x)=3+\log_{2}x\) 的图象与 \(g(x)\) 的图象关于\fillinblank{} 对称，则函数 \(g(x)=\fillinblank{}.\) 【注】填上你认为可以成为真命题的一件情形即可，不必考虑所有可能的情形.
\end{problem}

\section{解答题}
\begin{problem}
已知 \(-\frac{\pi}{2} < x < 0\)，\(\sin x + \cos x = \frac{1}{5}\).

\begin{enumerate}
\item 求 \(\sin x - \cos x\) 的值；
\item 求 \(\dfrac{3\sin^2 \frac{x}{2} - 2\sin \frac{x}{2}\cos \frac{x}{2} + \cos^2 \frac{x}{2}}{\tan x + \cot x}\) 的值.
\end{enumerate}
\end{problem}

\begin{problem}
甲，乙两人在罚球线投球命中的概率分别为 \(\frac{1}{2}\) 与 \(\frac{2}{5}\)，投中得 1 分，投不中得 0 分.

\begin{enumerate}
\item 甲，乙两人在罚球线各投球一次，求两人得分之和 \(\xi\) 的数学期望；
\item 甲，乙两人在罚球线各投球二次，求这四次投球中至少一次命中的概率.
\end{enumerate}
\end{problem}

\begin{problem}
已知函数 \(f(x) = \frac{ax - 6}{x^2 + b}\) 的图象在点 \(M(-1, f(x))\) 处的切线方程为 \(x + 2y + 5 = 0\).

\begin{enumerate}
\item 求函数 \(y = f(x)\) 的解析式；
\item 求函数 \(y = f(x)\) 的单调区间.
\end{enumerate}
\end{problem}

\begin{problem}
如图，直二面角 \(D - AB - E\) 中，四边形 \(ABCD\) 是边长为 2 的正方形，\(AE = EB\)，\(F\) 为 \(CE\) 上的点，且 \(BF \perp\) 平面 \(ACE\).

\begin{enumerate}
\item 求证 \(AE \perp\) 平面 \(BCE\)；
\item 求二面角 \(B - AC - E\) 的大小；
\item 求点 \(D\) 到平面 \(ACE\) 的距离.
\end{enumerate}

\bitmapfigure[max width=0.42\linewidth,max totalheight=6.0cm]{img/2005/fujian_science/q20_fig1.png}

\end{problem}

\begin{problem}
已知方向向量为 \(\bs{v} = (1, \sqrt{3})\) 的直线 \(l\) 过点 \((0, -2\sqrt{3})\) 和椭圆 \(C: \frac{x^2}{a^2} + \frac{y^2}{b^2} = 1\) \((a > b > 0)\) 的焦点，且椭圆 \(C\) 的中心关于直线 \(l\) 的对称点在椭圆 \(C\) 的右准线上.

\begin{enumerate}
\item 求椭圆 \(C\) 的方程；
\item 是否存在过点 \(E(-2,0)\) 的直线 \(m\) 交椭圆 \(C\) 于点 \(M, N\)，满足 \(\overrightarrow{OM} \cdot \overrightarrow{ON} = \frac{4\sqrt{6}}{3}\cot \angle MON \neq 0\)（\(O\) 为原点）. 若存在，求直线 \(m\) 的方程；若不存在，请说明理由.
\end{enumerate}
\end{problem}

\begin{problem}
已知数列 \(\{a_n\}\) 满足 \(a_1 = a\)，\(a_{n+1} = 1 + \frac{1}{a_n}\). 我们知道当 \(a\) 取不同的值时，得到不同的数列，如当 \(a = 1\) 时，得到无穷数列: \(1, \frac{3}{2}, \frac{5}{3}, \cdots\)；当 \(a = -\frac{1}{2}\) 时，得到有穷数列: \(-\frac{1}{2}, -1, 0\).

\begin{enumerate}
\item 求当 \(a\) 为何值时，\(a_4 = 0\)；
\item 设数列 \(\{b_n\}\) 满足 \(b_1 = -1\)，\(b_{n+1} = \frac{1}{b_n - 1}\) \((n \in \mathbf{N}_+)\)，求证 \(a\) 取数列 \(\{b_n\}\) 中的任一个数，都可以得到一个有穷数列 \(\{a_n\}\)；
\item 若 \(\frac{3}{2} < a_n < 2\) \((n \geqslant 4)\)，求 \(a\) 的取值范围.
\end{enumerate}
\end{problem}
