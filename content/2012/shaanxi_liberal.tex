\chapter{2012年陕西卷（文）}

\section{单选题}

\begin{problem}
集合 \(M = \{x \mid \lg x > 0\}\)，\(N = \{x \mid x^2 \leqslant 4\}\)，则 \(M \cap N =\)（\quad）

\choices
    {(1,2)}
    {\([1,2]\)}
    {\((1,2]\)}
    {\([1,2]\)}
\end{problem}

\begin{problem}
下列函数中，既是奇函数又是增函数的行为（\quad）

\choices
    {\(y = x + 1\)}
    {\(y = -x^{3}\)}
    {\(y = \frac{1}{x}\)}
    {\(y = x|x|\)}
\end{problem}

\begin{problem}
对某商店一个月内每天的顾客人数进行了统计，得到样本的茎叶图如图所示. 则该样本的中位数、众数、极差分别是（\quad）

\bitmapfigure[max width=0.34\linewidth,max totalheight=5.2cm]{img/2012/shaanxi_liberal/q3_fig1.png}

\choices
    {46, 45, 46}
    {46, 45, 53}
    {47, 45, 46}
    {45, 47, 53}
\end{problem}

\begin{problem}
设 \(a, b \in \R_{+}\) 是虚数单位，则 \(a^b = 0\) 是“复数 \(a + \frac{b}{\i}\) 纯虚数”的（\quad）

\choices
    {充分不必要条件}
    {必要不充分条件}
    {充分必要条件}
    {既不充分也不必要条件}
\end{problem}

\begin{problem}
如图所示是计算某年级 500 名学生期末考试(满分为 100 分)及格率 q 的程序框图，则图中空白框内应填入（\quad）

\choices
    {\(q = \frac{N}{M}\)}
    {\(q = \frac{M}{N}\)}
    {\(q = \frac{N}{M + N}\)}
    {\(q = \frac{M}{M + N}\)}
\end{problem}

\begin{problem}
已知圆 \(C: x^{2} + y^{2} - 4x = 0, l\) 是过点 \(P(3,0)\) 的直线，则（\quad）

\choices
    {\(l\) 与 \(C\) 相交}
    {\(l\) 与 \(C\) 相切}
    {\(l\) 与 \(C\) 相离}
    {以上三个选项均有可能}
\end{problem}

\begin{problem}
设向量 \(a = (1, \cos \theta)\) 与 \(b = (-1, 2 \cos \theta)\) 垂直，则 \(\cos 2\theta\) 等于（\quad）

\choices
    {\( \frac{\sqrt{2}}{2} \)}
    {\( \frac{1}{2} \)}
    {0}
    {\(-1\)}
\end{problem}

\begin{problem}
\bitmapfigure[max width=0.92\linewidth,max totalheight=7.6cm]{img/2012/shaanxi_liberal/q8_options.png}
\end{problem}

\begin{problem}
设函数 \( f(x) = \frac{2}{x} + \ln x \)，则（\quad）

\choices
    {\(x = \frac{1}{2}\) 为 \(f(x)\) 的极大值点}
    {\(x = \frac{1}{2}\) 为 \(f(x)\) 的极小值点}
    {\(x = 2\) 为 \(f(x)\) 的极大值点}
    {\(x = 2\) 为 \(f(x)\) 的极小值点}
\end{problem}

\begin{problem}
小王从甲地到乙地的往返时速分别为 a 和 b (a < b)，其全程的平均时速为 v，则（\quad）

\choices
    {\(a < v < \sqrt{ab}\)}
    {\(v = \sqrt{ab}\)}
    {\(\sqrt{ab} < v < \frac{a + b}{2}\)}
    {\(v = \frac{a + b}{2}\)}
\end{problem}

\section{填空题}

\begin{problem}
设函数 \( f(x)=\left\{\begin{aligned}\sqrt{x},&x\geqslant0,\\ \left(\frac{1}{2}\right)^{x},&x<0,\end{aligned}\right. \) 则 \( f(f(-4))= \)  \fillinblank{}.
\end{problem}

\begin{problem}
观察下列不等式: \[ 1 + \frac {1}{x ^{2}} < \frac {1}{3}, \] \[ 1 + \frac {1}{x ^{2}} + \frac {3}{4 ^{2}} < \frac {5}{4}, \] \[ 1 - \frac {1}{2 ^{2}} + \frac {3}{4 ^{2}} + \frac {5}{4 ^{2}} < \frac {7}{4}, \] 照此规律，第五个不等式为 \fillinblank{} \fillinblank{}.
\end{problem}

\begin{problem}
在 \(\triangle ABC\) 中，角 \(A, B, C\) 所对应的边长分别为 \(a, b, c\)，若 \(a = 2, B = \frac{\pi}{6}, c = 2\sqrt{3}\)，则 \(b = \fillinblank{}\).
\end{problem}

\begin{problem}
如图是抛物线形拱桥，当水面在 \(l\) 时，拱顶离水面 2 米，水面宽 4 米，水位下降 1 米后，水面宽 米 \fillinblank{}.
\end{problem}

\begin{problem}
三选一. 【A】若存在实数 x 使 \( |x - a| + |x - 1| \leqslant 3 \) 成立，则实数 a 的取值范围是 \fillinblank{}. 【B】如图，在圆 O 中，直径 AB 与弦 CD 垂直，垂足为 E， \( EF \perp DB \) . 垂足为 F，若 AB = 6，AE = 1，则 \( DF \cdot DB = \) .

【C】直线 \(2\rho \cos \theta = 1\) 与圆 \(\rho = 2\cos \theta\) 相交的长弦为
\end{problem}

\section{解答题}

\begin{problem}
已知等比数列 \(\{a_{n}\}\) 的公比为 \(q = \frac{1}{2}\). (1) 若 \( a_{3}=\frac{1}{4} \) ，求数列 \( \{a_{n}\} \) 的前 n 项和； (2) 证明：对任意 \(k \in \mathbf{N}_+\)，\(a_k, a_{k+2}, a_{k+1}\) 成等差数列.
\end{problem}

\begin{problem}
函数 \( f(x) = A \sin \left( \omega x - \frac{\pi}{6} \right) + 1 (A > 0, \omega > 0) \) 的最大值为3，其图象相邻两条对称轴之间的距离为 \( \frac{2}{3} \). (1) 求函数 \( f(x) \) 的解析式: (2) 设 \(\alpha \in \left(0, \frac{\pi}{2}\right), f\left(\frac{\alpha}{2}\right) = 2\)，求 \(\alpha\) 的值.
\end{problem}

\begin{problem}
假设甲乙两种品牌的同类产品在某地区市场上销售量相等，为了解他们的使用寿命，现从两种品牌的产品中分别随机抽取100个进行测试，结果统计如下：

(1) 估计甲品牌产品寿命小于 200 小时的概率; (2) 这两种品牌产品中，某个产品已使用了 200 小时，试估计该产品是甲品牌的概率.
\end{problem}

\begin{problem}
设函数 \( f_{n}(x) = x^{n} + bx + c (n \in \mathbf{N}_{+}, b, c \in \R) \). (1) 设 \(n \geqslant 2, b = 1, c = -1\)，证明: \(f_{n}(x)\) 在区间 \(\left(\frac{1}{2}, 1\right)\) 内存在唯一零点; (2) 设 \(n\) 为偶数，\(|f(-1)| \leqslant 1, |f(1)| \leqslant 1\)，求 \(b + 3c\) 的最小值和最大值; (3) 设 \(n = 2\)，若对任意 \(x_{1}, x_{2} \in [-1, 1]\)，有 \(|f_{2}(x_{1}) - f_{2}(x_{2})| \leqslant 4\)，求 \(b\) 的取值范围.
\end{problem}

\begin{problem}
直三棱柱 \(ABC - A_{1}B_{1}C_{1}\) 中，\(AB = AA_{1}, \angle CAB = \frac{\pi}{2}\). (1) 证明： \( CB_{1} \perp BA_{1} \) ; (2) 已知 \(AB = 2, BC = \sqrt{5}\)，求三棱锥 \(C_1 - ABA_1\) 的体积.
\end{problem}

\begin{problem}
已知椭圆 \(C_{1}: \frac{x^{2}}{4} + y^{2} = 1\)，椭圆 \(C_{2}\) 以 \(C_{1}\) 的长轴为短轴，且与 \(C_{1}\) 有相同的离心率. (1) 求椭圆 \(C_2\) 的方程； (2) 设 O 为坐标原点，点 A, B 分别在椭圆 \( C_{1} \) 和 \( C_{2} \) 上，\( \overrightarrow{OB} = 2\overrightarrow{OA} \) ，求直线 AB 的方程.
\end{problem}

\begin{problem}
设函数 \( f_{n}(x) = x^{n} + bx + c (n \in \mathbf{N}_{+}, b, c \in \R) \). (1) 设 \(n \geqslant 2, b = 1, c = -1\)，证明: \(f_n(x)\) 在区间 \(\left(\frac{1}{2}, 1\right)\) 内存在唯一零点; (2) 设 \(n\) 为偶数，\(|f(-1)| \leqslant 1, |f(1)| \leqslant 1\)，求 \(b + 3c\) 的最小值和最大值; (3) 设 \(n = 2\)，若对任意 \(x_1, x_2 \in [-1, 1]\)，有 \(|f_2(x_1) - f_2(x_2)| \leqslant 4\)，求 \(b\) 的取值范围.
\end{problem}

