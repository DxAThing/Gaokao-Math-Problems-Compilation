\chapter{2012年山东卷（理）}

\section{单选题}

\begin{problem}
若复数 \(z\) 满足 \(z(2 - \i) = 11 + 7\i\) （\(\i\) 为虚数单位），则 \(z\) 为（\quad）

\choices
    {\(3 + 5\i\)}
    {\(3 - 5\i\)}
    {\(-3 + 5\i\)}
    {\(-3 - 5\i\)}
\end{problem}

\begin{problem}
已知全集 \(U = \{0, 1, 2, 3, 4\}\)，集合 \(A = \{1, 2, 3\}\)，\(B = \{2, 4\}\)，则 \(\complement_U A \cup B =\)（\quad）

\choices
    {\(\{1, 2, 4\}\)}
    {\(\{2, 3, 4\}\)}
    {\(\{0, 2, 4\}\)}
    {\(\{0, 2, 3, 4\}\)}
\end{problem}

\begin{problem}
设 \(a > 0\) 且 \(a \neq 1\)，则“函数 \(f(x) = a^x\) 在 \(\R\) 上是减函数”是“函数 \(g(x) = (2 - a)x^2\) 在 \(\R\) 上是增函数”的（\quad）

\choices
    {充分不必要条件}
    {必要不充分条件}
    {充分必要条件}
    {既不充分也不必要条件}
\end{problem}

\begin{problem}
采用系统抽样方法从 960 人中抽取 32 人做问卷调查. 为此将他们随机编号为 1, 2, \ldots, 960，分组后在第一组采用简单随机抽样的方法抽到的号码为 9. 抽到的 32 人中，编号落入区间 \([1, 450]\) 的人做问卷 A，编号落入区间 \([451, 750]\) 的人做问卷 B，其余的人做问卷 C. 则抽到的人中，做问卷 B 的人数为（\quad）

\choices
    {7}
    {9}
    {10}
    {15}
\end{problem}

\begin{problem}
设变量 \(x, y\) 满足约束条件 \(\left\{ \begin{array}{l} x + 2y \geqslant 2, \\ 2x + y \leqslant 4, \\ 4x - y \geqslant -1, \end{array} \right.\) 则目标函数 \(z = 3x - y\) 的取值范围是（\quad）

\choices
    {\(\left[\frac{3}{2}, 6\right]\)}
    {\(\left[-\frac{3}{2}, -1\right]\)}
    {\([-1,6]\)}
    {\(\left[-6, \frac{3}{2}\right]\)}
\end{problem}

\begin{problem}
执行如图所示的程序框图，如果输入 \(a = 4\)，那么输出的 \(n\) 的值为（\quad）

\bitmapfigure[max width=0.3\linewidth,max totalheight=5.8cm]{img/2012/shandong_science/q6_fig1.png}

\choices
    {2}
    {3}
    {4}
    {5}
\end{problem}

\begin{problem}
若 \(\theta \in \left[\frac{\pi}{4}, \frac{\pi}{2}\right]\)，\(\sin 2\theta = \frac{3\sqrt{7}}{8}\)，则 \(\sin \theta =\)（\quad）

\choices
    {\(\frac{3}{5}\)}
    {\(\frac{4}{5}\)}
    {\(\frac{\sqrt{7}}{4}\)}
    {\(\frac{3}{4}\)}
\end{problem}

\begin{problem}
定义在 \(\R\) 的函数 \(f(x)\) 满足 \(f(x + 6) = f(x)\). 当 \(-3 \leqslant x < -1\) 时，\(f(x) = -(x + 2)^2\)；当 \(-1 \leqslant x < 3\) 时，\(f(x) = x\). 则 \(f(1) + f(2) + \cdots + f(2012) =\)（\quad）

\choices
    {335}
    {338}
    {1678}
    {2012}
\end{problem}

\begin{problem}
函数 \( f(x) = \frac{\cos 6x}{2^x - 2^{-x}} \) 的图象大致为（\quad）

\choices
    {\bitmapinclude[max width=2.9cm,max totalheight=2.6cm]{img/2012/shandong_science/q9_choice_a.png}}
    {\bitmapinclude[max width=2.9cm,max totalheight=2.6cm]{img/2012/shandong_science/q9_choice_b.png}}
    {\bitmapinclude[max width=2.9cm,max totalheight=2.6cm]{img/2012/shandong_science/q9_choice_c.png}}
    {\bitmapinclude[max width=2.9cm,max totalheight=2.6cm]{img/2012/shandong_science/q9_choice_d.png}}
\end{problem}

\begin{problem}
已知椭圆 \(C: \frac{x^2}{a^2} + \frac{y^2}{b^2} = 1 \ (a > b > 0)\) 的离心率为 \(\frac{\sqrt{3}}{2}\). 双曲线 \(x^2 - y^2 = 1\) 的渐近线与椭圆 \(C\) 有四个交点，以这四个交点为顶点的四边形的面积为 16，则椭圆 \(C\) 的方程为（\quad）

\choices
    {\(\frac{x^2}{8} + \frac{y^2}{2} = 1\)}
    {\(\frac{x^2}{12} + \frac{y^2}{6} = 1\)}
    {\(\frac{x^2}{16} + \frac{y^2}{4} = 1\)}
    {\(\frac{x^2}{20} + \frac{y^2}{5} = 1\)}
\end{problem}

\begin{problem}
现有 16 张不同的卡片，其中红色、黄色、蓝色、绿色卡片各 4 张，从中任取 3 张. 要求这 3 张卡片不能是同一种颜色，且红色卡片至多 1 张. 不同的取法种数为（\quad）

\choices
    {232}
    {252}
    {472}
    {484}
\end{problem}

\begin{problem}
设函数 \( f(x) = \frac{1}{x} \)，\( g(x) = ax^2 + bx \ (a, b \in \R, a \neq 0)\). 若 \( y = f(x) \) 的图象与 \( y = g(x) \) 图象有且仅有两个不同的公共点 \( A(x_1, y_1), B(x_2, y_2)\)，则下列判断正确的是（\quad）

\choices
    {当 \(a < 0\) 时，\(x_{1} + x_{2} < 0\)，\(y_{1} + y_{2} > 0\)}
    {当 \(a < 0\) 时，\(x_{1} + x_{2} > 0\)，\(y_{1} + y_{2} < 0\)}
    {当 \(a > 0\) 时，\(x_{1} + x_{2} < 0\)，\(y_{1} + y_{2} < 0\)}
    {当 \(a > 0\) 时，\(x_{1} + x_{2} > 0\)，\(y_{1} + y_{2} > 0\)}
\end{problem}

\section{填空题}

\begin{problem}
若不等式 \(|kx - 4| \leqslant 2\) 的解集为 \(\{x \mid 1 \leqslant x \leqslant 3\}\)，则实数 \(k = \) \fillinblank{}.
\end{problem}

\begin{problem}
如图，正方体 \(ABCD - A_{1}B_{1}C_{1}D_{1}\) 的棱长为 1，\(E, F\) 分别为线段 \(AA_{1}, B_{1}C\) 上的点，则三棱锥 \(D_{1} - EDF\) 的体积为 \fillinblank{}.

\bitmapfigure[max width=0.28\linewidth,max totalheight=5.8cm]{img/2012/shandong_science/q14_fig1.png}
\end{problem}

\begin{problem}
设 \(a > 0\)，若曲线 \(y = \sqrt{x}\) 与直线 \(x = a, y = 0\) 所围成封闭图形的面积为 \(a^2\)，则 \(a = \) \fillinblank{}.
\end{problem}

\begin{problem}
如图，在平面直角坐标系 \(xOy\) 中，一单位圆的圆心的初始位置在 \((0, 1)\)，此时圆上一点 \(P\) 的位置在 \((0, 0)\). 圆在 \(x\) 轴上沿正向滚动. 当圆滚动到圆心位于 \((2, 1)\) 时，\(\overrightarrow{OP}\) 的坐标为 \fillinblank{} \fillinblank{}.

\bitmapfigure[max width=0.42\linewidth,max totalheight=4.8cm]{img/2012/shandong_science/q16_fig1.png}
\end{problem}

\section{解答题}

\begin{problem}
已知向量 \(\bs{m} = (\sin x, 1)\)，\(\bs{n} = (\sqrt{3}A \cos x, \frac{A}{2} \cos 2x)\)（\(A > 0\)），函数 \(f(x) = \bs{m} \cdot \bs{n}\) 的最大值为 6.
\begin{enumerate}[label={（\arabic*）},itemsep=0.2em]
\item 求 \(A\)；
\item 将函数 \(y = f(x)\) 的图象向左平移 \(\frac{\pi}{12}\) 个单位，再将所得图象上各点的横坐标缩短为原来的 \(\frac{1}{2}\) 倍，纵坐标不变，得到函数 \(y = g(x)\) 的图象，求 \(g(x)\) 在 \(\left[0, \frac{5\pi}{24}\right]\) 上的值域.
\end{enumerate}
\end{problem}

\begin{problem}
在如图所示的几何体中，四边形 \(ABCD\) 是等腰梯形，\(AB // CD\)，\(\angle DAB = 60^{\circ}\)，\(FC \perp\) 平面 \(ABCD\)，\(AE \perp BD\)，\(CB = CD = CF\).
\begin{enumerate}[label={（\arabic*）},itemsep=0.2em]
\item 求证: \(BD \perp\) 平面 \(AED\)；
\item 求二面角 \(F - BD - C\) 的余弦值.
\end{enumerate}

\bitmapfigure[max width=0.42\linewidth,max totalheight=4.8cm]{img/2012/shandong_science/q18_fig1.png}
\end{problem}

\begin{problem}
在等差数列 \(\{a_{n}\}\) 中，\(a_{3} + a_{4} + a_{5} = 84\)，\(a_{9} = 73\).
\begin{enumerate}[label={（\arabic*）},itemsep=0.2em]
\item 求数列 \(\{a_{n}\}\) 的通项公式；
\item 对任意 \(m \in \mathbf{N}^*\)，将数列 \(\{a_n\}\) 中落入区间 \((9^m, 9^{2m})\) 内的项的个数记为 \(b_m\)，求数列 \(\{b_m\}\) 的前 \(m\) 项和 \(S_m\).
\end{enumerate}
\end{problem}

\begin{problem}
已知函数 \( f(x) = \frac{\ln x + k}{x} \)（\(k\) 为常数，\(\e = 2.71828 \cdots\) 是自然对数的底数），曲线 \( y = f(x) \) 在点 \( (1, f(1)) \) 处的切线与 \( x \) 轴平行.
\begin{enumerate}[label={（\arabic*）},itemsep=0.2em]
\item 求 \(k\) 的值；
\item 求 \( f(x) \) 的单调区间；
\item 设 \( g(x) = (x^2 + x)f'(x) \)，其中 \( f'(x) \) 为 \( f(x) \) 的导函数. 证明：对任意 \( x > 0, g(x) < 1 + \e^{-2} \).
\end{enumerate}
\end{problem}

\begin{problem}
现有甲乙两个靶，某射手向甲靶射击一次命中的概率为 \(\frac{3}{4}\)，命中得 1 分，没有命中得 0 分；向乙靶射击两次，每次命中的概率为 \(\frac{2}{3}\)，每命中一次得 2 分，没有命中得 0 分. 该射手每次射击的结果相互独立，假设该射手完成以上三次射击.
\begin{enumerate}[label={（\arabic*）},itemsep=0.2em]
\item 求该射手恰好命中一次的概率；
\item 求该射手的总得分 \(X\) 的分布列及数学期望 \(EX\).
\end{enumerate}
\end{problem}

\begin{problem}
在平面直角坐标系 \(xOy\) 中，\(F\) 是抛物线 \(C: x^2 = 2py \ (p > 0)\) 的焦点，\(M\) 是抛物线 \(C\) 上位于第一象限内的任意一点，过 \(M, F, O\) 三点的圆的圆心为 \(Q\)，点 \(Q\) 到抛物线 \(C\) 的准线的距离为 \(\frac{3}{4}\).
\begin{enumerate}[label={（\arabic*）},itemsep=0.2em]
\item 求抛物线 \(C\) 的方程；
\item 是否存在点 \(M\)，使得直线 \(MQ\) 与抛物线 \(C\) 相切于点 \(M\)? 若存在，求出点 \(M\) 的坐标；若不存在，说明理由；
\item 若点 \(M\) 的横坐标为 \(\sqrt{2}\)，直线 \(l: y = kx + \frac{1}{4}\) 与抛物线 \(C\) 有两个不同的交点 \(A, B\)，\(l\) 与圆 \(Q\) 有两个不同的交点 \(D, E\)，求当 \(\frac{1}{2} \leqslant k \leqslant 2\) 时，\(|AB|^2 + |DE|^2\) 的最小值.
\end{enumerate}
\end{problem}
