\chapter{2013年浙江卷（文）}

\section{单选题}

\begin{problem}
设集合 \( S = \{x \mid x > -2\} \)，\( T = \{x \mid -4 \leqslant x \leqslant 1\} \)，则 \( S \cap T = \)（\quad）

\choices
    {\([-4, +\infty)\)}
    {\((-2, +\infty)\)}
    {\([-4, 1]\)}
    {\((-2, 1]\)}
\end{problem}

\begin{problem}
已知 i 是虚数单位，则 \( (2 + \i)(3 + \i) = \)（\quad）

\choices
    {5 - 5i}
    {7 - 5i}
    {\( 5 + 5\i \)}
    {\(7 + 5\i\)}
\end{problem}

\begin{problem}
若 \(\alpha \in \R\)，则“\(\alpha = 0\)”是“\(\sin \alpha < \cos \alpha\)”的（\quad）

\choices
    {充分不必要条件}
    {必要不充分条件}
    {充分必要条件}
    {既不充分也不必要条件}
\end{problem}

\begin{problem}
设 \(m, n\) 是两条不同的直线，\(\alpha, \beta\) 是两个不同的平面（\quad）

\choices
    {若 \(m // \alpha, n // \alpha\)，则 \(m // n\)}
    {若 \(m // \alpha, m // \beta\)，则 \(\alpha // \beta\)}
    {若 \(m // n, m \perp \alpha\)，则 \(n \perp \alpha\)}
    {若 \(m // \alpha, \alpha \perp \beta\)，则 \(m \perp \beta\)}
\end{problem}

\begin{problem}
已知某几何体的三视图(单位: cm) 如图所示，则该几何体的体积是（\quad）

\bitmapfigure[max width=0.62\linewidth,max totalheight=4.8cm]{img/2013/zhejiang_liberal/q5_fig1.png}

\choices
    {\(108\mathrm{cm}^3\)}
    {\(100\mathrm{cm}^3\)}
    {\(92~\mathrm{cm}^3\)}
    {\(84~\mathrm{cm}^3\)}
\end{problem}

\begin{problem}
函数 \( f(x) = \sin x\cos x + \frac{\sqrt{3}}{2}\cos 2x \) 的最小正周期和振幅分别是（\quad）

\choices
    {\(\pi, 1\)}
    {\(\pi, 2\)}
    {\(2\pi, 1\)}
    {\(2\pi, 2\)}
\end{problem}

\begin{problem}
已知 \(a, b, c \in \R\)，函数 \(f(x) = ax^2 + bx + c\)，若 \(f(0) = f(4) > f(1)\)，则（\quad）

\choices
    {\(a > 0, 4a + b = 0\)}
    {\(a < 0,4a + b = 0\)}
    {\(a > 0, 2a + b = 0\)}
    {\(a < 0,2a + b = 0\)}
\end{problem}

\begin{problem}
已知函数 \( y = f(x) \) 的图象是下列四个图象之一，且其导函数 \( y = f'(x) \) 的图象如图所示，则该函数的图象是（\quad）

\bitmapfigure[max width=3.2cm,max totalheight=3.2cm]{img/2013/zhejiang_liberal/q8_fig1.png}

\choices
    {\bitmapinclude[max width=2.7cm,max totalheight=2.5cm]{img/2013/zhejiang_liberal/q8_choice_a.png}}
    {\bitmapinclude[max width=2.7cm,max totalheight=2.5cm]{img/2013/zhejiang_liberal/q8_choice_b.png}}
    {\bitmapinclude[max width=2.7cm,max totalheight=2.5cm]{img/2013/zhejiang_liberal/q8_choice_c.png}}
    {\bitmapinclude[max width=2.7cm,max totalheight=2.5cm]{img/2013/zhejiang_liberal/q8_choice_d.png}}
\end{problem}

\begin{problem}
如图，\(F_{1}, F_{2}\) 是椭圆 \(C_{1}: \frac{x^{2}}{4} + y^{2} = 1\) 与双曲线 \(C_{2}\) 的公共焦点，\(A, B\) 分别是 \(C_{1}, C_{2}\) 在第二、四象限的公共点. 若四边形 \(AF_{1}BF_{2}\) 为矩形，则 \(C_{2}\) 的离心率是（\quad）

\bitmapfigure[max width=0.58\linewidth,max totalheight=4.8cm]{img/2013/zhejiang_liberal/q9_fig1.png}

\choices
    {\(\sqrt{2}\)}
    {\(\sqrt{3}\)}
    {\( \frac{3}{2} \)}
    {\(\frac{\sqrt{6}}{2}\)}
\end{problem}

\begin{problem}
设 \(a, b \in \R\)，定义运算“\(\wedge\)”和“\(\vee\)”如下: \(a \wedge b = \begin{cases} a, & a \leqslant b, \\ b, & a > b, \end{cases}\) \(a \vee b = \begin{cases} b, & a \leqslant b, \\ a, & a > b. \end{cases}\) 若正数 \(a, b, c, d\) 满足 \(ab \geqslant 4, c + d \leqslant 4\)，则（\quad）

\choices
    {\(a \wedge b \geqslant 2, c \wedge d \leqslant 2\)}
    {\(a \wedge b \geqslant 2, c \vee d \geqslant 2\)}
    {\(a \vee b \geqslant 2, c \wedge d \leqslant 2\)}
    {\(a \vee b \geqslant 2, c \vee d \geqslant 2\)}
\end{problem}

\section{填空题}

\begin{problem}
已知函数 \( f(x) = \sqrt{x - 1} \). 若 \( f(a) = 3 \)，则实数 \( a = \)  \fillinblank{}.
\end{problem}

\begin{problem}
从3男3女共6名同学中任选2名(每名同学被选中的机会均等). 这2名都是女同学的概率等于 \fillinblank{} \fillinblank{}.
\end{problem}

\begin{problem}
直线 \( y = 2x + 3 \) 被圆 \( x^{2} + y^{2} - 6x - 8y = 0 \) 所截得的弦长等于 \fillinblank{} \fillinblank{}.
\end{problem}

\begin{problem}
若某程序框图如图所示，则该程序运行后输出的值等于 \fillinblank{} \fillinblank{}.

\bitmapfigure[max width=0.33\linewidth,max totalheight=6cm]{img/2013/zhejiang_liberal/q14_fig1.png}
\end{problem}

\begin{problem}
设 \(z = kx + y\)，其中实数 \(x, y\) 满足 \(\left\{ \begin{array}{l} x \geqslant 2, \\ x - 2y + 4 \geqslant 0, \\ 2x - y - 4 \leqslant 0. \end{array} \right.\) 若 \(z\) 的最大值为 12，则实数 \(k = \)  \fillinblank{}.
\end{problem}

\begin{problem}
设 \(a, b \in \R\)，若 \(x \geqslant 0\) 时恒有 \(0 \leqslant x^4 - x^3 + ax + b \leqslant (x^2 - 1)^2\)，则 \(ab = \)  \fillinblank{}.
\end{problem}

\begin{problem}
设 \(e_1, e_2\) 为单位向量，非零向量 \(b = x e_1 + y e_2, x, y \in \R\). 若 \(e_1, e_2\) 的夹角为 \(\frac{\pi}{6}\)，则 \(\frac{|x|}{|b|}\) 的最大值等于 \fillinblank{} \fillinblank{}.
\end{problem}

\section{解答题}

\begin{problem}
在锐角 \(\triangle ABC\) 中，内角 \(A, B, C\) 的对边分别为 \(a, b, c\)，且 \(2a \sin B = \sqrt{3} b\). (1) 求角 \(A\) 的大小; (2) 若 \(a = 6, b + c = 8\)，求 \(\triangle ABC\) 的面积.
\end{problem}

\begin{problem}
在公差为 \(d\) 的等差数列 \(\{a_{n}\}\) 中，已知 \(a_1 = 10\) ，且 \(a_1, 2a_2 + 2, 5a_3\) 成等比数列. (1) 求 \( d, a_{n} \) ; (2) 若 d < 0，求 \( \left|a_{1}\right| + \left|a_{2}\right| + \left|a_{3}\right| + \cdots + \left|a_{n}\right| \) .
\end{problem}

\begin{problem}
已知 \(a \in \R\)，函数 \(f(x) = 2x^3 - 3(a + 1)x^2 + 6ax\). (1) 若 \(a = 1\)，求曲线 \(y = f(x)\) 在点 \((2, f(2))\) 处的切线方程； (2) 若 \( |a| > 1 \)，求 \( f(x) \) 在闭区间 \([0, 2|a|]\) 上的最小值.
\end{problem}

\begin{problem}
已知抛物线 \(C\) 的顶点为 \(O(0,0)\). 焦点为 \(F(0,1)\). (1) 求抛物线 C 的方程; (2) 过点 \(F\) 作直线交抛物线 \(C\) 于 \(A, B\) 两点. 若直线 \(AO, BO\) 分别交直线 \(l: y = x - 2\) 于 \(M, N\) 两点，求 \(|MN|\) 的最小值.
\end{problem}

\begin{problem}
如图，在四棱锥 \(P - ABCD\) 中，\(PA \perp\) 平面 \(ABCD\)，\(AB = BC = 2\)，\(AD = CD = \sqrt{7}\)，\(PA = \sqrt{3}\)，\(\angle ABC = 120^\circ\)，\(G\) 为线段 \(PC\) 上的点. (1) 证明: \(BD \perp\) 平面 \(APC\); (2) 若 \(G\) 为 \(PC\) 的中点，求 \(DG\) 与平面 \(APC\) 所成的角的正切值; (3) 若 \(G\) 满足 \(PC \perp \) 平面 \(BGD\)，求 \(\frac{PG}{GC}\) 的值.

\bitmapfigure[max width=0.40\linewidth,max totalheight=6cm]{img/2013/zhejiang_liberal/q20_fig1.png}
\end{problem}
