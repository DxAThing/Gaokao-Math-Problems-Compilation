\chapter{2011年四川卷（文）}

\section{单选题}

\begin{problem}
若全集: \(M = \{1, 2, 3, 4, 5\}\)，\(N = \{2, 4\}\)，则 \(\complement_{M} N =\)（\quad）

\choices
    {\(\varnothing\)}
    {\(\{1, 3, 5\}\)}
    {\( \{2, 4\} \)}
    {\(\{1, 2, 3, 4, 5\}\)}
\end{problem}

\begin{problem}
有一容量为 66 的样本数据的分组及各组的频数如下: {11.5, 15.9} 5 {19.5, 23.5} 9 {25.5, 27.5} 18 {27.5,3,4,5} 11 {31.5,35,35} 12 {35.5,39.5} 7 {39.5,43.5} 3 根据样本的频率分布估计，大于 31.5 岁的数据约占（\quad）

\choices
    {\( \frac{2}{11} \)}
    {\( \frac{1}{3} \)}
    {\( \frac{1}{2} \)}
    {\(\frac{2}{3}\)}
\end{problem}

\begin{problem}
圆 \(x^{2} + y^{2} - 4x + 6y = 0\) 的圆心坐标是（\quad）

\choices
    {(2,3)}
    {\((-2, 3)\)}
    {\((-2, -3)\)}
    {\((2, -3)\)}
\end{problem}

\begin{problem}
函数 \( y = \left(\frac{1}{2}\right)^x + 1 \) 的图象关于直线 \( y = x \) 对称的图象大致是（\quad）

\choices
    {}
    {}
    {}
    {}
\end{problem}

\begin{problem}
\(z = x^{2}\) 是 \(x^{2} = 9\) 的（\quad）

\choices
    {充分而不必要的条件}
    {必要而不充分的条件}
    {充要条件}
    {既不充分也不必要的条件}
\end{problem}

\begin{problem}
\(l_{1}, l_{2}, l_{3}\) 是空间三条不同的直线，则下列命题正确的是（\quad）

\choices
    {\(l_{1} \perp l_{2}, l_{2} \perp l_{3} \Rightarrow l_{1} // l_{3}\)}
    {\(l_{1} \perp l_{2}, l_{2} // l_{3} \Rightarrow l_{1} \perp l_{3}\)}
    {\(l_{1} // l_{2} // l_{3} \Rightarrow l_{1}, l_{2}, l_{3}\) 共面}
    {\(l_{1}, l_{2}, l_{3}\) 共点 \(\Rightarrow l_{1}, l_{2}, l_{3}\) 共面}
\end{problem}

\begin{problem}
如图，正六边形 \(ABCDEF\) 中，\(\overrightarrow{BA} + \overrightarrow{CD} + \overrightarrow{EF} =\)（\quad）

\choices
    {0}
    {\(\overrightarrow{BE}\)}
    {\(\overrightarrow{AD}\)}
    {\(\overrightarrow{CP}\)}
\end{problem}

\begin{problem}
在 \(\triangle ABC\) 中，\(\sin^2 A \leqslant \sin^2 B + \sin^2 C - \sin B \sin C\)，则 \(A\) 的取值范围是（\quad）

\choices
    {\(\left(0, \frac{\pi}{6}\right]\)}
    {\(\left(\frac{\pi}{6}, \pi\right)\)}
    {\(\left(0, \frac{\pi}{3}\right]\)}
    {\(\left(\frac{\pi}{3}, \pi\right)\)}
\end{problem}

\begin{problem}
数列 \(\{a_{n}\}\) 的前 \(n\) 项和为 \(S_{n}\)，若 \(a_{1} = 1, a_{n+1} = 3S_{n} (n \geqslant 1)\)，则 \(a_{6} = \fillinblank{}\)（\quad）

\choices
    {\(3 \times 4^{5}\)}
    {\(3 \times 4^{4} + 1\)}
    {\(4^{5}\)}
    {\(4^{5} + 1\)}
\end{problem}

\begin{problem}
某运输公司有 12 名驾驶员和 19 名工人，有 8 辆载重量为 10 吨的甲型卡车和 7 辆载重量为 6 吨的乙型卡车. 某天需运往 A 地至少 72 吨的货物，派用的每辆车需载满且只运送一次. 派用的每辆甲型卡车需配 2 名工人. 运送一次可得利润 450 元; 派用的每辆乙型卡车需配 1 名工人. 运送一次可得利润 350 元. 该公司合理计划当天派用甲、乙卡车的车辆数，可得最大利润 x 万元（\quad）

\choices
    {4650 元}
    {4700 元}
    {4900 元}
    {5000 元}
\end{problem}

\begin{problem}
在抛物线 \( y = x^{2} + ax - 5 (a \neq 0) \) 上取横坐标为 \( x_{1} = -4, x_{2} = 2 \) 的两点，经过两点引一条斜线，有平行于该割线的一条直线同时与抛物线和圆 \( 5x^{2} + 5y^{2} = 36 \) 相切，则抛物线顶点的坐标为（\quad）

\choices
    {\((-2, - 9)\)}
    {\((0, - 5)\)}
    {\((2, - 9)\)}
    {\((1, - 6)\)}
\end{problem}

\begin{problem}
在集合 \(\{1, 2, 3, 4, 5\}\) 中任取一个偶数 \(a\) 和一个奇数 \(b\) 构成以原点为起点的向量 \(\alpha = (a, b)\). 从所有得到的以原点为起点的向量中任取两个向量为邻边作平行四边形，记所有作成的平行四边形的个数为 \(n\)，其中面积等于 2 的平行四边形的个数为 \(m\)，则 \(\frac{n!}{m!} =\)（\quad）

\choices
    {\(\frac{2}{15}\)}
    {\(\frac{1}{5}\)}
    {\(\frac{4}{15}\)}
    {\(\frac{1}{3}\)}
\end{problem}

\section{填空题}

\begin{problem}
\((x + 1)^{9}\) 的展开式中 \(x^{3}\) 的系数是 \fillinblank{}. (用数字作答)
\end{problem}

\begin{problem}
双曲线 \(\frac{x^2}{4} - \frac{y^2}{9} = 1\) 上一点 \(P\) 到双曲线右焦点的距离是 4，那么点 \(P\) 到左准线的距离是 \fillinblank{} \fillinblank{}.
\end{problem}

\begin{problem}
如图，半径为 4 的球 \(O\) 中有一内接圆柱. 当圆柱的侧面积最大时，球的表面积与该圆柱的侧面积之差是 \fillinblank{} \fillinblank{}.
\end{problem}

\begin{problem}
函数 \( f(x) \) 的定义域为 \( A \)，若 \( x_{1}, x_{2} \in A \) 且 \( f(x_{1}) = f(x_{2}) \) 时总有 \( x_{1} = x_{2} \)，则称 \( f(x) \) 为单函数. 例如，函数 \( f(x) = 2x + 1 (x \in \R) \) 是单函数. 下列命题: \circled{1} 函数 \( f(x)=x(x\in\R) \) 是单函数; \circled{2} 指数函数 \( f(x)=2^{x}(x\in\R) \) 是单函数; \circled{3} 若 \( f(x) \) 为单函数，\( x_{1}, x_{2} \in A \) 且 \( x_{1} \neq x_{2} \) ，则 \( f(x_{1}) \neq f(x_{2}) \) ; \circled{4} 在定义域上具有单调性的函数一定是单函数. 其中的真命题是 \fillinblank{}. (写出所有真命题的编号)
\end{problem}

\section{解答题}

\begin{problem}
本着健康、低碳的生活理念，租自行车骑游的人越来越多. 某自行车租车点的收费标准是每车每次租车时间不超过两小时免费，超过两小时的即每小时收费2元（不足1小时的部分按1小时计算）. 有甲、乙两人相互独立来该租车点租车骑游（各租一车一次）. 设甲、乙不超过两小时还车的概率分别为 \( \frac{1}{4} \) 、 \( \frac{1}{3} \) ；两小时以上且不超过三小时还车的概率分别是 \( \frac{1}{2} \) 、 \( \frac{1}{4} \) ；两人租车时间都不会超过四小时. (1) 分别求出甲、乙在三小时以上且不超过四小时还车的概率; (2) 求甲、乙两人所付的租车费用之和小于 6 元的概率.
\end{problem}

\begin{problem}
已知函数 \( f(x) = \sin \left( x + \frac{7\pi}{4} \right) + \cos \left( x - \frac{3\pi}{4} \right), x \in \R \). (1) 求 \( f(x) \) 的最小正周期和最小值； (2) 已知 \( \cos(\beta-\alpha)=\frac{1}{4} \) ， \( \cos(\beta+\alpha)=-\frac{4}{5} \) ， \( 0<\alpha<\beta\leqslant\frac{\pi}{2} \) ，求证： \( [f(\beta)]^{2}-2=0 \) .
\end{problem}

\begin{problem}
如图，在直三棱柱 \(ABC - A_{1}B_{1}C_{1}\) 中，\(\angle BAC = 90^{\circ}\)，\(AB = AC = AA_{1} = 1\). 延长 \(A_{1}C_{1}\) 至点 \(P\)，使 \(C_{1}P = A_{1}C_{1}\)，连接 \(AP\) 交棱 \(CC_{1}\) 于点 \(D\). (1) 求证: \( PB_{1} // \) 平面 \( BDA_{1} \) ; (2) 求二面角 \(A - A_{1}D - B\) 的平面角的余弦值.
\end{problem}

\begin{problem}
过点 \(C(0,1)\) 的椭圆 \(\frac{x^2}{3} + \frac{y^2}{5} = 1 (a > b > 0)\) 的离心率为 \(\frac{\sqrt{3}}{3}\)，椭圆与 \(x\) 轴交于两点 \(A(a,0), B(-a,0)\)，过点 \(C\) 的直线 \(l\) 与椭圆交于另一点 \(D\)，并与 \(x\) 轴交于点 \(P\)，直线 \(AC\) 与直线 \(BD\) 交于点 \(Q\). (1) 当直线 l 过椭圆右焦点时，求线段 CD 的长; (2) 当点 P 异于点 B 时，求证: \( \overrightarrow{OP} \cdot \overrightarrow{OQ} \) 为定值.
\end{problem}

\begin{problem}
已知函数 \( f(x) = \frac{2}{3} x + \frac{1}{2}, h(x) = \sqrt{x} \). (1) 设函数 \( F(x) = 18f(x) - x^2 [h(x)]^2 \)，求 \( F(x) \) 的单调区间与极值； (2) 设 \(a \in \R\)，解关于 \(x\) 的方程 \(\lg \left[\frac{3}{2} f(x - 1) - \frac{3}{4}\right] = 2 \lg h(a - x) - 2 \lg h(4 - x)\). (3) 设 \(n \in \mathbf{N}^*\). 证明: \(f(n)h(n) - [h(1) + h(2) + \cdots + h(n)] \geqslant \frac{1}{6}\).
\end{problem}

\begin{problem}
已知 \(\{a_{n}\}\) 是以 \(a\) 为首项，\(q\) 为公比的等比数列，\(S_{n}\) 为它的前 \(n\) 项和. (1) 当 \( S_{1}, S_{3}, S_{4} \) 成等差数列时，求 q 的值; (2) 当 \( S_{m} \) ，\( S_{n} \) ，\( S_{l} \) 成等差数列时，求证: 对任意自然数 k, \( a_{m+k} \) ，\( a_{n+k} \) ，\( a_{l+k} \) 也成等差数列.
\end{problem}

