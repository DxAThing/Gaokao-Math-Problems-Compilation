\chapter{2011年上海卷（秋文）}

\section{填空题}

\begin{problem}
若全集 \(U = \R\)，集合 \(A = \{x \mid x \geqslant 1\}\)，则 \(\complement_U A =\fillinblank{}\).
\end{problem}

\begin{problem}
\(\lim\limits_{n\to \infty}\left(1 - \frac{3n}{n + 3}\right) =\fillinblank{}\).
\end{problem}

\begin{problem}
若函数 \( f(x) = 2x + 1 \) 的反函数为 \( f^{-1}(x) \)，则 \( f^{-1}(-2) = \)\fillinblank{}.
\end{problem}

\begin{problem}
函数 \( y = 2\sin x - \cos x \) 的最大值为\fillinblank{}.
\end{problem}

\begin{problem}
若直线 \(l\) 过点 \( (3,4) \)，且 \( (1,2) \) 是它的一个法向量，则直线 \(l\) 的方程为\fillinblank{}.
\end{problem}

\begin{problem}
不等式 \(\frac{1}{x} < 1\) 的解集为\fillinblank{}.
\end{problem}

\begin{problem}
若一个圆锥的主视图（如图所示）是边长为 3， 3， 2 的三角形，则该圆锥的侧面积为\fillinblank{}.
\begin{center}
\bitmapinclude[max width=0.24\linewidth,max totalheight=4.8cm]{img/2011/shanghai_liberal/q7_fig1.png}
\end{center}
\end{problem}

\begin{problem}
在相距 2 千米的 \(A\)，\(B\) 两点处测量目标点 \(C\)，若 \( \angle CAB = 75^{\circ} \)，\( \angle CBA = 60^{\circ} \)，则 \(A\)，\(C\) 两点之间的距离是千米\fillinblank{}.
\end{problem}

\begin{problem}
若变量 \(x\)，\(y\) 满足条件 \(\left\{ \begin{array}{l} 3x - y \leqslant 0, \\ x - 3y + 5 \geqslant 0, \end{array} \right.\) 则 \(z = x + y\) 的最大值为\fillinblank{}.
\end{problem}

\begin{problem}
课题组进行城市空气质量调查，按地域把 24 个城市分成甲，乙，丙三组，对应的城市数分别为 4，12，8，若用分层抽样抽取 6 个城市，则丙组中应抽取的城市数为\fillinblank{}.
\end{problem}

\begin{problem}
行列式 \(\left| \begin{array}{cc}a & b\\ c & d \end{array} \right|\) \((a, b, c, d \in \{-1,1,2\})\) 所有可能的值中，最大的是\fillinblank{}.
\end{problem}

\begin{problem}
在正三角形 \(ABC\) 中，\(D\) 是 \(BC\) 上的点. 若 \(AB = 3\)，\(BD = 1\)，则 \(\overrightarrow{AB} \cdot \overrightarrow{AD} =\)\fillinblank{}.
\end{problem}

\begin{problem}
随机抽取的9位同学中，至少有2位同学在同一月份出生的概率为\fillinblank{}. （默认每个月的天数相同，结果精确到0.001）
\end{problem}

\begin{problem}
设 \(g(x)\) 是定义在 \(\R\) 上，以 1 为周期的函数，若函数 \(f(x) = x + g(x)\) 在区间 \([0,1]\) 上的值域为 \([-2,5]\)，则 \(f(x)\) 在区间 \([0,3]\) 上的值域为\fillinblank{}.
\end{problem}

\section{单选题}

\begin{problem}
下列函数中，既是偶函数，又在区间 \((0, +\infty)\) 上单调递减的是（\quad）

\choices
    {\( y = x^{-2} \)}
    {\( y = x^{-1} \)}
    {\( y = x^{2} \)}
    {\( y = x^{\frac{1}{3}} \)}
\end{problem}

\begin{problem}
若 \(a\)，\(b \in \R\)，且 \(ab > 0\)，则下列不等式中，恒成立的是（\quad）

\choices
    {\(a^2 + b^2 > 2ab\)}
    {\(a + b \geqslant 2\sqrt{ab}\)}
    {\(\frac{1}{a} + \frac{1}{b} > \frac{2}{\sqrt{ab}}\)}
    {\(\frac{b}{a} + \frac{a}{b} \geqslant 2\)}
\end{problem}

\begin{problem}
若三角方程 \(\sin x = 0\) 与 \(\sin 2x = 0\) 的解集分别为 \(E\)，\(F\)，则（\quad）

\choices
    {\(E \subsetneq F\)}
    {\(E \supsetneq F\)}
    {\(E = F\)}
    {\(E \cap F = \varnothing\)}
\end{problem}

\begin{problem}
设 \(A_{1}\)，\(A_{2}\)，\(A_{3}\)，\(A_{4}\) 是平面上给定的 4 个不同点，则使 \(\overrightarrow{MA_{1}} + \overrightarrow{MA_{2}} + \overrightarrow{MA_{3}} + \overrightarrow{MA_{4}} = \overrightarrow{0}\) 成立的点 \(M\) 的个数为（\quad）

\choices
    {0}
    {1}
    {2}
    {4}
\end{problem}

\section{解答题}

\begin{problem}
已知复数 \(z_{1}\) 满足 \((z_{1} - 2)(1 + \i) = 1 - \i\)（\(\i\) 为虚数单位），复数 \(z_{2}\) 的虚部为 2，且 \(z_{1} \cdot z_{2}\) 是实数，求 \(z_{2}\).
\end{problem}

\begin{problem}
已知函数 \(f(x) = a \cdot 2^{x} + b \cdot 3^{x}\)，其中常数 \(a\)，\(b\) 满足 \(a \cdot b \neq 0\).
\begin{enumerate}
\item 若 \(a \cdot b > 0\)，判断函数 \(f(x)\) 的单调性；
\item 若 \(a \cdot b < 0\)，求 \(f(x + 1) > f(x)\) 时的 \(x\) 的取值范围.
\end{enumerate}
\end{problem}

\begin{problem}
已知椭圆 \(C: \frac{x^2}{m^2} + y^{2} = 1\)（常数 \(m > 1\)），\(P\) 是曲线 \(C\) 上的动点，\(M\) 是曲线 \(C\) 的右顶点，定点 \(A\) 的坐标为 \((2,0)\).
\begin{enumerate}
\item 若 \(M\) 与 \(A\) 重合，求曲线 \(C\) 的焦点坐标；
\item 若 \(m = 3\)，求 \(|PA|\) 的最大值与最小值；
\item 若 \(|PA|\) 的最小值为 \(|MA|\)，求实数 \(m\) 的取值范围.
\end{enumerate}
\end{problem}

\begin{problem}
已知数列 \(\{a_{n}\}\) 和 \(\{b_{n}\}\) 的通项公式分别为 \(a_{n} = 3n + 6\)，\(b_{n} = 2n + 7\)（\(n \in \mathbf{N}^{*}\)），将集合 \(\{x \mid x = a_{n}, n \in \mathbf{N}^{*}\} \cup \{x \mid x = b_{n}, n \in \mathbf{N}^{*}\}\) 中的元素从小到大依次排列，构成数列 \(c_{1}\)，\(c_{2}\)，\(c_{3}\)，\(\cdots\)，\(c_{n}\)，\(\cdots\).
\begin{enumerate}
\item 求三个最小的数，使它们既是数列 \(\{a_{n}\}\) 中的项，又是数列 \(\{b_{n}\}\) 中的项；
\item 数列 \(c_{1}\)，\(c_{2}\)，\(c_{3}\)，\(\cdots\)，\(c_{40}\) 中有多少项不是数列 \(\{b_{n}\}\) 中的项？请说明理由；
\item 求数列 \(\{c_{n}\}\) 的前 \(4n\) 项和 \(S_{4n}\)（\(n \in \mathbf{N}^{*}\)）.
\end{enumerate}
\end{problem}

\begin{problem}
已知 \(ABCD - A_{1}B_{1}C_{1}D_{1}\) 是底面边长为 \(1\) 的正四棱柱，高 \(AA_{1} = 2\)，求：
\begin{enumerate}
\item 异面直线 \(BD\) 与 \(AB_{1}\) 所成角的余弦值；
\item 四面体 \(AB_{1}D_{1}C\) 的体积.
\end{enumerate}
\begin{center}
\bitmapinclude[max width=0.38\linewidth,max totalheight=4.8cm]{img/2011/shanghai_liberal/q20_fig1.png}
\end{center}
\end{problem}
