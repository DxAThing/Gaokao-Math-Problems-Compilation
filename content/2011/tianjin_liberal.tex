\chapter{2011年天津卷（文）}

\section{单选题}

\begin{problem}
1 是虚数单位，复数 \(\frac{1 - 3\i}{\i - \i} =\)（\quad）

\choices
    {2 - i}
    {\(2 + \i\)}
    {\(-1\) - 2i}
    {\(-1 + 2\i\)}
\end{problem}

\begin{problem}
设变量 \(x, y\) 满足的约束条件 \(\left\{ \begin{array}{l} x \geqslant 1, \\ x + y - 4 \leqslant 0, \\ x - 3y + 4 \leqslant 0, \end{array} \right.\) 则目标函数 \(z = 3x - y\) 的最大值为（\quad）

\choices
    {\(-4\)}
    {0}
    {\( \frac{4}{3} \)}
    {4}
\end{problem}

\begin{problem}
阅读程序框图，运行相应的程序，若输入 x 的值为 \(-4\)，则输出 y 的值为（\quad）

\choices
    {0.5}
    {1}
    {2}
    {4}
\end{problem}

\begin{problem}
设集合 \(A = \{x \in \R \mid x - 2 > 0\}\)，\(B = \{x \in \R \mid x < 0\}\)，\(C = \{x \in \R \mid x(x - 2) > 0\}\)，则 \(x^2 \in A \cup B\) “是 \(x \in C\)”的（\quad）

\choices
    {充分而不必要条件}
    {必要而不充分条件}
    {充分必要条件}
    {既不充分也不必要条件}
\end{problem}

\begin{problem}
已知 \(a = \log_26.6, b = \log_64.32, c = \log_64.36\) 则（\quad）

\choices
    {\(a > b > c\)}
    {\(a > c > b\)}
    {\(b > a > c\)}
    {\(c > a > b\)}
\end{problem}

\begin{problem}
已知双曲线 \(\frac{x^2}{9} - \frac{y^2}{7} = 1 (a > 0, b > 0)\) 的左顶点与抛物线 \(y^2 = 2px\) \((p > 0)\) 的焦点的距离为4，且双曲线的一条渐近线与抛物线的准线的交点坐标为 \((-2, -1)\)，则双曲线的焦距为（\quad）

\choices
    {\(2\sqrt{3}\)}
    {\(2\sqrt{5}\)}
    {\(4\sqrt{3}\)}
    {\(4\sqrt{5}\)}
\end{problem}

\begin{problem}
已知函数 \( f(x) = 2\sin (\omega x + \varphi), x \in \R \)，其中 \( \omega > 0, -\pi < \varphi \leqslant \pi \). 若 \( f(x) \) 的最小正周期为 \( 6\pi \)，且当 \( x = \frac{\pi}{2} \) 时，\( f(x) \) 取得最大值，则（\quad）

\choices
    {\(f(x)\) 在区间 \([-2\pi, 0]\) 上是增函数}
    {\(f(x)\) 在区间 \([-3\pi, -\pi]\) 上是增函数}
    {\(f(x)\) 在区间 \([3\pi, 5\pi]\) 上是减函数}
    {\(f(x)\) 在区间 \([4\pi, 6\pi]\) 上是减函数}
\end{problem}

\begin{problem}
对实数 a 和 b，定义运算“\(\otimes\)”： \( a \otimes b = \begin{cases} a, & a - b \leqslant 1, \\ b, & a - b > 1. \end{cases} \) 设函数 \( f(x) = (x^{2} - 2) \otimes (x - 1), x \in \R \) . 若函数 \( y = f(x) - c \) 的图象与 x 轴恰有两个公共点，则实数 c 的取值范围是（\quad）

\choices
    {\((-1,1]\cup (2, + \infty)\)}
    {\((-2, - 1] \cup (1, 2]\)}
    {\((-\infty, -2) \cup (1, 2]\)}
    {\([-2, - 1]\)}
\end{problem}

\section{填空题}

\begin{problem}
已知集合 \(A = \{x \in \R \mid |x - 1| < 2\}\)，\(\Z\) 为整数集，则集合 \(A \cap \Z\) 中所有元素的和等于 \fillinblank{} \fillinblank{}.
\end{problem}

\begin{problem}
一个几何体的三视图如图所示（单位：m），则这个几何体的体积为 m\( ^{3} \).

正视图

俯视图

侧视图
\end{problem}

\begin{problem}
已知 \( \{a_{n}\} \) 为等差数列，\( S_{n} \) 为其前 n 项和. \( n \in N^{*} \) . 若 \( a_{3} = 16 \) ，\( S_{20} = 20 \) ，则 \( S_{10} \) 的值为 \fillinblank{} \fillinblank{}.
\end{problem}

\begin{problem}
已知 \(\log_2a + \log_2b \geqslant 1\)，则 \(3^a + 9^b\) 的最小值为 \fillinblank{} \fillinblank{}.
\end{problem}

\begin{problem}
如图，已知圆中两条弦 \(AB\) 与 \(CD\) 相交于点 \(F, E\) 是 \(AB\) 延长线上一点，且 \(DF = CF = \sqrt{2}, AF:FB:BE = 4:2:1\). 若 \(CE\) 与圆相切，则 \(CE\) 的长为 \fillinblank{} \fillinblank{}.
\end{problem}

\begin{problem}
已知直角梯形 \(ABCD\) 中，\(AD // BC\)，\(\angle ADC = 90^{\circ}\)，\(AD = 2\)，\(BC = 1\)，\(P\) 是腰 \(DC\) 上的动点，则 \(\boxed{PA} + 3\overrightarrow{PB}\) 的最小值为 \fillinblank{} \fillinblank{}.
\end{problem}

\section{解答题}

\begin{problem}
编号分别为 \(A_{1}, A_{2}, \cdots, A_{16}\) 各篮球运动员在某次训练比赛中的得分记录如下： 运动员编号 \( A_1 \) \( A_2 \) \( A_3 \) \( A_4 \) \( A_5 \) \( A_6 \) \( A_7 \) \( A_8 \) 得分 15 35 21 28 25 36 18 34 运动员编号 \( A_9 \) \( A_{10} \) \( A_{11} \) \( A_{12} \) \( A_{13} \) \( A_{14} \) \( A_{15} \) \( A_{16} \) 得分 17 26 25 33 22 12 31 38 (1) 将得分在对应区间内的人数填入相应的空格; 区间 [10,20) [20,30) [30,40] 人数 (2) 从得分在区间 [20, 30) 内的运动员中随机抽取 2 人， \circled{1} 用运动员编号列出所有可能的抽取结果； \circled{2} 求这 2 人得分之和大于 50 的概率.
\end{problem}

\begin{problem}
在 \(\triangle ABC\) 中，内角 \(A, B, C\) 的对边分别为 \(a, b, c\)，已知 \(B = C, 2b = \sqrt{3}a\). (1) 求 \(\cos A\) 的值; (2) \(\cos \left(2A + \frac{\pi}{4}\right)\) 的值.
\end{problem}

\begin{problem}
如图，在四棱锥 \(P - ABCD\) 中，底面 \(ABCD\) 为平行四边形，\(\angle ADC = 45^{\circ}\)，\(AD = AC = 1\)，\(O\) 为 \(AC\) 中点，\(PO \perp\) 平面 \(ABCD\)，\(PO = 2\)，\(M\) 为 \(PD\) 中点. (1) 证明: \(PB //\) 平面 \(ACM\); (2) 证明: \(AD \perp\) 平面 \(PAC\); (3) 求直线 \(AM\) 与平面 \(ABCD\) 所成角的正切值.
\end{problem}

\begin{problem}
已知函数 \( f(x) = 4x^{3} + 3tx^{2} - 6t^{2}x + t - 1, x \in \R \)，其中 \( t \in \R \). (1) 当 \(t = 1\) 时，求曲线 \(y = f(x)\) 在点 \((0, f(0))\) 处的切线方程; (2) 当 \(t \neq 0\) 时，求 \(f(x)\) 的单调区间; (3) 证明：对任意的 \(t \in (0, +\infty)\)，\(f(x)\) 在区间 \((0, 1)\) 内均存在零点.
\end{problem}

\begin{problem}
已知数列 \(\{a_{n}\}\) 与 \(\{b_{n}\}\) 满足: \(b_{n+1}a_{n} + b_{n}a_{n+1} = (-2)^{n} + 1\)，\(b_{n} = \frac{3 + (-1)^{n-1}}{2}, n \in \mathbf{N}^{*}\)，且 \(a_{1} = 2\). (1) 求 \(a_{2}, a_{3}\) 的值; (2) 设 \(c_{n} = a_{2n + 1} - a_{2n - 1}, n \in \mathbf{N}^{*}\)，证明: 数列 \(\{c_{n}\}\) 是等比数列; (3) 设 \(S_{n}\) 为 \(\{a_{n}\}\) 的前 \(n\) 项和，证明 \(\frac{S_{1}}{a_{1}} + \frac{S_{2}}{a_{2}} + \ldots + \frac{S_{2n-1}}{a_{2n-1}} + \frac{S_{2n}}{a_{2n}} \leqslant n - \frac{1}{3}\) \((n \in \mathbf{N}^{*})\).
\end{problem}

\begin{problem}
设椭圆 \(\frac{x^2}{a_2^2} + \frac{y^2}{b^2} = 1 (a > b > 0)\) 的左、右焦点分别为 \(F_1, F_2\). 点 \(P(a, b)\) 满足 \(|PF_2| = |F_1F_2|\). (1) 求椭圆的离心率 \( e \); (2) 设直线 \(PF_{2}\) 与椭圆相交于 \(A, B\) 两点，若直线 \(PF_{2}\) 与圆 \((x + 1)^{2} + (p - \sqrt{3})^{2} = 16\) 相交于 \(M, N\) 两点，且 \(|MN| = \frac{5}{8} |AB|\)，求椭圆的方程.
\end{problem}

