\chapter{2011年辽宁卷（文）}

\section{单选题}

\begin{problem}
已知集合 \(A = \{x \mid x > 1\}\)，\(B = \{x \mid -1 < x < 2\}\)，则 \(A \cap B =\)（\quad）

\choices
    {\(\{x\mid -1 < x < 2\}\)}
    {\( \{x \mid x > -1\} \)}
    {\( \{x \mid -1 < x < 1\} \)}
    {\( \{x \mid 1 < x < 2\} \)}
\end{problem}

\begin{problem}
i 为虚数单位，\( \frac{1}{1} + \frac{1}{1^{2}} + \frac{1}{1^{2}} + \frac{1}{1^{2}} = \)（\quad）

\choices
    {0}
    {2i}
    {-2i}
    {4i}
\end{problem}

\begin{problem}
已知向量 \(a = (2,1)\)，\(b = (-1,k)\)，\(a \cdot (2a - b) = 0\)，则 \(k =\)（\quad）

\choices
    {\(-12\)}
    {\(-6\)}
    {6}
    {12}
\end{problem}

\begin{problem}
已知命题 \(p: \exists n \in \mathbf{N}, 2^n > 1000\)，则 \(\neg p\) 为（\quad）

\choices
    {\(\forall n\in \mathbf{N},2^{n}\leqslant 1000\)}
    {\(\forall n\in \mathbf{N},2^n >1000\)}
    {\(\exists n\in \mathbf{N},2^n\leqslant 1000\)}
    {\(\exists n\in \mathbf{N},2^n < 1000\)}
\end{problem}

\begin{problem}
若等比数列 \(\{a_{n}\}\) 满足 \(a_{n}a_{n + 1} = 16^{n}\)，则公比为（\quad）

\choices
    {2}
    {4}
    {8}
    {16}
\end{problem}

\begin{problem}
若函数 \( f(x) = \frac{x}{(2x + 1)(x - a)} \) 为奇函数，则 \( a = \)（\quad）

\choices
    {\(\frac{1}{2}\)}
    {\( \frac{2}{3} \)}
    {\(\frac{3}{4}\)}
    {1}
\end{problem}

\begin{problem}
已知 F 是抛物线 \( y^{2} = x \) 的焦点，A, B 是该抛物线上的两点，\( |AF| + |BF| = 3 \) ，则线段 AB 的中点到 y 轴的距离为（\quad）

\choices
    {\( \frac{3}{4} \)}
    {1}
    {\( \frac{5}{4} \)}
    {\( \frac{7}{4} \)}
\end{problem}

\begin{problem}
一个正三棱柱的侧棱长和底面边长相等，体积为 \(2 \sqrt{3}\)，它的三视图中的俯视图如图所示，左视图是一个矩形，则这个矩形的面积是（\quad）

俯视图（\quad）

\choices
    {4}
    {\(2 \sqrt{3}\)}
    {2}
    {\(\sqrt{3}\)}
\end{problem}

\begin{problem}
执行下面的程序框图，如果输入的 n 是 4，则输出的 p 是（\quad）

\choices
    {8}
    {5}
    {3}
    {2}
\end{problem}

\begin{problem}
已知球的直径 \(SC = 4, A, B\) 是该球球面上的两点，\(AB = 2, \angle ASC = \angle BSC = 45^\circ\)，则锥棱 \(S - ABC\) 的体积为（\quad）

\choices
    {\(\frac{\sqrt{3}}{3}\)}
    {\(\frac{2\sqrt{3}}{3}\)}
    {\(\frac{4\sqrt{3}}{3}\)}
    {\(\frac{5\sqrt{3}}{3}\)}
\end{problem}

\begin{problem}
函数 \( f(x) \) 的定义域为 \( \R \)，\( f(-1) = 2 \)，对任意 \( x \in \R \)，\( f'(x) > 2 \)，则 \( f(x) > 2x + 4 \) 的解集为（\quad）

\choices
    {\((-1, 1)\)}
    {\((-1, +\infty)\)}
    {\((-\infty, -1)\)}
    {\((-\infty, +\infty)\)}
\end{problem}

\begin{problem}
已知函数 \( f(x) = A \tan (\omega x + \varphi) \left( \omega > 0, |\varphi| < \frac{\pi}{2} \right) \)，\( y = f(x) \) 的部分图象如图，则 \( f\left(\frac{\pi}{4}\right) = \)（\quad）

\choices
    {\(2 + \sqrt{3}\)}
    {\(\sqrt{3}\)}
    {\(\frac{\sqrt{3}}{3}\)}
    {\(2 - \sqrt{3}\)}
\end{problem}

\section{填空题}

\begin{problem}
已知圆 \(C\) 经过 \(A(5,1)\)，\(B(1,3)\) 两点，圆心在 \(x\) 轴上，则 \(C\) 的方程为 \fillinblank{} \fillinblank{}.
\end{problem}

\begin{problem}
调查了某地若干户家庭的年收入 \(x\)（单位：万元）和年饮食支出 \(y\)（单位：万元），调查显示年收入 \(x\) 与年饮食支出 \(y\) 具有线性相关关系. 并由调查数据得到 \(y\) 对 \(x\) 的回归直线方程：\(\hat{y} = 0.254x + 0.321\). 由回归直线方程可知，家庭年收入每增加1万元，年饮食支出平均增加万元 \fillinblank{}.
\end{problem}

\begin{problem}
\(S_{m}\) 为等差数列 \(\{a_{n}\}\) 的前 \(n\) 项和. \(S_{2} = S_{m}, a_{4} = 1\) ，则 \(a_{5} =\)
\end{problem}

\begin{problem}
已知函数 \( f(x) = \e^{x} - 2x + a \) 有零点，则 \( a \) 的取值范围是 \fillinblank{} \fillinblank{}.
\end{problem}

\section{解答题}

\begin{problem}
\(\triangle ABC\) 的三个内角 \(A, B, C\) 所对的边分别为 \(a, b, c, a \sin A \sin B + b \cos^2 A = \sqrt{2} a\). (1) 求 \( \frac{b}{c} \) ; (2) 若 \(c^2 = b^2 + \sqrt{3} a^2\)，求 \(B\).
\end{problem}

\begin{problem}
如图，四边形 \(ABCD\) 为正方形，\(QA \perp\) 平面 \(ABCD\)，\(PD // QA\)，\(QA = AB = \frac{1}{4} PD\). (1) 证明: \(PQ \perp\) 平面 \(DCQ\); (2) 求棱锥 \(Q - ABCD\) 的体积与棱锥 \(P - DCQ\) 的体积比值.
\end{problem}

\begin{problem}
某农场计划种植某种新作物. 为此对这种作物的两个品种（分别称为品种甲和品种乙）进行田间试验. 选取两大块地. 每大块地分成 \(n\) 小块地. 在总共 \(2n\) 小块地中，随机选 \(n\) 小块地种植品种甲，另外 \(n\) 小块地种植品种乙. (1) 假设 \(n = 2\). 求第一大块地都种植品种甲的概率. (2) 试验时每大块地分成 8 小块，即 \(n = 8\). 试验结束后得到品种甲和品种乙在各小块地上的每公顷产量 (单位: \(\mathrm{kg} / \mathrm{hm}^2\)) 如下表: 品种甲 405 397 390 404 388 400 412 406 品种乙 419 403 412 418 408 423 400 413 分别求品种甲和品种乙每公顷产量的样本平均数时样本方差: 根据试验结果，你应该种植哪一品种\(\perp\) 解: 样本数据 \( x_{1}, x_{2}, \cdots, x_{n} \) 的样本方差 \( s^{2} = \frac{1}{n}[(x_{1} - \overline{x})^{2} + (x_{2} - \overline{x})^{2} + \cdots + (x_{n} - \overline{x})^{2}] \) ，表中 \( \overline{x} \) 为样本平均数.
\end{problem}

\begin{problem}
如图，已知椭圆 \(C_1\) 的中心在原点 \(O\)，长轴左、右端点 \(M, N\) 在 \(x\) 轴上，椭圆 \(C_2\) 的短轴为 \(MN\)，且 \(C_1, C_2\) 的离心率都为 \(e\). 直线 \(l \perp MN\)，\(l\) 与 \(C_1\) 交于两点. 与 \(C_2\) 交于两点. 这四点按纵坐标从大到小依次为 \(A, B, C, D\). (1) 设 \(e = \frac{1}{2}\)，求 \(|BC|\) 与 \(|AD|\) 的比值; (2) 当 \(e\) 变化时，是否存在直线 \(l\)，使得 \(BO // AN\)，并说明理由.

【B】在平面直角坐标系 \(xOy\) 中，曲线 \(C_1\) 的参数方程为 \(\left\{ \begin{array}{l} x = \cos \varphi, \\ y = \sin \varphi. \end{array} \right.\) （\(\varphi\) 为参数），曲线 \(C_2\) 的参数方程为 \(\left\{ \begin{array}{l} x = a\cos \varphi, \\ y = b\sin \varphi. \end{array} \right.\) （\(a > b > 0\)，\(\varphi\) 为参数）. 在以 \(O\) 为极点，\(x\) 的轴正半轴为极轴的单位根系中，射线 \(l: \theta = a\) 与 \(C_1, C_2\) 各有一个交点. 当 \(a = 0\) 时，这两个交点间的距离为 2，当 \(a = \frac{\pi}{2}\) 时，这两个交点重合. (1) 分别说明 \(C_1, C_2\) 是什么曲线，并求出 \(a\) 与 \(b\) 的值； (2) 设当 \( \alpha = \frac{\pi}{4} \) 时，l 与 \( C_{1}, C_{2} \) 的交点分别为 \( A_{1}, B_{1} \) ，当 \( \alpha = -\frac{\pi}{4} \) 时，l 与 \( C_{1}, C_{2} \) 的交点分别为 \( A_{2}, B_{2} \) . 求四边形 \( A_{1}A_{2}B_{2}B_{1} \) 的面积. 【C】已知函数 \(f(x) = |x - 2| - |x - 5|\). (1) 证明： \( -3 \leqslant f(x) \leqslant 3; \) (2) 求不等式 \( f(x) \geqslant x^{2} - 8x + 15 \) 的解集.
\end{problem}

