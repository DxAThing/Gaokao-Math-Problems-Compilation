\chapter{2009年辽宁卷（文）}

\section{单选题}

\begin{problem}
已知集合 \(M = \{x \mid -3 < x \leqslant 5\}\)，\(N = \{x \mid x < -5\) 或 \(x > 5\}\)，则 \(M \cup N =\)（\quad）

\choices
    {\(\{x\mid x < - 5\) 或 \(x > - 3\}\)}
    {\(\{x\mid -5 < x < 5\}\)}
    {\(\{x\mid -3 < x < 5\}\)}
    {\(\{x\mid x < - 3\) 或 \(x > 5\}\)}
\end{problem}

\begin{problem}
已知复数 \(z = 1 - 2\i\)，那么 \(\frac{1}{z} =\)（\quad）

\choices
    {\(\frac{\sqrt{5}}{5} + \frac{2\sqrt{5}}{5}\i\)}
    {\(\frac{\sqrt{5}}{5} -\frac{2\sqrt{5}}{5}\i\)}
    {\(\frac{1}{5} +\frac{2}{5}\i\)}
    {\(\frac{1}{5} -\frac{2}{5}\i\)}
\end{problem}

\begin{problem}
\(\{a_{n}\}\) 为等差数列，且 \(a_7 - 2a_4 = -1, a_3 = 0\) ，则公差 \(d =\) （\quad）

\choices
    {\(-2\)}
    {\(-\frac{1}{2}\)}
    {\(\frac{1}{2}\)}
    {\(2\)}
\end{problem}

\begin{problem}
平面向量 a 与 b 的夹角为 \( 60^{\circ} \) ，\( a = (2, 0) \) ，\( |b| = 1 \) ，则 \( |a + 2b| = \)（\quad）

\choices
    {\(\sqrt{3}\)}
    {\(2 \sqrt{3}\)}
    {4}
    {12}
\end{problem}

\begin{problem}
如果把地球看成一个球体，则地球上北纬 \(60^{\circ}\) 纬线和赤道线长的比值为（\quad）

\choices
    {0.8}
    {0.75}
    {0.5}
    {0.25}
\end{problem}

\begin{problem}
已知函数 \( f(x) \) 满足：当 \( x \geqslant 4 \) 时，\( f(x) = \left(\frac{1}{2}\right)^x \); 当 \( x < 4 \) 时，\( f(x) = f(x + 1) \). 则 \( f(2 + \log_2 3) = \)（\quad）

\choices
    {\(\frac{1}{24}\)}
    {\(\frac{1}{12}\)}
    {\(\frac{1}{8}\)}
    {\(\frac{3}{8}\)}
\end{problem}

\begin{problem}
已知圆 \(C\) 与直线 \(x - y = 0\) 及 \(x - y - 4 = 0\) 都相切，圆心在直线 \(x + y = 0\) 上，则圆 \(C\) 的方程为（\quad）

\choices
    {\((x + 1)^2 +(y - 1)^2 = 2\)}
    {\((x - 1)^{2} + (y + 1)^{2} = 2\)}
    {\((x - 1)^{2} + (y - 1)^{2} = 2\)}
    {\((x + 1)^{2} + (y + 1)^{2} = 2\)}
\end{problem}

\begin{problem}
已知 \(\tan \theta = 2\)，则 \(\sin^2\theta + \sin \theta \cos \theta - 2\cos^2\theta =\)（\quad）

\choices
    {\(-\frac{4}{3}\)}
    {\( \frac{5}{4} \)}
    {\(-\frac{3}{4}\)}
    {\(\frac{4}{5}\)}
\end{problem}

\begin{problem}
\(ABCD\) 为长方形，\(AB = 2\)，\(BC = 1\)，\(O\) 为 \(AB\) 的中点. 在长方形 \(ABCD\) 内随机取一点，取到的点到 \(O\) 的距离大于 1 的概率为（\quad）

\choices
    {\( \frac{3}{4} \)}
    {\(1 - \frac{\pi}{4}\)}
    {\(\frac{\pi}{8}\)}
    {\(1 - \frac{\pi}{8}\)}
\end{problem}

\begin{problem}
某店一个月的收入和支出总共记录了 \(N\) 个数据 \(a_1, a_2, \dots, a_N\)，其中收入记为正数. 支出记为负数. 该店用下边的程序框图计算月总收入 \(S\) 和月净盈利 \(V\)，那么在图中空白的判断框和处理框中，应分别填入下列四个选项中的（\quad）

\bitmapfigure[max width=0.34\linewidth,max totalheight=5.0cm]{img/2009/liaoning_liberal/q10_fig1.png}

\choices
    {\(A > 0, V = S - T\)}
    {\(A < 0,V = S - T\)}
    {\(A > 0, V = S + T\)}
    {\(A < 0,V = S + T\)}
\end{problem}

\begin{problem}
下列 4 个命题 \(p_1\)：\(\exists x \in (0, +\infty)\)，\(\left(\frac{1}{2}\right)^x < \left(\frac{1}{3}\right)^x\)；\(p_2\)：\(\exists x \in (0,1)\)，\(\log_{\frac12} x > \log_{\frac13} x\)；\(p_3\)：\(\forall x \in (0, +\infty)\)，\(\left(\frac{1}{2}\right)^x > \log_{\frac12} x\)；\(p_4\)：\(\forall x \in \left(0,\frac{1}{3}\right)\)，\(\left(\frac{1}{2}\right)^x < \log_{\frac13} x\). 其中的真命题是（\quad）

\choices
    {\(p_1, p_3\)}
    {\(p_1, p_4\)}
    {\(p_2, p_3\)}
    {\(p_2, p_4\)}
\end{problem}

\begin{problem}
已知偶函数 \( f(x) \) 在区间 \([0, +\infty)\) 单调增加，则满足 \( f(2x - 1) < f\left(\frac{1}{3}\right) \) 的 \( x \) 取值范围是（\quad）

\choices
    {\(\left(\frac{1}{3},\frac{2}{3}\right)\)}
    {\(\left[\frac{1}{3},\frac{2}{3}\right)\)}
    {\(\left(\frac{1}{2},\frac{2}{3}\right)\)}
    {\(\left[\frac{1}{2},\frac{2}{3}\right)\)}
\end{problem}

\section{填空题}

\begin{problem}
在平面直角坐标系 \(xOy\) 中，四边形 \(ABCD\) 的边 \(AB // DC\)，\(AD // BC\). 已知点 \(A(-2,0)\)，\(B(6,8)\)，\(C(8,6)\)，则 \(D\) 点的坐标为 \fillinblank{} \fillinblank{}.
\end{problem}

\begin{problem}
已知函数 \( f(x) = \sin (\omega x + \varphi) (\omega > 0) \) 的图象如图所示，则 \( \omega = \)  \fillinblank{}.

\bitmapfigure[max width=0.34\linewidth,max totalheight=4.8cm]{img/2009/liaoning_liberal/q14_fig1.png}
\end{problem}

\begin{problem}
若函数 \( f(x) = \frac{x^2 + a}{x + 1} \) 在 \( x = 1 \) 处取极值，则 \( a = \)  \fillinblank{}.
\end{problem}

\begin{problem}
设某几何体的三视图如下 (尺寸的长度单位为 m). 则该几何体的体积为 \fillinblank{}\(\mathrm{m}^{3}\).

\bitmapfigure[max width=0.58\linewidth,max totalheight=5.4cm]{img/2009/liaoning_liberal/q16_fig1.png}
\end{problem}

\section{解答题}

\begin{problem}
等比数列 \(\{a_{n}\}\) 的前 \(n\) 项和为 \(S_{n}\). 已知 \(S_{1}, S_{3}, S_{2}\) 成等差数列. (1) 求 \(\{a_{n}\}\) 的公比 \(q\); (2) 求 \(a_{1} - a_{3} = 3\)，求 \(S_{n}\).
\end{problem}

\begin{problem}
如图，\(A, B, C, D\) 都在同一个与水平面垂直的平面内，\(B, D\) 为两岛上的两座灯塔的塔顶. 测量船于水面 \(A\) 处测得 \(B\) 点和 \(D\) 点的仰角分别为 \(75^{\circ}, 30^{\circ}\)，于水面 \(C\) 处测得 \(B\) 点和 \(D\) 点的仰角均为 \(60^{\circ}\)，\(AC = 0.1 \mathrm{~km}\). 试探究图中 \(B, D\) 间距离与另外哪两点间距离相等，然后求 \(B, D\) 的距离. (计算结果精确到 \(0.01 \mathrm{~km}, \sqrt{2} \approx 1.414, \sqrt{6} \approx 2.449\))

\bitmapfigure[max width=0.42\linewidth,max totalheight=5.6cm]{img/2009/liaoning_liberal/q18_fig1.png}
\end{problem}

\begin{problem}
如图，已知两个正方形 \(ABCD\) 和 \(DCEF\) 不在同一平面内，\(M, N\) 分别为 \(AB, DF\) 的中点. (1) 若 \(CD = 2\)，平面 \(ABCD \perp\) 平面 \(DCEF\)，求直线 \(MN\) 的长; (2) 用反证法证明: 直线 \(ME\) 与 \(BN\) 是两条异面直线.

\bitmapfigure[max width=0.46\linewidth,max totalheight=5.6cm]{img/2009/liaoning_liberal/q19_fig1.png}
\end{problem}

\begin{problem}
某企业有两个分厂生产某种零件，按规定内径尺寸 (单位: mm) 的值落在 [29.94, 30.06] 的零件为优质品. 从两个分厂生产的零件中各抽出 500 件，量其内径尺寸，得结果如下表:

\begin{center}
\begin{tabular}{c@{\hspace{2em}}c}
甲厂 & 乙厂 \\
\begin{tabular}{|c|c|}
\hline
分组 & 频数 \\
\hline
\([29.86,29.90)\) & 12 \\
\([29.90,29.94)\) & 63 \\
\([29.94,29.98)\) & 86 \\
\([29.98,30.02)\) & 182 \\
\([30.02,30.06)\) & 92 \\
\([30.06,30.10)\) & 61 \\
\([30.10,30.14)\) & 4 \\
\hline
\end{tabular}
&
\begin{tabular}{|c|c|}
\hline
分组 & 频数 \\
\hline
\([29.86,29.90)\) & 29 \\
\([29.90,29.94)\) & 71 \\
\([29.94,29.98)\) & 85 \\
\([29.98,30.02)\) & 76 \\
\([30.02,30.06)\) & 62 \\
\([30.06,30.10)\) & 18 \\
\hline
\end{tabular}
\end{tabular}
\end{center}

(1) 试分别估计两个分厂生产的零件的优质品率;
(2) 由于以上统计数据填下面 \(2 \times 2\) 列联表，并问是否有 \(99\%\) 的把握认为 “两个分厂生产的零件的质量有差异”.

\begin{center}
\begin{tabular}{|c|c|c|c|}
\hline
 & 甲厂 & 乙厂 & 合计 \\
\hline
优质品 &  &  &  \\
\hline
非优质品 &  &  &  \\
\hline
合计 &  &  &  \\
\hline
\end{tabular}
\end{center}

附：\(\chi^2 = \frac{n(n_{11}n_{22} - n_{12}n_{21})^2}{n_{1+}n_{2+}n_{+1}n_{+2}}\)，
\begin{center}
\begin{tabular}{c|cc}
\(P(\chi^2 \geqslant k)\) & 0.05 & 0.01 \\
\hline
\(k\) & 3.841 & 6.635
\end{tabular}
\end{center}
\end{problem}

\begin{problem}
设 \( f(x) = \e^{x}(ax^{2} + x + 1) \)，且曲线 \( y = f(x) \) 在 \( x = 1 \) 处的切线与 \( x \) 轴平行. (1) 求 a 的值，并讨论 \( f(x) \) 的单调性; (2) 证明：当 \(\theta \in \left[0, \frac{\pi}{2}\right]\) 时，\(|f(\cos \theta) - f(\sin \theta)| < 2\).
\end{problem}

\begin{problem}
已知椭圆 \(C\) 过点 \(A\left(1, \frac{3}{2}\right)\)，两个焦点为 \((-1, 0)\)，\((1, 0)\). (1) 求椭圆 \(C\) 的方程; (2) \(E, F\) 是椭圆 \(C\) 上的两个动点，如果直线 \(AE\) 的斜率与 \(AF\) 的斜率互为相反数，证明直线 \(EF\) 的斜率为定值. 并求出这个定值.
\end{problem}
