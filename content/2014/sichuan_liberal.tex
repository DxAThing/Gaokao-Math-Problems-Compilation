\chapter{2014年四川卷（文）}

\section{单选题}

\begin{problem}
已知集合 \(A = \{x \mid (x + 1)(x - 2) \leqslant 0\}\)，集合 \(B\) 为整数集，则 \(A \cap B = \fillinblank{}\)（\quad）

\choices
    {\( \{-1,0\} \)}
    {\(\{0, 1\}\)}
    {\(\{-2, -1, 0, 1\}\)}
    {\(\{-1, 0, 1, 2\}\)}
\end{problem}

\begin{problem}
在“世界读书日”前夕，为了了解某地 5000 名居民某天的影响，该网点向 A 中抽取了 200 名居民的阅读时间进行统计分析. 在这个问题中，5000 名居民的阅读时间的全体是（\quad）

\choices
    {总体}
    {个体}
    {样本的容量}
    {从总体中抽取的一个样本}
\end{problem}

\begin{problem}
为了得到函数 \( y = \sin (x + 1) \) 的图象，只需把函数 \( y = \sin x \) 的图象上所有的点（\quad）

\choices
    {向左平行移动 1 个单位长度}
    {向右平行移动 1 个单位长度}
    {向左平行移动 \(\pi\) 个单位长度}
    {向右平行移动 \(\pi\) 个单位长度}
\end{problem}

\begin{problem}
某三棱锥的侧视图、俯视图如图所示，则该三棱锥的体积是（\quad）

侧视图

俯视图（\quad）

\choices
    {3}
    {2}
    {\(\sqrt{3}\)}
    {1}
\end{problem}

\begin{problem}
若 \(a > b > 0, c < d < 0\)，则一定有（\quad）

\choices
    {\(\frac{a}{b} > \frac{b}{d}\)}
    {\(\frac{a}{c} < \frac{b}{d}\)}
    {\(\frac{a}{d} >\frac{b}{c}\)}
    {\(\frac{a}{d} < \frac{b}{c}\)}
\end{problem}

\begin{problem}
执行如图所示的程序框图，如果输入的 \(x, y \in \R\)，则输出的 \(S\) 的最大值为（\quad）

\choices
    {0}
    {1}
    {2}
    {3}
\end{problem}

\begin{problem}
已知 \(b > 0, \log_2 b = a, \lg b = c, 5^a = 10\)，则下列等式一定成立的是（\quad）

\choices
    {\(d = ac\)}
    {\(a = cd\)}
    {\(c = ad\)}
    {\(d = a + c\)}
\end{problem}

\begin{problem}
如图，从气球 \(A\) 上测得正前方的河流的两岸 \(B, C\) 的俯角分别为 \(75^{\circ}, 30^{\circ}\)，此时气球的高是 \(60 \mathrm{~m}\)，则河流的宽度 \(BC\) 等于（\quad）

\choices
    {\(240(\sqrt{3} - 1)\mathrm{m}\)}
    {\(180(\sqrt{2} - 1)\mathrm{m}\)}
    {\(120(\sqrt{3} - 1)\mathrm{m}\)}
    {\(30(\sqrt{3} + 1)\mathrm{m}\)}
\end{problem}

\begin{problem}
设 \(m \in \R\)，过定点 \(A\) 的动直线 \(x + my = 0\) 和过定点 \(B\) 的动直线 \(mx - y - m + 3 = 0\) 交于点 \(P(x, y)\). 则 \(|PA| + |PB|\) 的取值范围是（\quad）

\choices
    {\([\sqrt{5}, 2\sqrt{9}]\)}
    {\([\sqrt{10}, 2\sqrt{5}]\)}
    {\([\sqrt{10}, 4\sqrt{5}]\)}
    {\([2\sqrt{5}, 4\sqrt{5}]\)}
\end{problem}

\begin{problem}
已知 F 是抛物线 \( y^{2} = x \) 的焦点，点 A, B 在该抛物线上且位于 x 轴的两侧， \( \overrightarrow{OA} \cdot \overrightarrow{OB} = 2 \) (其中 O 为坐标原点)，则 \( \triangle ABO \) 与 \( \triangle AFO \) 面积之间的关系的最小值是（\quad）

\choices
    {2}
    {3}
    {\(\frac{17\sqrt{2}}{8}\)}
    {\(\sqrt{10}\)}
\end{problem}

\section{填空题}

\begin{problem}
复数 \(\frac{2 - 2\i}{1 + \i} =\)
\end{problem}

\begin{problem}
设 \( f(x) \) 是定义在 R 上的周期为 2 的函数，当 \( x \in [-1,1) \) 时， \( f(x) = \begin{cases} -4x^{2} + 2, & -1 \leqslant x < 0, \\ x, & 0 \leqslant x < 1, \end{cases} \) 则 \( f\left(\frac{3}{4}\right) = \)  \fillinblank{}.
\end{problem}

\begin{problem}
平面向量 \(\alpha = (1, 2)\)，\(b = (4, 2)\)，\(c = m\alpha + b\) (\(m \in \R\))，且 \(c\) 与 \(\alpha\) 的夹角等于 \(c\) 与 \(b\) 的夹角，则 \(m = \)  \fillinblank{}.
\end{problem}

\begin{problem}
以 \(A\) 表示值域为 \(\R\) 的函数组成的集合，\(B\) 表示具有如下性质的函数 \(\varphi(x)\) 组成的集合: 对于函数 \(\varphi(x)\)，存在一个正数 \(M\)，使得函数 \(\varphi(x)\) 的值域包含于区间 \([-M, M]\). 例如，当 \(\varphi_1(x) = x^3\)，\(\varphi_2(x) = \sin x\) 时，\(\varphi_1(x) \in A\)，\(\varphi_2(x) \in B\). 现有如下命题: \circled{1} 设函数 \( f(x) \) 的定义域为 \( D \)，则 \( f'(x) \in A' \) 的充要条件是“\( \forall b \in \R, \exists a \in D, f(a) \in A' \)”； \circled{2} 函数 \( f(x) \in B' \) 的充要条件是 \( f(x) \) 有最大值和最小值； \circled{3} 若函数 \( f(x), g(x) \) 的定义域相同，且 \( f(x) \in A, g(x) \in B \)，则 \( f(x) + g(x) \notin B \); \circled{4} 若函数 \( f(x) = a \ln (x + 2) + \frac{x}{x^2 + 1} (x > -2, a \in \R) \) 有最大值，则 \( f(x) \in B \). 其中的真命题有.（写出所有真命题的序号）
\end{problem}

\section{解答题}

\begin{problem}
一个盒子里有三张卡片，分别标记有数字 1, 2, 3，这三张卡片除标记的数字外完全相同，随机有效地抽取 3 次，每次抽取 1 张，将抽取的卡片上的数字依次记为 \(a, b, c\). (1) 求“抽取的卡片上的数字满足 \(a + b = c\)”的概率； (2) 求“抽取的卡片上的数字 \(a, b, c\) 不完全相同”的概率.
\end{problem}

\begin{problem}
已知函数 \( f(x) = \sin \left(3x + \frac{\pi}{4}\right) \). (1) 求 \( f(x) \) 的单调递增区间; (2) 若 \(\alpha\) 是第二象限角，\(f\left(\frac{\alpha}{3}\right) = \frac{4}{3}\cos \left(\alpha + \frac{\pi}{4}\right)\cos 2\alpha\)，求 \(\cos \alpha - \sin \alpha\) 的值.
\end{problem}

\begin{problem}
在如图所示的多面体中，四边形 \(ABB_{1}A_{1}\) 和 \(ACC_{1}A_{1}\) 都为矩形. (1) 若 \( AC \perp BC \) ，证明：直线 \( BC \perp \) 平面 \( ACC_{1}A_{1} \) ; (2) 设 \(D, E\) 分别是线段 \(BC, CC_{1}\) 的中点，在线段 \(AB\) 上是否存在一点 \(M\)，使直线 \(DE //\) 平面 \(A_{1}MC\)\(\perp\) 请证明你的结论.
\end{problem}

\begin{problem}
已知椭圆 \(C: \frac{x^2}{a^2} + \frac{y^2}{b^2} = 1 (a > b > 0)\) 的左焦点为 \(F(-2,0)\)，离心率为 \(\frac{\sqrt{6}}{3}\). (1) 求椭圆 C 的标准方程; (2) 设 O 为坐标原点，T 为直线 x = -3 上一点，过 F 作 TF 的垂线交椭圆于 P, Q. 当四边形 OPTQ 是平行四边形时，求四边形 OPTQ 的面积.
\end{problem}

\begin{problem}
已知函数 \( f(x) = \e^{x} - ax^{2} - bx - 1 \)，其中 \( a, b \in \R, \e = 2.71828\cdots \) 为自然对数的底数. (1) 设 \( g(x) \) 是函数 \( f(x) \) 的导函数，求函数 \( g(x) \) 在区间 \( [0,1] \) 上的最小值; (2) 若 \( f(1)=0 \) ，函数 \( f(x) \) 在区间 \( (0,1) \) 内有零点，证明：e-2<e<1.
\end{problem}

\begin{problem}
设等差数列 \(\{a_{n}\}\) 的公差为 \(d\)，点 \((a_{n}, b_{n})\) 在函数 \(f(x) = 2^{x}\) 的图象上 \((n \in \mathbf{N}^{*})\). (1) 证明: 数列 \( \{b_{n}\} \) 为等比数列; (2) 若 \(a_{1} = 1\)，函数 \(f(x)\) 的图象在点 \((a_{2}, b_{2})\) 处的切线在 \(x\) 轴上的截距为 \(2 - \frac{1}{\ln 2}\)，求数列 \(\{a_{n} / b_{n}^{n}\}\) 的前 \(n\) 项和 \(S_{n}\).
\end{problem}

