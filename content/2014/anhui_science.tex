\chapter{2014年安徽卷（理）}

\section{单选题}

\begin{problem}
设 \(i\) 是虚数单位，\(\bar z\) 表示复数 \(z\) 的共轭复数. 若 \(z = 1 + \i\)，则 \(\frac{z}{\i} + \i \cdot \bar z = \)（\quad）

\choices
    {\(-2\)}
    {\(-2i\)}
    {2}
    {2i}
\end{problem}

\begin{problem}
“\(x < 0\)”是“\(\ln(x + 1) < 0\)”的（\quad）

\choices
    {充分不必要条件}
    {必要不充分条件}
    {充分必要条件}
    {既不充分也不必要条件}
\end{problem}

\begin{problem}
如图所示，程序框图（算法流程图）的输出结果是（\quad）

\texfigure[max width=0.42\linewidth]{img_repaint/2014/anhui_science/q3_fig1.tex}

\choices
    {34}
    {55}
    {78}
    {89}
\end{problem}

\begin{problem}
以平面直角坐标系的原点为极点，\(x\) 轴的正半轴为极轴，建立极坐标系，两种坐标系中取相同的长度单位，已知直线 \(l\) 的参数方程是 \(\begin{cases}
x = t + 1,\\
y = t - 3,
\end{cases}\)（\(t\) 为参数），圆 \(C\) 的极坐标方程是 \(\rho = 4\cos \theta\)，则直线 \(l\) 被圆 \(C\) 截得的弦长为（\quad）

\choices
    {\(\sqrt{14}\)}
    {\(2 \sqrt{14}\)}
    {\(\sqrt{2}\)}
    {\(2\sqrt{2}\)}
\end{problem}

\begin{problem}
\(x\)，\(y\) 满足约束条件 \(\begin{cases}
x + y - 2 \leqslant 0,\\
x - 2y - 2 \leqslant 0,\\
2x - y + 2 \geqslant 0,
\end{cases}\) 若 \(z = y - ax\) 取得最大值的最优解不唯一，则实数 \(a\) 的值为（\quad）

\choices
    {\(\frac{1}{2}\) 或 \(-1\)}
    {2 或 \(\frac{1}{2}\)}
    {2 或 1}
    {2 或 \(-1\)}
\end{problem}

\begin{problem}
设函数 \(f(x)\)（\(x \in \R\)）满足 \( f(x + \pi) = f(x) + \sin x \). 当 \( 0 \leqslant x < \pi \) 时，\( f(x) = 0 \)，则 \( f\left(\frac{23\pi}{6}\right) = \)（\quad）

\choices
    {\( \frac{1}{2} \)}
    {\( \frac{\sqrt{3}}{2} \)}
    {0}
    {\(-\frac{1}{2}\)}
\end{problem}

\begin{problem}
一个多面体的三视图如图所示，则该多面体的表面积为（\quad）

\bitmapfigure[max width=0.47\linewidth]{img/2014/anhui_science/q7_fig1.png}

\choices
    {\(21 + \sqrt{3}\)}
    {\(18 + \sqrt{3}\)}
    {21}
    {18}
\end{problem}

\begin{problem}
从正方体六个面的对角线中任取两条作为一对，其中所成的角为 \(60^{\circ}\) 的共有（\quad）

\choices
    {24 对}
    {30 对}
    {48 对}
    {60 对}
\end{problem}

\begin{problem}
若函数 \( f(x) = |x + 1| + |2x + a| \) 的最小值为 3，则实数 \( a \) 的值为（\quad）

\choices
    {5 或 8}
    {\(-1\) 或 5}
    {\(-1\) 或 \(-4\)}
    {\(-4\) 或 8}
\end{problem}

\begin{problem}
在平面直角坐标系 \(xOy\) 中，已知向量 \(a\)，\(b\)，\(|a| = |b| = 1\)，\(a \cdot b = 0\)，点 \(Q\) 满足 \(\overrightarrow{OQ} = \sqrt{2}(a + b)\). 曲线 \(C = \left\{P \mid \overrightarrow{OP} = a \cos \theta + b \sin \theta, 0 \leqslant \theta < 2\pi\right\}\)，区域 \(\Omega = \left\{P \mid 0 < r \leqslant \left|\overrightarrow{PQ}\right| \leqslant R, r < R\right\}\). 若 \(C \cap \Omega\) 为两段分离的曲线，则（\quad）

\choices
    {\(1 < r < R < 3\)}
    {\(1 < r < 3 \leqslant R\)}
    {\(r \leqslant 1 < R < 3\)}
    {\(1 < r < 3 < R\)}
\end{problem}

\section{填空题}

\begin{problem}
若将函数 \( f(x) = \sin \left(2x + \frac{\pi}{4}\right) \) 的图象向右平移 \( \varphi \) 个单位，所得图象关于 \( y \) 轴对称，则 \( \varphi \) 的最小正值是\fillinblank{}.
\end{problem}

\begin{problem}
数列 \( \{a_{n}\} \) 是等差数列. 若 \(a_{1} + 1\)，\(a_{3} + 3\)，\(a_{5} + 5\) 构成公比为 \( q \) 的等比数列，则 \( q =\fillinblank{} \).
\end{problem}

\begin{problem}
设 \(a \neq 0\)，\(n\) 是大于 1 的自然数，\(\left(1 + \frac{x}{a}\right)^n\) 的展开式为 \(a_0 + a_1x + a_2x^2 + \cdots + a_nx^n\). 若点 \(A_i(i, a_i)\)（\(i = 0\)，\(1\)，\(2\)）的位置如图所示，

\bitmapfigure[max width=0.34\linewidth]{img/2014/anhui_science/q13_fig1.png}

则 \(a =\fillinblank{}\).
\end{problem}

\begin{problem}
设 \(F_{1}\)，\(F_{2}\) 分别是椭圆 \(E: x^{2} + \frac{y^{2}}{b^{2}} = 1\)（\(0 < b < 1\)）的左，右焦点，过点 \(F_{1}\) 的直线交椭圆 \(E\) 于 \(A\)，\(B\) 两点，若 \(|AF_{1}| = 3|BF_{1}|\)，\(AF_{2} \perp x\text{轴}\)，则椭圆 \(E\) 的方程为\fillinblank{}.
\end{problem}

\begin{problem}
已知两个不相等的非零向量 \(a\)，\(b\)，两组向量 \(x_{1}\)，\(x_{2}\)，\(x_{3}\)，\(x_{4}\)，\(x_{5}\) 和 \(y_{1}\)，\(y_{2}\)，\(y_{3}\)，\(y_{4}\)，\(y_{5}\) 均由 2 个 \(a\) 和 3 个 \(b\) 排列而成，记 \(S = x_{1} \cdot y_{1} + x_{2} \cdot y_{2} + x_{3} \cdot y_{3} + x_{4} \cdot y_{4} + x_{5} \cdot y_{5}\). \(S_{\min}\) 表示 \(S\) 所有可能取值中的最小值. 则下列命题正确的是\fillinblank{}. （写出所有正确命题的编号）

\begin{circlelist}
\item \(S\) 有 5 个不同的值；

\item 若 \(a \perp b\)，则 \(S_{\min}\) 与 \(|a|\) 无关；

\item 若 \(a \parallel b\)，则 \(S_{\min}\) 与 \(|b|\) 无关；

\item 若 \(|b| > 4|a|\)，则 \(S_{\min} > 0\)；

\item 若 \(|b| = 2|a|\)，\(S_{\min} = 8|a|^2\)，则 \(a\) 与 \(b\) 的夹角为 \(\frac{\pi}{4}\).
\end{circlelist}
\end{problem}

\section{解答题}

\begin{problem}
设 \(\triangle ABC\) 的内角 \(A\)，\(B\)，\(C\) 所对边的长分别是 \(a\)，\(b\)，\(c\)，且 \(b = 3\)，\(c = 1\)，\(A = 2B\).

\begin{enumerate}
\item 求 \(a\) 的值；
\item 求 \(\sin\left(A+\frac{\pi}{4}\right)\) 的值.
\end{enumerate}
\end{problem}

\begin{problem}
甲乙两人进行围棋比赛，约定先连胜两局者直接赢得比赛，若赛完 5 局仍未出现连胜，则判定获胜局数多者赢得比赛，假设每局甲获胜的概率为 \(\frac{2}{3}\)，乙获胜的概率为 \(\frac{1}{3}\)，各局比赛结果相互独立.

\begin{enumerate}
\item 求甲在 4 局以内（含 4 局）赢得比赛的概率；
\item 记 \(X\) 为比赛决出胜负时的总局数，求 \(X\) 的分布列和均值（数学期望）.
\end{enumerate}
\end{problem}

\begin{problem}
设函数 \( f(x) = 1 + (1 + a)x - x^2 - x^3 \)，其中 \( a > 0 \).

\begin{enumerate}
\item 讨论 \(f(x)\) 在其定义域上的单调性；
\item 当 \(x \in [0,1]\) 时，求 \(f(x)\) 取得最大值和最小值时的 \(x\) 的值.
\end{enumerate}
\end{problem}

\begin{problem}
如图，已知两条抛物线 \(E_{1}: y^{2} = 2p_{1}x\)（\(p_{1} > 0\)）和 \(E_{2}: y^{2} = 2p_{2}x\)（\(p_{2} > 0\)），过原点 \(O\) 的两条直线 \(l_{1}\) 和 \(l_{2}\)，\(l_{1}\) 与 \(E_{1}\)，\(E_{2}\) 分别交于 \(A_{1}\)，\(A_{2}\) 两点，\(l_{2}\) 与 \(E_{1}\)，\(E_{2}\) 分别交于 \(B_{1}\)，\(B_{2}\) 两点.

\begin{enumerate}
\item 证明：\(A_{1}B_{1} \parallel A_{2}B_{2}\)；
\item 过原点 \(O\) 作直线 \(l\)（异于 \(l_{1}\)，\(l_{2}\)）与 \(E_{1}\)，\(E_{2}\) 分别交于 \(C_{1}\)，\(C_{2}\) 两点. 记 \(\triangle A_{1}B_{1}C_{1}\) 与 \(\triangle A_{2}B_{2}C_{2}\) 的面积分别为 \(S_{1}\) 与 \(S_{2}\)，求 \(\frac{S_{1}}{S_{2}}\) 的值.
\end{enumerate}

\bitmapfigure[max width=0.42\linewidth]{img/2014/anhui_science/q19_fig1.png}
\end{problem}

\begin{problem}
如图，四棱柱 \(ABCD - A_{1}B_{1}C_{1}D_{1}\) 中，\(A_{1}A \perp\) 底面 \(ABCD\). 四边形 \(ABCD\) 为梯形，\(AD \parallel BC\)，且 \(AD = 2BC\). 过 \(A_{1}\)，\(C\)，\(D\) 三点的平面记为 \(\alpha\)，\(BB_{1}\) 与 \(\alpha\) 的交点为 \(Q\).

\begin{enumerate}
\item 证明：\(Q\) 为 \(BB_{1}\) 的中点；
\item 求此四棱柱被平面 \(\alpha\) 所分成上下两部分的体积之比；
\item 若 \(A_{1}A = 4\)，\(CD = 2\)，梯形 \(ABCD\) 的面积为 6，求平面 \(\alpha\) 与底面 \(ABCD\) 所成二面角的大小.
\end{enumerate}

\bitmapfigure[max width=0.42\linewidth]{img/2014/anhui_science/q20_fig1.png}
\end{problem}

\begin{problem}
设实数 \(c > 0\)，整数 \(p > 1\)，\(n \in \N^*\).

\begin{enumerate}
\item 证明：当 \(x > -1\) 且 \(x \neq 0\) 时，\((1 + x)^p > 1 + px\)；
\item 数列 \(\{a_{n}\}\) 满足 \(a_1 > c^{1/p}\)，\(a_{n+1} = \frac{p-1}{p}a_n + \frac{c}{p}a_n^{1-p}\)，证明：\(a_n > a_{n+1} > c^{1/p}\).
\end{enumerate}
\end{problem}
