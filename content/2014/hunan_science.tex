\chapter{2014年湖南卷（理）}

\section{单选题}

\begin{problem}
满足 \(\frac{x + 1}{z} = \i\) (i 为虚数单位) 的复数 \(z =\)（\quad）

\choices
    {\(\frac{1}{2} + \frac{1}{2}\i\)}
    {\(\frac{1}{2} - \frac{1}{2}\i\)}
    {\(-\frac{1}{2} + \frac{1}{2}\i\)}
    {\(-\frac{1}{2} - \frac{1}{2}\i\)}
\end{problem}

\begin{problem}
对一个容量为 \(N\) 的总体抽取容量为 \(n\) 的样本，当选取简单随机抽样、系统抽样和分层抽样三种不同方法抽取样本时，总体中每个个体被抽中的概率分别为 \(p_1, p_2, p_3\)，则（\quad）

\choices
    {\(p_1 = p_2 < p_3\)}
    {\(p_2 = p_3 < p_1\)}
    {\(p_1 = p_3 < p_2\)}
    {\(p_1 = p_2 = p_3\)}
\end{problem}

\begin{problem}
已知 \( f(x) \) ，\( g(x) \) 分别定义是在 R 上的偶函数和奇函数，且 \( f(x) - g(x) = x^{3} + x^{2} + 1 \) ，则 \( f(1) + g(1) = \)（\quad）

\choices
    {\(-3\)}
    {\(-1\)}
    {1}
    {3}
\end{problem}

\begin{problem}
\(\left(\frac{1}{2} x - 2b\right)^5\) 的展开式中 \(x^2 y^4\) 的系数是（\quad）

\choices
    {\(-20\)}
    {\(-5\)}
    {5}
    {20}
\end{problem}

\begin{problem}
已知命题 \(p\) 若 \(x > y\)，则 \(-x < -y\); 命题 \(q\) 若 \(x > y\)，则 \(x^2 > y^2\)，在命题\circled{1} \(p \wedge q\); \circled{2} \(p \vee q\); \circled{3} \(p \wedge (\neg q)\); \circled{4} \((\neg y) \vee q\) 中，真命题是（\quad）

\choices
    {\circled{1}\circled{3}}
    {\circled{1}\circled{4}}
    {\circled{2}\circled{3}}
    {\circled{2}\circled{4}}
\end{problem}

\begin{problem}
执行如图所示的程序框图，如果输入的 \(t \in [-2, 2]\)，则输出的 \(S\) 属于（\quad）

\choices
    {\([-6, - 2]\)}
    {\([-5, - 1]\)}
    {\([-4, 5]\)}
    {\([-3, 6]\)}
\end{problem}

\begin{problem}
一块石材表示的几何体的三视图如图所示，将该石材切削、打磨，加工成球，则能得到的最大球的半径等于（\quad）

正视图

侧视图

俯视图（\quad）

\choices
    {1}
    {2}
    {3}
    {4}
\end{problem}

\begin{problem}
某市生产总值连续两年持续增加. 第一年的增长率为 \( p \)，第二年的增长率为 \( q \)，则该市这两年生产总值的年平均增长率为（\quad）

\choices
    {\(\frac{p + q}{2}\)}
    {\(\frac{(p + 1)(q + 1) - 1}{2}\)}
    {\(\sqrt{pq}\)}
    {\(\sqrt{(p + 1)(q + 1)} - 1\)}
\end{problem}

\begin{problem}
已知函数 \( f(x) = \sin (x - \varphi) \)，且 \( \int_{0}^{\frac{\pi}{3}} f(x) \, \mathrm{d}x = 0 \)，则函数 \( f(x) \) 的图象的一条对称轴是（\quad）

\choices
    {\(x = \frac{5\pi}{6}\)}
    {\(x = \frac{7\pi}{12}\)}
    {\(x = \frac{\pi}{3}\)}
    {\(x = \frac{\pi}{6}\)}
\end{problem}

\begin{problem}
已知函数 \( f(x) = x^2 + \e^x - \frac{1}{2} (x < 0) \) 与 \( g(x) = x^2 + \ln (x + a) \) 的图象上存在关于 \( y \) 轴对称的点，则 \( a \) 的取值范围是（\quad）

\choices
    {\(\left(-\infty, \frac{1}{\sqrt{\e}}\right)\)}
    {\((-\infty, \sqrt{\e})\)}
    {\(\left(-\frac{1}{\sqrt{\e}}, \sqrt{\e}\right)\)}
    {\(\left(-\sqrt{\e}, \frac{1}{\sqrt{\e}}\right)\)}
\end{problem}

\section{填空题}

\begin{problem}
在平面直角坐标系中，倾斜角为 \(\frac{\pi}{4}\) 的直线 \(l\) 与曲线 \(C: \left\{ \begin{array}{l} x = 2 + \cos \alpha, \\ y = 1 + \sin \alpha. \end{array} \right.\) (\(\alpha\) 为参数) 关于 \(A, B\) 两点，且 \(|AB| = 2\)，以坐标原点 \(O\) 为极点，\(x\) 轴正半轴为极轴建立极坐标系，则直线 \(l\) 的极坐标方程是 \fillinblank{} \fillinblank{}.
\end{problem}

\begin{problem}
如图，已知 \(AB, BC\) 是 \(\odot O\) 的两条弦，\(AO \perp BC\)，\(AB = \sqrt{3}\)，\(BC = 2\sqrt{2}\)，则 \(\odot O\) 的半径等于 \fillinblank{} \fillinblank{}.
\end{problem}

\begin{problem}
若关于 \(x\) 的不等式 \(\left|ax - 2\right| < 3\) 的解集为 \(\left\{x\mid -\frac{5}{3} < x < \frac{1}{3}\right\}\)，则 \(a = \fillinblank{}\).
\end{problem}

\begin{problem}
若变量 \(x, y\) 满足约束条件 \(\left\{ \begin{array}{l} 9 \leqslant x, \\ x + y \leqslant 4, \\ y \geqslant k, \end{array} \right.\) 且 \(z = 2x + y\) 的最小值为\(-6\)，则 \(k = \)  \fillinblank{}.
\end{problem}

\begin{problem}
如图，正方形 \(ABCD\) 和正方形 \(DEFG\) 的边长分别为 \(a, b (a < b)\)，原点 \(O\) 为 \(AD\) 的中点，抛物线 \(y^{2} = 2px (p > 0)\) 经过 \(C, F\) 两点，则 \(\frac{a}{b} = \)  \fillinblank{}.
\end{problem}

\begin{problem}
在平面直角坐标系中， \(O\) 为原点， \(A(-1,0),B(0,\sqrt{3}),C(3,0)\) .动点 \(D\) 满足 \(\left|\overrightarrow{CD}\right| = 1\) ，则 \(\left|\overrightarrow{OA} +\overrightarrow{OB} +\overrightarrow{OD}\right|\) 的最大值是
\end{problem}

\section{解答题}

\begin{problem}
某企业有甲、乙两个研发小组，他们研发新产品成功的概率分别为 \(\frac{2}{3}\) 和 \(\frac{2}{5}\)，现安排甲组研发新产品 A，乙组研发新产品 B. 设甲、乙两组的研发相互独立. (1) 求至少有一种新产品研发成功的概率; (2) 若新产品 A 研发成功，预计企业可获利 120 万元; 若新产品 B 研发成功，预计企业可获利 100 万元. 求该企业可获利的分布和初数学期望.
\end{problem}

\begin{problem}
如图，在平面四边形 \(ABCD\) 中，\(AD = 1\)，\(CD = 2\)，\(AC = \sqrt{7}\). (1) 求 \(\cos \angle CAD\) 的值; (2) 若 \( \cos\angle BAD = -\frac{\sqrt{7}}{14} \) ， \( \sin\angle CBA = \frac{\sqrt{21}}{6} \) . 求 BC 的长.
\end{problem}

\begin{problem}
如图，四棱柱 \(ABCD - A_{1}B_{1}C_{1}D_{1}\) 的所有棱长都相等，\(AC \cap BD = O\)，\(A_{1}C_{1} \cap B_{1}D_{1} = O_{1}\)，四边形 \(ACC_{1}A_{1}\) 和四边形 \(BDD_{1}B_{1}\) 均为矩形. (1) 证明: \(O_{1}O \perp\) 底面 \(ABCD\); (2) 若 \(\angle CBA = 60^{\circ}\)，求二面角 \(C_{1} - OB_{1} - D\) 的余弦值.
\end{problem}

\begin{problem}
如图，\(O\) 为坐标原点，椭圆 \(C_1: \frac{x^2}{a^2} + \frac{y^2}{b^2} = 1 (a > b > 0)\) 的左、右焦点分别为 \(F_1, F_2\)，离心率为 \(c_1\); 双曲线 \(C_2: \frac{x^2}{a^2} - \frac{y^2}{b^2} = 1\) 的左、右焦点分别为 \(F_3, F_4\)，离心率为 \(c_2\). 已知 \(c_1 c_2 = \frac{\sqrt{3}}{2}\)，且 \(|F_2 F_4| = \sqrt{3} - 1\). (1) 求 \( C_{1}, C_{2} \) 的方程; (2) 过 \(F_{1}\) 作 \(C_{1}\) 的不垂直于 \(y\) 轴的弦 \(AB, M\) 为弦 \(AB\) 的中点. 当直线 \(OM\) 与 \(C_{2}\) 交于 \(P, Q\) 两点时，求四边形 \(APBQ\) 面积的最小值.
\end{problem}

\begin{problem}
已知常数 \(a > 0\)，函数 \(f(x) = \ln (1 + ax) - \frac{2x}{x + 2}\). (1) 讨论 \( f(x) \) 在区间 \( (0, +\infty) \) 上的单调性. (2) 若 \( f(x) \) 存在两个极值点 \( x_{1}, x_{2} \) ，且 \( f(x_{1}) + f(x_{2}) > 0 \) ，求 a 的取值范围.
\end{problem}

\begin{problem}
已知数列 \(\{a_{n}\}\) 满足 \(a_1 = 1, |a_{n + 1} - a_n| = p^n, n \in \mathbf{N}^*\). (1) 若 \( \{a_{n}\} \) 是递增数列，且 \( a_{1}, 2a_{2}, 3a_{3} \) 成等差数列，求 p 的值； (2) 若 \( p = \frac{1}{2} \)，且 \( \{a_{2n-1}\} \) 是递增数列，\( \{a_{2n}\} \) 是递减数列，求数列 \( \{a_n\} \) 的通项公式.
\end{problem}

