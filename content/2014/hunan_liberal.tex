\chapter{2014年湖南卷（文）}

\section{单选题}

\begin{problem}
设命题 \(p: \forall x \in \R, x^2 + 1 > 0\)，则 \(\neg p\) 为（\quad）

\choices
    {\(\exists x_0 \in \R, x_0^2 + 1 > 0\)}
    {\(\exists x_0 \in \R, x_0^2 + 1 \leqslant 0\)}
    {\(\exists x_0 \in \R, x_0^2 + 1 < 0\)}
    {\(\forall x \in \R, x^2 + 1 \leqslant 0\)}
\end{problem}

\begin{problem}
已知集合 \(A = \{x \mid x > 2\}\)，\(B = \{x \mid 1 < x < 3\}\)，则 \(A \cap B =\)（\quad）

\choices
    {\( \{x \mid x > 2\} \)}
    {\( \{x \mid x > 1\} \)}
    {\(\{x\mid 2 < x < 3\}\)}
    {\(\{x\mid 1 < x < 3\}\)}
\end{problem}

\begin{problem}
对一个容量为 \(N\) 的总体抽取容量为 \(n\) 的样本，当选取简单随机抽样、系统抽样和分层抽样三种不同方法抽取样本时，总体中每个个体被抽中的概率分别为 \(p_1, p_2, p_3\)，则（\quad）

\choices
    {\(p_1 = p_2 < p_3\)}
    {\(p_2 = p_3 < p_1\)}
    {\(p_1 = p_3 < p_2\)}
    {\(p_1 = p_2 = p_3\)}
\end{problem}

\begin{problem}
下列函数中，既是偶函数又在区间 \((- \infty, 0)\) 上单调递增的是（\quad）

\choices
    {\(f(x) = \frac{1}{x^{2}}\)}
    {\(f(x) = x^{2} + 1\)}
    {\(f(x) = x^{3}\)}
    {\(f(x) = 2^{-x}\)}
\end{problem}

\begin{problem}
在区间 \([-2,3]\) 上随机选取一个数 \(X\)，则 \(X \leqslant 1\) 的概率为（\quad）

\choices
    {\(\frac{4}{5}\)}
    {\(\frac{3}{5}\)}
    {\( \frac{2}{5} \)}
    {\(\frac{1}{5}\)}
\end{problem}

\begin{problem}
若圆 \(C_1: x^2 + y^2 = 1\) 与圆 \(C_2: x^2 + y^2 - 6x - 8y + m = 0\) 外切，则 \(m =\)（\quad）

\choices
    {21}
    {19}
    {9}
    {\(-11\)}
\end{problem}

\begin{problem}
执行如图所示的程序框图，如果输入的 \(t \in [-2, 2]\)，则输出的 \(S\) 属于（\quad）

\bitmapfigure[max width=0.38\linewidth,max totalheight=4.8cm]{img/2014/hunan_liberal/q7_fig1.png}

\choices
    {\([-6, - 2]\)}
    {\([-5, - 1]\)}
    {\([-4, 5]\)}
    {\([-3,6]\)}
\end{problem}

\begin{problem}
一块石材表示的几何体的三视图如图所示，将该石材切削、打磨，加工成球，则能得到的最大球的半径等于（\quad）

\bitmapfigure[max width=0.45\linewidth,max totalheight=4.8cm]{img/2014/hunan_liberal/q8_fig1.png}

\choices
    {1}
    {2}
    {3}
    {4}
\end{problem}

\begin{problem}
若 \(0 < x_{1} < x_{2} < 1\)，则（\quad）

\choices
    {\(\e^{x_2} - \e^{x_1} > \ln x_2 - \ln x_1\)}
    {\(\e^{x_2} - \e^{x_1} < \ln x_2 - \ln x_1\)}
    {\(x_{2}\e^{x_{1}} > x_{1}\e^{x_{2}}\)}
    {\(x_{2}\e^{x_{1}} < x_{1}\e^{x_{2}}\)}
\end{problem}

\begin{problem}
在平面直角坐标系中，\(O\) 为原点，\(A(-1,0)\)，\(B(0,\sqrt{3})\)，\(C(3,0)\). 动点 \(D\) 满足 \(\left|\overrightarrow{CD}\right| = 1\)，则 \(\left|\overrightarrow{OA} + \overrightarrow{OB} + \overrightarrow{OD}\right|\) 的取值范围是（\quad）

\choices
    {[4, 6]}
    {\([\sqrt{19} - 1, \sqrt{19} + 1]\)}
    {\([2\sqrt{3}, 2\sqrt{7}]\)}
    {\([\sqrt{7} - 1, \sqrt{7} + 1]\)}
\end{problem}
\section{填空题}

\begin{problem}
复数 \(\frac{3 + \i}{\i^2}\)（\(i\) 为虚数单位）的实部等于 \fillinblank{}.
\end{problem}

\begin{problem}
在平面直角坐标系中，曲线 \(C: \left\{ \begin{array}{l} x = 2 + \frac{\sqrt{2}}{2} t, \\ y = 1 + \frac{\sqrt{2}}{2} t, \end{array} \right.\) （\(t\) 为参数）的普通方程  \fillinblank{}.
\end{problem}

\begin{problem}
若变量 \(x, y\) 满足约束条件 \(\left\{ \begin{array}{l} y \leqslant x, \\ x + y \leqslant 4, \\ y \geqslant 1, \end{array} \right.\) 则 \(z = 2x + y\) 的最大值为 \fillinblank{}.
\end{problem}

\begin{problem}
平面上一机器人在行进中始终保持与点 \(F(1,0)\) 的距离和到直线 \(x = -1\) 的距离相等. 若机器人接触不到过点 \(P(-1,0)\) 且斜率为 \(k\) 的直线，则 \(k\) 的取值范围是 \fillinblank{}.
\end{problem}

\begin{problem}
若 \( f(x) = \ln (\e^{3x} + 1) + ax \) 是偶函数，则 \( a = \)  \fillinblank{}.
\end{problem}

\section{解答题}

\begin{problem}
已知数列 \(\{a_{n}\}\) 的前 \(n\) 项和 \(S_{n} = \frac{n^{2} + n}{2}, n \in \mathbf{N}^{*}\).

\begin{enumerate}
\item 求数列 \( \{a_{n}\} \) 的通项公式；
\item 设 \( b_{n} = 2^{a_{n}} + (-1)^{n}a_{n} \)，求数列 \( \{b_{n}\} \) 的前 \( 2n \) 项和.
\end{enumerate}
\end{problem}

\begin{problem}
某企业有甲、乙两个研发小组，为了比较他们的研发水平，现随机抽取这两个小组往年研发新产品的结果如下：\((a, b), (a, \overline{b}), (a, b), (\overline{a}, b), (\overline{a}, \overline{b})\)，
\((a, b), (a, b), (a, \overline{b}), (\overline{a}, b), (a, \overline{b})\)，
\((\overline{a}, \overline{b}), (a, b), (a, \overline{b}), (\overline{a}, b), (a, b)\)，其中 \(a, \overline{a}\) 分别表示甲组研发成功和失败；\(b, \overline{b}\) 分别表示乙组研发成功和失败.

\begin{enumerate}
\item 若某组成功研发一种新产品，则给该组记 1 分，否则记 0 分。试计算甲、乙两组研发新产品的成绩的平均数和方差，并比较甲、乙两组的研发水平；
\item 若该企业安排甲、乙两组各自研发一种新产品，试估计恰有一组研发成功的概率.
\end{enumerate}
\end{problem}

\begin{problem}
如图，已知二面角 \(\alpha - MN - \beta\) 的大小为 \(60^{\circ}\)，菱形 \(ABCD\) 在面 \(\beta\) 内，\(A, B\) 两点在棱 \(MN\) 上，\(\angle BAD = 60^{\circ}\)，\(E\) 是 \(AB\) 的中点，\(DO \perp\) 面 \(\alpha\)，垂足为 \(O\).

\bitmapfigure[max width=0.45\linewidth,max totalheight=4.8cm]{img/2014/hunan_liberal/q18_fig1.png}

\begin{enumerate}
\item 证明： \(AB \perp\) 平面 \(ODE\)；
\item 求异面直线 BC 与 OD 所成角的余弦值.
\end{enumerate}

\end{problem}

\begin{problem}
如图，在平面四边形 \(ABCD\) 中，\(DA \perp AB\)，\(DE = 1\)，\(EC = \sqrt{7}\)，\(EA = 2\)，\(\angle ADC = \frac{2\pi}{3}\)，\(\angle BEC = \frac{\pi}{3}\).

\bitmapfigure[max width=0.38\linewidth,max totalheight=4.8cm]{img/2014/hunan_liberal/q19_fig1.png}

\begin{enumerate}
\item 求 \( \sin \angle CED \) 的值；
\item 求 BE 的长.
\end{enumerate}

\end{problem}

\begin{problem}
如图，\(O\) 为坐标原点，双曲线 \(C_{1}:\frac{x^{2}}{a_{1}^{2}} - \frac{y^{2}}{b_{1}^{2}} = 1\,(a_{1} > 0, b_{1} > 0)\) 和椭圆 \(C_{2}:\frac{y^{2}}{a_{2}^{2}} + \frac{x^{2}}{b_{2}^{2}} = 1\,(a_{2} > b_{2} > 0)\) 均过点 \(P\left(\frac{2\sqrt{3}}{3},1\right)\)，且以 \(C_{1}\) 的两个顶点和 \(C_{2}\) 的两个焦点为顶点的四边形是面积为 \(2\) 的正方形.

\bitmapfigure[max width=0.42\linewidth,max totalheight=4.8cm]{img/2014/hunan_liberal/q20_fig1.png}

\begin{enumerate}
\item 求 \(C_1, C_2\) 的方程；
\item 是否存在直线 \(l\)，使得 \(l\) 与 \(C_1\) 交于 \(A, B\) 两点，与 \(C_2\) 只有一个公共点，且 \(\left|\overrightarrow{OA} + \overrightarrow{OB}\right| = \left|\overrightarrow{AB}\right|\)，证明你的结论.
\end{enumerate}

\end{problem}

\begin{problem}
已知函数 \( f(x) = x \cos x - \sin x + 1 \,(x > 0)\).

\begin{enumerate}
\item 求 \( f(x) \) 的单调区间；
\item 记 \(x_{i}\) 为 \(f(x)\) 的从小到大的第 \(i\)（\(i \in \mathbf{N}^{*}\)）个零点，证明：对一切 \(n \in \mathbf{N}^{*}\)，有 \(\frac{1}{x_1^2} + \frac{1}{x_2^2} + \cdots + \frac{1}{x_n^2} < \frac{2}{3}\).
\end{enumerate}
\end{problem}
