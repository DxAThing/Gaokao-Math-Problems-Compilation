\chapter{2014年浙江卷（文）}

\section{单选题}

\begin{problem}
设集合 \( S = \{x \mid x \geqslant 2\} \)，\( T = \{x \mid x \leqslant 5\} \)，则 \( S \cap T = \)（\quad）

\choices
    {\((- \infty, 5]\)}
    {\([2, +\infty)\)}
    {(2,5)}
    {\([2,5]\)}
\end{problem}

\begin{problem}
设四边形 \(ABCD\) 的两条对角线为 \(AC, BD\)，则“四边形 \(ABCD\) 为菱形”是 “\(AC \perp BD\)”的（\quad）

\choices
    {充分不必要条件}
    {必要不充分条件}
    {充分必要条件}
    {既不充分也不必要条件}
\end{problem}

\begin{problem}
某几何体的三视图（单位：cm）如图所示，则该几何体的体积是（\quad）

\bitmapfigure[max width=0.62\linewidth,max totalheight=4.8cm]{img/2014/zhejiang_liberal/q3_fig1.png}

\choices
    {\(72 \mathrm{~cm}^{3}\)}
    {\(90 \mathrm{~cm}^{3}\)}
    {\(108 \mathrm{~cm}^{3}\)}
    {\(138\mathrm{cm}^3\)}
\end{problem}

\begin{problem}
为了得到函数 \( y = \sin 3x + \cos 3x \) 的图象，可以将函数 \( y = \sqrt{2}\cos 3x \) 的图象（\quad）

\choices
    {向右平移 \(\frac{\pi}{4}\) 个单位}
    {向左平移 \(\frac{\pi}{4}\) 个单位}
    {向右平移 \(\frac{\pi}{12}\) 个单位}
    {向左平移 \(\frac{\pi}{12}\) 个单位}
\end{problem}

\begin{problem}
已知圆 \(x^{2} + y^{2} + 2x - 2y + a = 0\) 截直线 \(x + y + 2 = 0\) 所得弦的长度为4，则实数 \(a\) 的值为（\quad）

\choices
    {\(-2\)}
    {\(-4\)}
    {\(-6\)}
    {\(-8\)}
\end{problem}

\begin{problem}
设 \(m, n\) 是两条不同的直线，\(\alpha, \beta\) 是两个不同的平面（\quad）

\choices
    {若 \(m \perp n, n \parallel \alpha\)，则 \(m \perp \alpha\)}
    {若 \(m \parallel \beta, \beta \perp \alpha\)，则 \(m \perp \alpha\)}
    {若 \(m \perp \beta, n \perp \beta, n \perp \alpha\)，则 \(m \perp \alpha\)}
    {若 \(m \perp n, n \perp \beta, \beta \perp \alpha\)，则 \(m \perp \alpha\)}
\end{problem}

\begin{problem}
已知函数 \( f(x) = x^{3} + ax^{2} + bx + c \)，且 \( 0 < f(-1) = f(-2) = f(-3) \leqslant 3 \)，则（\quad）

\choices
    {\(c \leqslant 3\)}
    {\(3 < c \leqslant 6\)}
    {\(6 < c \leqslant 9\)}
    {\(c > 9\)}
\end{problem}

\begin{problem}
在同一直角坐标系中，函数 \( f(x) = x^a (x \geqslant 0) \)，\( g(x) = \log_a x \) 的图象可能是（\quad）

\choices
    {\includegraphics[width=0.20\linewidth]{img/2014/zhejiang_liberal/q8_choice_a.png}}
    {\includegraphics[width=0.20\linewidth]{img/2014/zhejiang_liberal/q8_choice_b.png}}
    {\includegraphics[width=0.20\linewidth]{img/2014/zhejiang_liberal/q8_choice_c.png}}
    {\includegraphics[width=0.20\linewidth]{img/2014/zhejiang_liberal/q8_choice_d.png}}
\end{problem}

\begin{problem}
设 \(\theta\) 为两个非零向量 \(a, b\) 的夹角. 已知对任意实数 \(t, |b + t a|\) 的最小值为 1. 则（\quad）

\choices
    {若 \(\theta\) 确定，则 \(|a|\) 唯一确定}
    {若 \(\theta\) 确定，则 \(|b|\) 唯一确定}
    {若 \(|a|\) 确定，则 \(\theta\) 唯一确定}
    {若 \(|b|\) 确定，则 \(\theta\) 唯一确定}
\end{problem}

\begin{problem}
如图，某人在垂直于水平地面 \(ABC\) 的墙面前的点 \(A\) 处进行射击训练，已知点 \(A\) 到墙面的距离为 \(AB\)，某目标点 \(P\) 沿墙面上的射线 \(CM\) 移动，此人为了准确瞄准目标点 \(P\)，需计算出点 \(A\) 观察点 \(P\) 的仰角 \(\theta\) 的大小（仰角 \(\theta\) 为直线 \(AP\) 与平面 \(ABC\) 所成的角）. 若 \(AB = 15 \mathrm{~m}, AC = 25 \mathrm{~m}, \angle BCM = 30^{\circ}\)，则 \(\tan \theta\) 的最大值是（\quad）

\bitmapfigure[max width=0.36\linewidth,max totalheight=4.8cm]{img/2014/zhejiang_liberal/q10_fig1.png}

\choices
    {\(\frac{\sqrt{30}}{5}\)}
    {\(\frac{\sqrt{30}}{10}\)}
    {\(\frac{4\sqrt{3}}{9}\)}
    {\(\frac{5\sqrt{3}}{9}\)}
\end{problem}

\section{填空题}

\begin{problem}
已知 \(i\) 是虚数单位，计算 \( \frac{1-\i}{(1+\i)^{2}} =\fillinblank{} \).
\end{problem}

\begin{problem}
若实数 \(x, y\) 满足 \(\left\{ \begin{array}{l} x + 2y - 4 \leqslant 0, \\ x - y - 1 \leqslant 0, \\ x \geqslant 1, \end{array} \right.\) 则 \(x + y\) 的取值范围是\fillinblank{}.
\end{problem}

\begin{problem}
若某程序框图如图所示，当输入 50 时，则该程序运行后输出的结果是\fillinblank{}.

\bitmapfigure[max width=0.26\linewidth,max totalheight=7.4cm]{img/2014/zhejiang_liberal/q13_fig1.png}
\end{problem}

\begin{problem}
在 3 张奖券中有一，二等奖各一张，另 1 张无奖. 甲，乙两人各抽取一张，两人都中奖的概率为\fillinblank{}.
\end{problem}

\begin{problem}
设函数 \( f(x) = \begin{cases} x^2 + 2x + 2, & x \leqslant 0, \\ -x^2, & x > 0. \end{cases} \) 若 \( f(f(a)) = 2 \)，则 \( a = \)\fillinblank{}.
\end{problem}

\begin{problem}
已知实数 \(a, b, c\) 满足 \(a + b + c = 0, a^2 + b^2 + c^2 = 1\)，则 \(a\) 的最大值是\fillinblank{}.
\end{problem}

\begin{problem}
设直线 \(x - 3y + m = 0 (m \neq 0)\) 与双曲线 \(\frac{x^2}{a^2} - \frac{y^2}{b^2} = 1 (a > 0, b > 0)\) 的两条渐近线分别交于点 \(A, B\). 若点 \(P(m, 0)\) 满足 \(|PA| = |PB|\)，则该双曲线的离心率是\fillinblank{}.
\end{problem}

\section{解答题}

\begin{problem}
在 \(\triangle ABC\) 中，内角 \(A, B, C\) 所对的边分别为 \(a, b, c\). 已知 \(4\sin^2\frac{A - B}{2} + 4\sin A\sin B = 2 + \sqrt{2}\).

\begin{enumerate}
\item 求角 \(C\) 的大小;
\item 已知 \(b = 4\)，\(\triangle ABC\) 的面积为 6，求边长 \(c\) 的值.
\end{enumerate}
\end{problem}

\begin{problem}
已知等差数列 \(\{a_{n}\}\) 的公差 \(d > 0\)，设 \(\{a_{n}\}\) 的前 \(n\) 项和为 \(S_{n}\)，\(a_{1} = 1\)，\(S_{2} \cdot S_{3} = 36\).

\begin{enumerate}
\item 求 \(d\) 及 \(S_{n}\);
\item 求 \(m, k\)（\(m, k \in \mathbf{N}^*\)）的值，使得 \(a_m + a_{m+1} + a_{m+2} + \cdots + a_{m+k} = 65\).
\end{enumerate}
\end{problem}

\begin{problem}
已知函数 \( f(x) = x^3 + 3|x - a| \) (\( a > 0 \))，若 \( f(x) \) 在 \([-1, 1]\) 上的最小值记为 \( g(a) \).

\begin{enumerate}
\item 求 \(g(a)\);
\item 证明：当 \(x \in [-1, 1]\) 时，恒有 \(f(x) \leqslant g(a) + 4\).
\end{enumerate}
\end{problem}

\begin{problem}
已知 \(\triangle ABP\) 的三个顶点都在抛物线 \(C: x^2 = 4y\) 上，\(F\) 为抛物线 \(C\) 的焦点，点 \(M\) 为 \(AB\) 的中点，\(\overrightarrow{PF} = 3\overrightarrow{FM}\).

\begin{enumerate}
\item 若 \(|PF| = 3\)，求点 \(M\) 的坐标;
\item 求 \(\triangle ABP\) 面积的最大值.
\end{enumerate}
\bitmapfigure[max width=0.42\linewidth,max totalheight=4.8cm]{img/2014/zhejiang_liberal/q22_fig1.png}
\end{problem}

\begin{problem}
如图，在四棱锥 \(A - BCDE\) 中，平面 \(ABC \perp\) 平面 \(BCDE\)，\(\angle CDE = \angle BED = 90^\circ\)，\(AB = CD = 2\)，\(DE = BE = 1\)，\(AC = \sqrt{2}\).

\begin{enumerate}
\item 证明: \(AC \perp\) 平面 \(BCDE\);
\item 求直线 \(AE\) 与平面 \(ABC\) 所成的角的正切值.
\end{enumerate}

\bitmapfigure[max width=0.42\linewidth,max totalheight=4.8cm]{img/2014/zhejiang_liberal/q20_fig1.png}
\end{problem}
