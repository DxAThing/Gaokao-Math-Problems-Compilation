\chapter{2008年安徽卷（理）}

\section{单选题}

\begin{problem}
复数 \(\i^2 (1 + \i)^2 =\)（\quad）

\choices
    {2}
    {\(-2\)}
    {2i}
    {-2i}
\end{problem}

\begin{problem}
集合 \(A = \{y \in \R \mid y = \lg x, x > 1\}\)，\(B = \{-2, -1, 1, 2\}\) 则下列结论正确的是（\quad）

\choices
    {\(A \cap B = \{2, -1\}\)}
    {\((\complement_{\R}A)\cup B = (-\infty ,0)\)}
    {\(A \cup B = (0, +\infty)\)}
    {\((\complement_{\R}A)\cap B = \{-2, - 1\}\)}
\end{problem}

\begin{problem}
在平行四边形 \(ABCD\) 中，\(AC\) 为一条对角线，若 \(\overrightarrow{AB} = (2, 4)\)，\(\overrightarrow{AC} = (1, 3)\)，则 \(\overrightarrow{BD} =\)（\quad）

\choices
    {\((-2, - 4)\)}
    {\((-3, - 5)\)}
    {(3, 5)}
    {(3, 2)}
\end{problem}

\begin{problem}
已知 m, n 是两条不同直线，\( \alpha, \beta, \gamma \) 是三个不同平面，下列命题中正确的是（\quad）

\choices
    {若 \(m // \alpha, n // \alpha\)，则 \(m // n\)}
    {若 \(\alpha \perp \gamma, \beta \perp \gamma\)，则 \(\alpha \not\perp \beta\)}
    {若 \(m // \alpha, m // \beta\)，则 \(\alpha // \beta\)}
    {若 \(m \perp \alpha, n \perp \alpha\)，则 \(m // n\)}
\end{problem}

\begin{problem}
将函数 \( y = \sin \left(2\pi + \frac{\pi}{3}\right) \) 的图象按向量 \( \mathbf{a} \) 平移后所得的图象关于点 \( \left(-\frac{\pi}{12}, 0\right) \) 中心对称，则向量 \( \mathbf{a} \) 的坐标可能为（\quad）

\choices
    {\( \left(-\frac{\pi}{12},0\right) \)}
    {\( \left(-\frac{\pi}{6},0\right) \)}
    {\(\left(\frac{\pi}{12},0\right)\)}
    {\(\left(\frac{\pi}{6},0\right)\)}
\end{problem}

\begin{problem}
设 \((1 + x)^{8} = a_{0} + a_{1}x + \dots +a_{8}x^{8}\)，则 \(a_0, a_1, \dots, a_8\) 中奇数的个数为（\quad）

\choices
    {2}
    {3}
    {4}
    {5}
\end{problem}

\begin{problem}
\(a < 0\) 是方程 \(ax^2 + 2x + 1 = 0\) 至少有一个负数根的（\quad）

\choices
    {必要不充分条件}
    {充分不必要条件}
    {充分必要条件}
    {既不充分也不必要条件}
\end{problem}

\begin{problem}
若过点 \(A(4,0)\) 的直线 \(l\) 与曲线 \((x - 2)^2 + y^2 = 1\) 有公共点，则直线 \(l\) 的斜率的取值范围为（\quad）

\choices
    {\([- \sqrt{3}, \sqrt{3} ]\)}
    {\((- \sqrt{3}, \sqrt{3} ]\)}
    {\(\left[-\frac{\sqrt{3}}{3}, \frac{\sqrt{3}}{3}\right]\)}
    {\(\left(-\frac{\sqrt{3}}{3}, \frac{\sqrt{3}}{3}\right)\)}
\end{problem}

\begin{problem}
在同一平面直角坐标系中，函数 \( y = g(x) \) 的图象与 \( y = \e^{x} \) 的图象关于直线 \( y = x \) 对称. 而函数 \( y = f(x) \) 的图象与 \( y = g(x) \) 的图象关于 \( y \) 轴对称. 若 \( f(m) = -1 \)，则 \( m \) 的值是（\quad）

\choices
    {-e}
    {\(-\frac{1}{\e}\)}
    {c}
    {\(\frac{1}{\e}\)}
\end{problem}

\begin{problem}
设两个正态分布 \(N(\mu_1, \sigma_1^2) (\sigma_1 > 0)\) 和 \(N(\mu_2, \sigma_2^2) (\sigma_2 > 0)\) 的密度函数图象如图所示，则有（\quad）

\choices
    {\(\mu_{1} < \mu_{2}, \sigma_{1} < \sigma_{2}\)}
    {\(\mu_{1} < \mu_{2}, \sigma_{1} > \sigma_{2}\)}
    {\(\mu_{1} > \mu_{2}, \sigma_{1} < \sigma_{2}\)}
    {\(\mu_{1} > \mu_{2}, \sigma_{1} > \sigma_{2}\)}
\end{problem}

\begin{problem}
若函数 \( f(x), g(x) \) 分别是 \( \R \) 上的奇函数，偶函数，且满足 \( f(x) - g(x) = \e^{x} \)，则有（\quad）

\choices
    {\(f(2) < f(3) < g(0)\)}
    {\(g(0) < f(3) < f(2)\)}
    {\(f(2) < g(0) < f(3)\)}
    {\(g(0) < f(2) < f(3)\)}
\end{problem}

\begin{problem}
12 名同学合影，站成前排 4 人后排 8 人，现摄影师要从后排 8 人中抽 2 人调整到前排，若其他人的相对顺序不变，则不同调整方法的总数是（\quad）

\choices
    {\(\mathrm{C}_8^0\mathrm{A}_8^2\)}
    {\(\mathrm{C}_8^2\mathrm{A}_8^6\)}
    {\(\mathrm{C}_8^8\mathrm{A}_8^2\)}
    {\(\mathrm{C}_8^8\mathrm{A}_8^2\)}
\end{problem}

\section{填空题}

\begin{problem}
函数 \( f(x) = \frac{\sqrt{|x - 2| - 1}}{\log_2(x - 1)} \) 的定义域为 \fillinblank{} \fillinblank{}.
\end{problem}

\begin{problem}
在数列 \(\{a_{n}\}\) 中，\(a_{n} = 4n - \frac{5}{2}, a_{1} + a_{2} + \cdots + a_{n} = an^{2} + bn, n \in \mathbf{N}^{*}\)，其中 \(a_{n}\) 为常数，则 \(\lim_{n \to \infty} \frac{a_{n} - b^{2}}{a^{n} + b^{n}}\) 的值是 \fillinblank{} \fillinblank{}.
\end{problem}

\begin{problem}
若 \(A\) 为不等式组 \(\left\{ \begin{array}{l} x \leqslant 0, \\ y \geqslant 0, \\ y - x \leqslant 2 \end{array} \right.\) 表示的平面区域，则当 \(\mathbf{a}\) 从 \(-2\) 连续变化到 1 时，动直线 \(x + y = 0\) 过 \(A\) 中的那部分区域的面积为 \fillinblank{} \fillinblank{}.
\end{problem}

\begin{problem}
已知 \(A, B, C, D\) 在同一个球面上，\(AB \perp\) 平面 \(BCD, BC \perp CD\)，若 \(AB = 6, AC = 2\sqrt{13}, AD = 8\)，则 \(B, C\) 两点间的球面距离是 \fillinblank{} \fillinblank{}.
\end{problem}

\section{解答题}

\begin{problem}
已知函数 \( f(x) = \cos \left(2x - \frac{\pi}{3}\right) + 2\sin \left(x - \frac{\pi}{4}\right)\sin \left(x + \frac{\pi}{4}\right) \). (1) 求函数 \( f(x) \) 的最小正周期和图象的对称轴方程； (2) 求函数 \( f(x) \) 在区间 \( \left[-\frac{\pi}{12}, \frac{\pi}{2}\right] \) 上的值域.
\end{problem}

\begin{problem}
如图，在四棱锥 \(O - ABCD\) 中，底面 \(ABCD\) 四边长为 1 的菱形，\(\angle ABC = \frac{\pi}{4}\)，\(OA \perp\) 底面 \(ABCD\)，\(OA = 2\)，\(M\) 为 \(OA\) 的中点，\(N\) 为 \(BC\) 的中点. (1) 证明: 直线 \(MN //\) 平面 \(OCD\); (2) 求异面直线 \(AB\) 与 \(MD\) 所成角的大小; (3) 求点 B 到平面 OCD 的距离.
\end{problem}

\begin{problem}
为防止风沙危害，某地决定建设防护绿化带，种植杨树、沙柳等植物. 某人一次种植了 \(n\) 株沙柳，各株沙柳成活与否是相互独立的，成活率为 \(p\). 设 \(k\) 为成活沙柳的株数，数学期望 \(E_{k} = 3\)，标准差 \(\sigma_{k}^{2}\) 为 \(\frac{\sqrt{8}}{2}\). 求 (1) 求 \(n, p\) 的值，并写出 (5) 的分布列; (2) 若有 3 株或 3 株以上的沙柳未成活，则需要补种，求需要补种沙柳的概率.
\end{problem}

\begin{problem}
设函数 \( f(x) = \frac{1}{x + x^2} (x > 0 \text{且} x \neq 1) \). (1) 求函数 \( f(x) \) 的单调区间; (2) 已知 \(2^{5} > x^{n}\) 对任意 \(x \in (0,1)\). 成立，求实数 \(a\) 的取值范围.
\end{problem}

\begin{problem}
设数列 \(\{a_{n}\}\) 满足 \(a_0 = 0, a_{n+1} = \alpha a_n^2 + 1 - c, n \in \mathbf{N}^*\)，其中 \(c\) 为实数. (1) 证明: \(a_{n} \in [0,1]\) 对任意 \(n \in \mathbf{N}^{*}\) 成立的充分必要条件是 \(c \in [0,1]\); (2) 设 \(0 < c < \frac{1}{3}\). 证明: \(a_{n} \geqslant 1 - (3c)^{n-1}, n \in \mathbf{N}^{*}\); (3) 设 \(0 < c < \frac{1}{3}\). 证明: \(a_1^2 + a_2^2 + \cdots + a_n^2 > n + 1 - \frac{2}{1 - 3c}, n \in \mathbf{N}^*\)
\end{problem}

\begin{problem}
设椭圆 \(C: \frac{x^2}{a^2} + \frac{y^2}{b^2} = 1 (a > b > 0)\) 过点 \(M(\sqrt{2}, 1)\). 且左焦点为 \(F_1(-\sqrt{2}, 0)\). (1) 求椭圆 \(C\) 的方程; (2) 当过点 \(P(4,1)\) 的动直线 \(l\) 与椭圆 \(C\) 相交与两不同点 \(A, B\) 时，在线段 \(AB\) 上取点 \(Q\)，满足 \(\left|\overrightarrow{AP}\right| \cdot \left|\overrightarrow{QB}\right| = \left|\overrightarrow{AQ}\right| \cdot \left|\overrightarrow{PB}\right|\)，证点 \(Q\) 总在某定直线上.
\end{problem}

