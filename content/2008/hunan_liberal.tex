\chapter{2008年湖南卷（文）}

\section{单选题}

\begin{problem}
已知 \(U = \{2, 3, 4, 5, 6, 7\}\)，\(M = \{3, 4, 5, 7\}\)，\(N = \{2, 4, 5, 6\}\)，则（\quad）

\choices
    {\(M \cap N = \{4, 6\}\)}
    {\(M \cup N = U\)}
    {\((\R_U \cap) \cup M = U\)}
    {\((\R_U M) \cap N = N\)}
\end{problem}

\begin{problem}
\(\{x\mid -1 < 2^{x}\) 是 \(z < 3^{x}\) 的（\quad）

\choices
    {充分不必要条件}
    {必要不充分条件}
    {充分必要条件}
    {既不充分也不必要条件}
\end{problem}

\begin{problem}
已知变量 \(x, y\) 满足 \(\left\{ \begin{array}{l} x \geqslant 1, \\ y \leqslant 2, \\ x - y \leqslant 0, \end{array} \right.\) 则 \(x + y\) 的最小值是（\quad）

\choices
    {4}
    {3}
    {2}
    {1}
\end{problem}

\begin{problem}
函数 \( f(x) = x^{2} (x \leqslant 0) \) 的反函数是（\quad）

\choices
    {\(f^{-1}(x) = \sqrt{x} (x\geqslant 0)\)}
    {\(f^{-1}(x) = -\sqrt{x} (x\geqslant 0)\)}
    {\(f^{-1}(x) = -\sqrt{-x} (x\leqslant 0)\)}
    {\(f^{-1}(x) = -x^{2}(x\leqslant 0)\)}
\end{problem}

\begin{problem}
已知直线 \(m, n\) 和平面 \(\alpha, \beta\) 满足 \(m \perp n, m \perp \alpha, \alpha, \beta\)，则（\quad）

\choices
    {\(n \perp \beta\)}
    {\(n // \beta\)，或 \(n \subset \beta\)}
    {\(n \perp \alpha\)}
    {\(n // \alpha\)，或 \(n \subset \alpha\)}
\end{problem}

\begin{problem}
下面不等式成立的是（\quad）

\choices
    {\(\log_32 < \log_23 < \log_52\)}
    {\(\log_32 < \log_52 < \log_23\)}
    {\(\log_23 < \log_42 < \log_52\)}
    {\(\log_23 < \log_52 < \log_32\)}
\end{problem}

\begin{problem}
在 \(\triangle ABC\) 中，\(AB = 3\)，\(AC = 2\)，\(BC = \sqrt{10}\)，则 \(3\overrightarrow{AB} \cdot \overrightarrow{AC} =\)（\quad）

\choices
    {\(-\frac{3}{2}\)}
    {\(-\frac{2}{3}\)}
    {\(\frac{3}{5}\)}
    {\(\frac{3}{2}\)}
\end{problem}

\begin{problem}
某市拟从 4 个重点项目和 6 个一般项目中各选 2 个项目作为本年度启动的项目，则重点项目 A 和一般项目 B 至少有一个被选中的不同选法种数是（\quad）

\choices
    {15}
    {45}
    {60}
    {75}
\end{problem}

\begin{problem}
长方体 \(ABCD - A_{1}B_{1}C_{1}D_{1}\) 的 8 个顶点在同一个球面上，且 \(AB = 2\)，\(AD = \sqrt{3}, AA_{1} = 1\)，则顶点 \(A\)、\(B\) 间的球面距离是（\quad）

\choices
    {\(\frac{\sqrt{2}\pi}{4}\)}
    {\(\frac{\sqrt{2}\pi}{2}\)}
    {\(\sqrt[3]{2}\pi\)}
    {\(2\sqrt[3]{2}\pi\)}
\end{problem}

\begin{problem}
双曲线 \(\frac{x^2}{a^2} - \frac{y^2}{b^2} = 1 (a > 0, b > 0)\) 的右支上存在一点，它到右焦点及左准线的距离相等，则双曲线离心率的取值范围是（\quad）

\choices
    {\((1, \sqrt{2})\)}
    {\([\sqrt{2}, +\infty)\)}
    {\((1, \sqrt{2} + 1]\)}
    {\([\sqrt{2} + 1, +\infty)\)}
\end{problem}

\section{填空题}

\begin{problem}
已知向量 \( a = (1, \sqrt{3}) \) ，\( b = (-2, 0) \) ，则 \( |a + b| = \)  \fillinblank{}.
\end{problem}

\begin{problem}
从某地区 15000 位老人中随机抽取 500 人，其生活能否自理的情况如下表所示: 生活能否自理人性 男 女 能 178 278 不能 23 21 则该地区生活不能自理的老人中男性比女性约多人 \fillinblank{}.
\end{problem}

\begin{problem}
记 \(\left(\frac{2x + \frac{1}{x}}{n}\right)^2\) 的展开式中第 \(m\) 项的系数为 \(b_{m}\)，若 \(b_{3} = 2b_{4}\)，则 \(n =\)  \fillinblank{}.
\end{problem}

\begin{problem}
将圆 \(x^{2} + y^{2} = 1\) 沿 \(x\) 轴正向平移 1 个单位后得到圆 \(C\)，则圆 \(C\) 的方程是; 若过点 \((3,0)\) 的直线 \(l\) 和圆 \(C\) 相切，则直线 \(l\) 的斜率为 \fillinblank{} \fillinblank{}.
\end{problem}

\begin{problem}
设 \([x]\) 表示不超 \(x\) 的最大整数（如 \([2] = 2, \left[\frac{5}{3}\right] = 1\)）. 对于给定的 \(n \in \mathbf{N}^*\)，定义 \(C_n^n = \frac{n(n - 1) \cdots (n - |x| + 1)}{x(x - 1) \cdots (x - |x| + 1)}\)，\(x \in [1, +\infty)\)，则 \(C_8^3 = \) ；当 \(x \in [2, 3]\) 时，函数 \(C_8^n\) 的值域是 \fillinblank{} \fillinblank{}.
\end{problem}

\section{解答题}

\begin{problem}
甲、乙、丙三人参加了一家公司的招聘面试，面试合格者可正式签约. 甲表示只要面试合格就签约. 乙、丙则约定：两人面试都合格就一同签约. 否则两人都不签约. 设每人面试合格的概率都是 \(\frac{1}{2}\)，且面试是否合格互不影响. 求： (1) 至少有 1 人面试合格的概率; (2) 没有人签约的概率.
\end{problem}

\begin{problem}
已知函数 \( f(x) = \cos^2 \frac{x}{2} - \sin^2 \frac{x}{2} + \sin x \). (1) 求函数 \( f(x) \) 的最小正周期； (2) 当 \(x_0 \in \left(0, \frac{\pi}{4}\right)\) 且 \(f(x_0) = \frac{4\sqrt{2}}{5}\) 时，求 \(f\left(x_0 + \frac{\pi}{6}\right)\) 的值.
\end{problem}

\begin{problem}
如图所示，四棱锥 \(P-ABCD\) 的底面 \(ABCD\) 是边长为 1 的菱形，\(\angle BCD = 60^\circ\)，\(E\) 是 \(CD\) 的中点，\(PA \perp\) 底面 \(ABCD\)，\(PA = \sqrt{3}\). (1) 证明: 平面 \(PBE \perp\) 平面 \(PAB\); (2) 求二面角 \(A - BE - P\) 的大小.
\end{problem}

\begin{problem}
已知椭圆的中心在原点，一个焦点是 \(F(2,0)\)，且两条连线间的距离为 \(\lambda (\lambda > 4)\). (1) 求椭圆的方程; (2) 若存在过点 A(1,0) 的直线 l，使点 F 关于直线 l 的对称点在椭圆上，求 \( \lambda \) 的取值范围.
\end{problem}

\begin{problem}
数列 \(\{a_{n}\}\) 满足 \(a_1 = 1, a_2 = 2, a_{n+2} = \left(1 + \cos^2\frac{n\pi}{2}\right)a_n + \sin^2\frac{n\pi}{2}, n = 1\)， \(2,3,\dots\). (1) 求 \(a_{3}, a_{4}\)，并求数列 \(\{a_{n}\}\) 的通项公式; (2) 设 \(S_{k} = a_{1} + a_{3} + \dots + a_{2k-1}, T_{k} = a_{2} + a_{4} + \dots + a_{2k}, W_{k} = \frac{2S_{k}}{2 + T_{k}}\) \((k \in \mathbf{N}^{*})\)，求使 \(W_{k} > 1\) 的所有 \(k\) 的值，并说明理由.
\end{problem}

\begin{problem}
已知函数 \( f(x) = \frac{1}{4} x^4 + x^2 - \frac{9}{2} x^2 + cx \) 有三个极值点. (1) 证明： \( -27 < c^{2} < 5; \) (2) 若存在实数 \(c\)，使函数 \(f(x)\) 在区间 \([a, a + 2]\) 上单调递减，求 \(a\) 的取值范围.
\end{problem}

