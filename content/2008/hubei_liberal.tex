\chapter{2008年湖北卷（文）}

\section{单选题}

\begin{problem}
设 \(a = (1, -2)\)，\(b = (-3, 4)\)，\(c = (3, 2)\)，则 \((a + 2b) \cdot c =\)（\quad）

\choices
    {\((-15, 12)\)}
    {0}
    {\(-3\)}
    {\(-11\)}
\end{problem}

\begin{problem}
\(\left(2x^{3} - \frac{1}{2x^{2}}\right)^{10}\) 的展开式中常数项是（\quad）

\choices
    {210}
    {\(\frac{105}{2}\)}
    {\(\frac{1}{4}\)}
    {\(-105\)}
\end{problem}

\begin{problem}
若集合 \(P = \{1, 2, 3, 4\}\)，\(Q = \{x \mid 0 < x < 5, x \in \R\}\)，则（\quad）

\choices
    {“\(x\in P\)”是“\(x\in Q\)”的充分条件但不是必要条件}
    {“\(x\in P\)”是“\(x\in Q\)”的必要条件但不是充分条件}
    {“\(x\in P\)”是“\(x\in Q\)”的充要条件}
    {“\(x\in P\)”既不是“\(x\in Q\)”的充分条件也不是“\(x\in Q\)”的必要条件}
\end{problem}

\begin{problem}
用与球心距离为 1 的平面去截球，所得的截面面积为 \(\pi\)，则球的体积为（\quad）

\choices
    {\(\frac{8\pi}{3}\)}
    {\(\frac{8\sqrt{2}\pi}{3}\)}
    {\(8\sqrt{2}\pi\)}
    {\(\frac{32\pi}{3}\)}
\end{problem}

\begin{problem}
在平面直角坐标系 \(xOy\) 中，满足不等式组 \(\left\{ \begin{array}{l} |x| \leqslant |y|, \\ |x| < 1 \end{array} \right.\) 的点 \((x, y)\) 的集合用阴影表示为下列图中的（\quad）

\bitmapfigure[trim=0 0 0 0.18cm,clip,max width=0.88\linewidth,max totalheight=6.8cm]{img/2008/hubei_liberal/q5_options.png}
\end{problem}

\begin{problem}
已知 \( f(x) \) 在 \( \R \) 上是奇函数，且 \( f(x + 4) = f(x) \)，当 \( x \in (0,2) \) 时，\( f(x) = 2x^2 \)，则 \( f(7) = \)（\quad）

\choices
    {\(-2\)}
    {2}
    {\(-98\)}
    {98}
\end{problem}

\begin{problem}
将函数 \( y = \sin (x - \theta) \) 的图象 \(F\) 向右平移 \( \frac{\pi}{3} \) 个单位长度得到图象 \(F'\). 若 \(F'\) 的一条对称轴是直线 \( x = \frac{\pi}{4} \)，则 \( \theta \) 的一个可能取值是（\quad）

\choices
    {\( \frac{5}{12}\pi \)}
    {\( -\frac{5}{12}\pi \)}
    {\( \frac{11}{12}\pi \)}
    {\( -\frac{11}{12}\pi \)}
\end{problem}

\begin{problem}
函数 \( f(x) = \frac{1}{x} \ln (\sqrt{x^2 - 3x + 2} + \sqrt{-x^2 - 3x + 4}) \) 的定义域为（\quad）

\choices
    {\( (-\infty,-4]\cup[2,+\infty) \)}
    {\( (-4,0)\cup(0,1) \)}
    {\( [-4,0)\cup(0,1] \)}
    {\( [-4,0)\cup(0,1) \)}
\end{problem}

\begin{problem}
从 3 名男生和 5 名女生中选 3 人组队参加某集体项目的比赛，其中至少有一名女生入选的组队方案数为（\quad）

\choices
    {100}
    {110}
    {120}
    {180}
\end{problem}

\begin{problem}
如图所示，“嫦娥一号” 探月卫星沿地月转移轨道飞向月球. 在月球附近一点 P 轨进入以月球圆心 F 为一个焦点的椭圆轨道 I 绕月飞行，之后卫星在 P 点第二次变轨进入仍以 F 为一个焦点的椭圆轨道 II 绕月飞行，最终卫星在 P 点第三次变轨进入以 F 为圆心的圆形轨道 III 绕月飞行，若用 \( 2c_{1} \) 和 \( 2c_{2} \) 分别表示椭圆轨道 I 和 II 的焦距. 用 \( 2a_{1} \) 和 \( 2a_{2} \) 分别表示椭圆轨道 I 和 II 的长轴的长，给出下列式子：

\circled{1} \( a_{1} + c_{1} = a_{2} + c_{2} \) ；\circled{2} \( a_{1} - c_{1} = a_{2} - c_{2} \) ；\circled{3} \( c_{1}a_{2} > a_{1}c_{2} \) ；\circled{4} \( \frac{c_{1}}{a_{1}} < \frac{c_{2}}{a_{2}} \) .

其中正确式子的序号是（\quad）

\bitmapfigure[max width=0.34\linewidth,max totalheight=4.8cm]{img/2008/hubei_liberal/q10_fig1.png}

\choices
    {\circled{1}\circled{3}}
    {\circled{2}\circled{3}}
    {\circled{1}\circled{4}}
    {\circled{2}\circled{4}}
\end{problem}

\section{填空题}

\begin{problem}
一个公司共有 1000 名员工，下设一些部门. 要采用分层抽样方法从全体员工中抽取一个容量为 50 的样本，已知某部门有 200 名员工，那么从该部门抽取的工人数是\fillinblank{}.
\end{problem}

\begin{problem}
在 \(\triangle ABC\) 中，\(a\)，\(b\)，\(c\) 分别是角 \(A\)，\(B\)，\(C\) 所对的边，已知 \(a = \sqrt{3}\)，\(b = 3\)，\(C = 30^{\circ}\)，则 \(A = \)\fillinblank{}.
\end{problem}

\begin{problem}
方程 \(2^{x} + x^{2} = 3\) 的实数解的个数为\fillinblank{}.
\end{problem}

\begin{problem}
明天上午李明要参加奥运志愿者活动，为了准时起床，他用甲，乙两个闹钟叫醒自己. 假设甲闹钟准时响的概率是 0.80，乙闹钟准时响的概率是 0.90，则两个闹钟至少有一准时响的概率是\fillinblank{}.
\end{problem}

\begin{problem}
圆 \(C: \left\{ \begin{array}{l} x = 3 + 4\cos \theta, \\ y = -2 + 4\sin \theta. \end{array} \right.\) （\(\theta\) 为参数） 的圆心坐标为\fillinblank{}，和圆 \(C\) 关于直线 \(x - y = 0\) 对称的圆 \(C'\) 的普通方程是\fillinblank{}.
\end{problem}

\section{解答题}

\begin{problem}
已知函数 \( f(x) = \sin \frac{x}{2} \cos \frac{x}{2} + \cos^2 \frac{x}{2} - 2 \).
\begin{enumerate}
\item 将函数 \( f(x) \) 化简成 \( A\sin(\omega x + \varphi) + B (A > 0, \omega > 0, \varphi \in [0, 2\pi)) \) 的形式，并指出 \( f(x) \) 的周期；
\item 求函数 \( f(x) \) 在 \( \left[\pi, \frac{17\pi}{12}\right] \) 上的最大值和最小值.
\end{enumerate}
\end{problem}

\begin{problem}
已知函数 \( f(x) = x^3 + mx^2 - m^2x + 1 \) （ \( m \) 为常数，且 \( m > 0 \)） 有极大值 9.
\begin{enumerate}
\item 求 \( m \) 的值；
\item 若斜率为 \(-5\) 的直线是曲线 \( y = f(x) \) 的切线，求此直线方程.
\end{enumerate}
\end{problem}

\begin{problem}
如图，在直三棱柱 \(ABC - A_{1}B_{1}C_{1}\) 中，平面 \(A_{1}BC\) \(\perp\) 侧面 \(A_{1}ABB_{1}\).
\begin{enumerate}
\item 求证： \(AB \perp BC\)；
\item 若 \(AA_{1} = AC = a\)，直线 \(AC\) 与平面 \(A_{1}BC\) 所成的角为 \(\theta\)，二面角 \(A_{1} - BC - A\) 的大小为 \(\varphi\)，求证：\(\theta + \varphi = \frac{\pi}{2}\).
\end{enumerate}

\bitmapfigure[max width=0.34\linewidth,max totalheight=4.8cm]{img/2008/hubei_liberal/q18_fig1.png}
\end{problem}

\begin{problem}
如图，要设计一张矩形广告，该广告含有大小相等的左右两个矩形栏目（即图中阴影部分），这两栏的面积之和为 \(18000\mathrm{cm}^2\) ，四周空白的宽度为 \(10\mathrm{cm}\)，两栏之间的中缝空白的宽度为 \(5\mathrm{cm}\) ，怎样确定广告的高与宽的尺寸（单位：\(\mathrm{cm}\)），能使矩形广告面积最小？

\bitmapfigure[max width=0.55\linewidth,max totalheight=4.0cm]{img/2008/hubei_liberal/q19_fig1.png}
\end{problem}

\begin{problem}
已知双曲线 \(C: \frac{x^2}{a^2} - \frac{y^2}{b^2} = 1 (a > 0, b > 0)\) 的两个焦点为 \(F_1(-2,0)\)，\(F_2(2,0)\)，点 \(P(3, \sqrt{5})\) 在双曲线 \(C\) 上.
\begin{enumerate}
\item 求双曲线 C 的方程；
\item 记 O 为坐标原点，过点 \( Q(0,2) \) 的直线 l 与双曲线 C 相交于不同的两点 E，F，若 \( \triangle OEF \) 的面积为 \( 2\sqrt{2} \) ，求直线 l 的方程.
\end{enumerate}
\end{problem}

\begin{problem}
已知数列 \(\{a_{n}\}\) 和 \(\{b_{n}\}\) 满足： \(a_1 = \lambda\)，\(a_{n + 1} = \frac{2}{3} a_n + n - 4\)，\(b_n = (-1)^n (a_n - 3n + 21)\)，其中 \(\lambda\) 为实数，\(n\) 为正整数.
\begin{enumerate}
\item 证明：对任意实数 \(\lambda\)，数列 \(\{a_{n}\}\) 不是等比数列；
\item 证明：当 \(\lambda \neq -18\) 时，数列 \(\{b_n\}\) 是等比数列； （2） 设 \(S_{n}\) 为数列 \(\{b_{n}\}\) 的前 \(n\) 项和，是否存在实数 \(\lambda\)，使得对任意正整数 \(n\)，都有 \(S_{n} > -12\)? 若存在，求 \(\lambda\) 的取值范围；若不存在，说明理由.
\end{enumerate}
\end{problem}
