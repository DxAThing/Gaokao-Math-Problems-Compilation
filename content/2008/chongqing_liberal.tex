\chapter{2008年重庆卷（文）}

\section{单选题}

\begin{problem}
已知 \(\{a_{n}\}\) 为等差数列，\(a_{2} + a_{8} = 12\)，则 \(a_{5}\) 等于（\quad）

\choices
    {4}
    {5}
    {6}
    {7}
\end{problem}

\begin{problem}
设 \(x\) 是实数，则 \(x^2 > 0\) 是“\(|x| > 0\)”的（\quad）

\choices
    {充分而不必要条件}
    {必要而不充分条件}
    {充要条件}
    {既不充分也不必要条件}
\end{problem}

\begin{problem}
曲线 C: \( \left\{\begin{aligned} x &= \cos\theta - 1, \\ y &= \sin\theta + 1, \end{aligned}\right. \) ( \( \theta \) 为参数) 的普通方程为（\quad）

\choices
    {\((x - 1)^2 + (y + 1)^2 = 1\)}
    {\((x + 1)^{2} + (y + 1)^{2} = 1\)}
    {\((x + 1)^{2} + (y - 1)^{2} = 1\)}
    {\((x - 1)^{2} + (y - 1)^{2} = 1\)}
\end{problem}

\begin{problem}
若点 \(P\) 分有向线段 \(\overrightarrow{AB}\) 所成的比为 \(-\frac{1}{3}\)，则点 \(B\) 分有向线段 \(\overrightarrow{PA}\) 所成的比是（\quad）

\choices
    {\(-\frac{3}{2}\)}
    {\(-\frac{1}{2}\)}
    {\(\frac{1}{2}\)}
    {3}
\end{problem}

\begin{problem}
某校高二年级有男生 500 人，女生 400 人. 为了解该年级学生的健康情况，从男生中任意抽取 25 人，从女生中任意抽取 20 人进行调查. 这种抽样方法是（\quad）

\choices
    {简单随机抽样法}
    {抽签法}
    {随机数表法}
    {分层抽样法}
\end{problem}

\begin{problem}
函数 \( y = 10^{x^{2} - 1} \) (\( 0 < x \leqslant 1 \)) 的反函数是（\quad）

\choices
    {\(y = -\sqrt{1 + \lg x}\left(x > \frac{1}{10}\right)\)}
    {\( y = \sqrt{1 + \lg x}\left(x > \frac{1}{10}\right) \)}
    {\( y = -\sqrt{1 + \lg x}\left(\frac{1}{10} < x \leqslant 1\right) \)}
    {\( y = \sqrt{1 + \lg x}\left(\frac{1}{10} < x \leqslant 1\right) \)}
\end{problem}

\begin{problem}
函数 \( f(x) = \frac{\sqrt{x}}{x + 1} \) 的最大值为（\quad）

\choices
    {\( \frac{2}{5} \)}
    {\( \frac{1}{2} \)}
    {\( \frac{\sqrt{2}}{2} \)}
    {1}
\end{problem}

\begin{problem}
若双曲线 \(\frac{x^2}{3} - \frac{16y^2}{p^2} = 1\) 的左焦点在抛物线 \(y^2 = 2px\) 的准线上，则 \(p\) 的值为（\quad）

\choices
    {2}
    {3}
    {4}
    {\(4 \sqrt{2}\)}
\end{problem}

\begin{problem}
从编号为 1，2，…，10 的 10 个大小相同的球中任取 4 个，则所取 4 个球的最大号码是 6 的概率为（\quad）

\choices
    {\( \frac{1}{84} \)}
    {\( \frac{1}{21} \)}
    {\( \frac{2}{5} \)}
    {\( \frac{3}{5} \)}
\end{problem}

\begin{problem}
若 \( \left(x+\frac{1}{2x}\right)^{n} \) 的展开式中前三项的系数成等差数列，则展开式中 \( x^{4} \) 项的系数为（\quad）

\choices
    {6}
    {7}
    {8}
    {9}
\end{problem}

\begin{problem}
如图，模块\(\circled{1}\sim\circled{5}\)均由 4 个棱长为 1 的小正方体构成，模块\circled{6}由 15 个棱长为 1 的小正方体构成. 现从模块\(\circled{1}\sim\circled{5}\)中选出三个放到模块\circled{6}上，使得模块\circled{6}成为一个棱长为 3 的大正方体. 则下列选择方案中，能够完成任务的为（\quad）

\bitmapfigure[max width=0.82\linewidth,max totalheight=5.2cm]{img/2008/chongqing_liberal/q11_fig1.png}

\choices
    {模块\circled{1}\circled{2}\circled{5}}
    {模块\circled{1}\circled{3}\circled{5}}
    {模块\circled{2}\circled{4}\circled{6}}
    {模块\circled{3}\circled{4}\circled{5}}
\end{problem}

\begin{problem}
函数 \( f(x) = \frac{\sin x}{\sqrt{5 + 4\cos x}} (0 \leqslant x \leqslant 2\pi) \) 的值域是（\quad）

\choices
    {\( \left[-\frac{1}{4},\frac{1}{4}\right] \)}
    {\( \left[-\frac{1}{3},\frac{1}{3}\right] \)}
    {\( \left[-\frac{1}{2},\frac{1}{2}\right] \)}
    {\(\left[-\frac{2}{3},\frac{2}{3}\right]\)}
\end{problem}

\section{填空题}

\begin{problem}
设集合 \(U = \{1, 2, 3, 4, 5\}\)，\(A = \{2, 3, 4\}\)，\(B = \{4, 5\}\)，则 \(A \cap (\complement_U B) =\)  \fillinblank{}.
\end{problem}

\begin{problem}
若 \(x > 0\)，则 \(\left(2x^{\frac{1}{4}} + 3^{\frac{3}{2}}\right)\left(2x^{\frac{1}{4}} - 3^{\frac{3}{2}}\right) - 4x^{-\frac{1}{2}}\left(x - x^{\frac{1}{2}}\right) =\)  \fillinblank{}.
\end{problem}

\begin{problem}
已知圆 \(C: x^{2} + y^{2} + 2x + ay - 3 = 0\) ( \(a\) 为实数) 上任意一点关于直线 \(l: x - y + 2 = 0\) 的对称点都在圆 \(C\) 上，则 \(a = \)  \fillinblank{}.
\end{problem}

\begin{problem}
某人有 3 种颜色的灯泡（每种颜色的灯泡足够多），要在如图所示的 6 个点
A、B、C、A\(_1\)、B\(_1\)、C\(_1\) 上各装一个灯泡，要求同一条线段两端的灯泡不同色，

\bitmapfigure[max width=0.45\linewidth,max totalheight=4.8cm]{img/2008/chongqing_liberal/q16_fig1.png}

则不同的安装方法共有 \fillinblank{} 种. （用数字作答）
\end{problem}

\section{解答题}

\begin{problem}
设 \(\triangle ABC\) 的内角 \(A, B, C\) 的对边分别为 \(a, b, c\). 已知 \(b^2 + c^2 = a^2 + \sqrt{3}bc\)，求:
\begin{enumerate}
\item \(A\) 的大小;
\item \(2\sin B\cos C - \sin (B - C)\) 的值.
\end{enumerate}
\end{problem}

\begin{problem}
在每道单项选择题给出的 4 个备选答案中，只有一个是正确的. 若对 4 道选择题中的每一道都任意选定一个答案，求这 4 道题中:
\begin{enumerate}
\item 恰有两道题答对的概率;
\item 至少答对一道题的概率.
\end{enumerate}
\end{problem}

\begin{problem}
设函数 \( f(x) = x^{3} + ax^{2} - 9x - 1 (a < 0) \). 若曲线 \( y = f(x) \) 的斜率最小的切线与直线 \( 12x + y = 6 \) 平行，求:
\begin{enumerate}
\item \(a\) 的值;
\item 函数 \( f(x) \) 的单调区间.
\end{enumerate}
\end{problem}

\begin{problem}
如图，\(\alpha\) 和 \(\beta\) 为平面，\(\alpha \cap \beta = l, A \in \alpha, B \in \beta, AB = 5, A, B\) 在棱 \(l\) 上的射影分别为 \(A', B', AA' = 3, BB' = 2\). 若二面角 \(\alpha - l - \beta\) 的大小为 \(\frac{2\pi}{3}\)，求:

\bitmapfigure[max width=0.54\linewidth,max totalheight=4.8cm]{img/2008/chongqing_liberal/q20_fig1.png}

\begin{enumerate}
\item 点 B 到平面 \( \alpha \) 的距离;
\item 异面直线 l 与 AB 所成的角. (用反三角函数表示)
\end{enumerate}
\end{problem}

\begin{problem}
如图，\(M(-2,0)\) 和 \(N(2,0)\) 是平面上的两点，动点 \(P\) 满足: \(\left||PM|-|PN|\right| = 2\).

\bitmapfigure[max width=0.46\linewidth,max totalheight=4.8cm]{img/2008/chongqing_liberal/q21_fig1.png}

\begin{enumerate}
\item 求点 \(P\) 的轨迹方程;
\item 设 \(d\) 为点 \(P\) 到直线 \(l: x = \frac{1}{2}\) 的距离，若 \(|PM| = 2|PN|^2\)，求 \(\frac{|PM|}{d}\) 的值.
\end{enumerate}
\end{problem}

\begin{problem}
设各项均为正数的数列 \(\{a_{n}\}\) 满足 \(a_1 = 2, a_n = a_{n+1}^{\frac{3}{2}} a_{n+2} (n \in \mathbf{N}^*)\).
\begin{enumerate}
\item 若 \(a_2 = \frac{1}{4}\)，求 \(a_3, a_4\)，并猜想 \(a_{2008}\) 的值 (不需证明);
\item 若 \(2\sqrt{2} \leqslant a_1a_2 \cdots a_n < 4\) 对 \(n \geqslant 2\) 恒成立，求 \(a_2\) 的值.
\end{enumerate}
\end{problem}
