\chapter{2015年重庆卷（理）}

\section{单选题}

\begin{problem}
已知集合 \(A = \{1, 2, 3\}\)，\(B = \{2, 3\}\)，则（\quad）

\choices
    {\(A = B\)}
    {\(A \cap B = \varnothing\)}
    {\(A \not\subset B\)}
    {\(B \not\subset A\)}
\end{problem}

\begin{problem}
在等差数列 \(\{a_{n}\}\) 中，若 \(a_{2} = 4, a_{4} = 2\)，则 \(a_{6} =\)（\quad）

\choices
    {\(-1\)}
    {0}
    {1}
    {6}
\end{problem}

\begin{problem}
重庆市 2013 年各月的平均气温(\(^{\circ}\mathrm{C}\)) 数据的茎叶图如下:

\bitmapfigure[max width=0.25\linewidth,max totalheight=4.8cm]{img/2015/chongqing_science/q3_fig1.png}

\choices
    {19}
    {20}
    {21.5}
    {23}
\end{problem}

\begin{problem}
“x > 1”是“ \( \log_{\frac{1}{2}}(x + 2) < 0 \) ”的（\quad）

\choices
    {充要条件}
    {充分不必要条件}
    {必要而不充分条件}
    {既不充分也不必要条件}
\end{problem}

\begin{problem}
某几何体的三视图如图所示，则该几何体的体积为（\quad）

\bitmapfigure[max width=0.42\linewidth,max totalheight=4.8cm]{img/2015/chongqing_science/q5_fig1.png}

\choices
    {\(\frac{1}{3} + \pi\)}
    {\(\frac{2}{3} + \pi\)}
    {\(\frac{1}{3} + 2\pi\)}
    {\(\frac{2}{3} + 2\pi\)}
\end{problem}

\begin{problem}
若非零向量 \(a, b\) 满足 \( \left|a\right|=\frac{2\sqrt{2}}{3}\left|b\right| \)，且 \( (a-b)\perp(3a+2b) \)，则 \(a\) 与 \(b\) 的夹角为（\quad）

\choices
    {\( \frac{\pi}{4} \)}
    {\( \frac{\pi}{2} \)}
    {\( \frac{3\pi}{4} \)}
    {\(\pi\)}
\end{problem}

\begin{problem}
执行如图所示的程序框图，若输出 k 的值为 8，则判断框内可填入的条件是（\quad）

\bitmapfigure[max width=0.38\linewidth,max totalheight=4.8cm]{img/2015/chongqing_science/q7_fig1.png}

\choices
    {\(s \leqslant \frac{3}{4}\)}
    {\(s \leqslant \frac{5}{6}\)}
    {\(s \leqslant \frac{11}{12}\)}
    {\(s \leqslant \frac{25}{24}\)}
\end{problem}

\begin{problem}
已知直线 \(t: x + ay - 1 = 0 (a \in \R)\) 是圆 \(C: x^2 + y^2 - 4x - 2y + 1 = 0\) 的对称轴. 过点 \(A(-4, a)\) 作圆 \(C\) 的一条切线. 切点为 \(B\)，则 \(|AB| = \fillinblank{}\)（\quad）

\choices
    {2}
    {\(4\sqrt{2}\)}
    {6}
    {\(2\sqrt{10}\)}
\end{problem}

\begin{problem}
若 \(\tan \alpha = 2\tan \frac{\pi}{5}\)，则 \(\frac{\cos\left(\alpha - \frac{3\pi}{10}\right)}{\sin\left(\alpha - \frac{\pi}{5}\right)} =\)（\quad）

\choices
    {1}
    {2}
    {3}
    {4}
\end{problem}

\begin{problem}
设双曲线 \(\frac{x^2}{a^2} - \frac{y^2}{b^2} = 1 (a > 0, b > 0)\) 的右焦点为 \(F\)，右顶点为 \(A\)，过 \(F\) 作 \(AF\) 的垂线与双曲线交于 \(B, C\) 两点，过 \(B, C\) 分别作 \(AC, AB\) 的垂线，两垂线交于点 \(D\). 若 \(D\) 到直线 \(BC\) 的距离小于 \(a + \sqrt{a^2 + b^2}\)，则该双曲线的渐近线斜率的取值范围是（\quad）

\choices
    {\((-1,0)\cup (0,1)\)}
    {\((-\infty, -1) \cup (1, +\infty)\)}
    {\((-\sqrt{2}, 0) \cup (0, \sqrt{2})\)}
    {\((-\infty, -\sqrt{2}) \cup (\sqrt{2}, +\infty)\)}
\end{problem}

\section{填空题}

\begin{problem}
设复数 \(a + bi\) （\(a, b \in \R\)）的模为 \(\sqrt{3}\)，则 \((a + bi)(a - bi) = \)  \fillinblank{}.
\end{problem}

\begin{problem}
\(\left(x^{3} + \frac{1}{2\sqrt{x}}\right)^{5}\) 的展开式中 \(x^{8}\) 的系数是 \fillinblank{}. (用数字作答)
\end{problem}

\begin{problem}
在 \(\triangle ABC\) 中，\(B = 120^{\circ}\)，\(AB = 2\)，\(A\) 的角平分线 \(AD = 3\)，则 \(AC = \)  \fillinblank{}.
\end{problem}

\begin{problem}
如图，圆 \(O\) 的弦 \(AB\)，\(CD\) 相交于点 \(E\)，过点 \(A\) 作圆 \(O\) 的切线与 \(DC\) 的延长线交于点 \(P\)，若 \(PA = 6\)，\(AE = 9\)，\(PC = 3\)，\(CE:ED = 2:1\)，则 \(BE = \)  \fillinblank{}.

\bitmapfigure[max width=0.32\linewidth,max totalheight=4.8cm]{img/2015/chongqing_science/q14_fig1.png}
\end{problem}

\begin{problem}
已知直线 \(l\) 的参数方程为 \(\left\{ \begin{array}{l} x = -1 + t, \\ y = 1 + t, \end{array} \right.\) （\(t\) 为参数），以坐标原点为极点，\(x\) 轴的正半轴为极轴建立极坐标系，曲线 \(C\) 的极坐标方程为 \(\rho^2 \cos 2\theta = 4\rho\) （\(\rho > 0, \frac{3\pi}{4} < \theta < \frac{5\pi}{4}\)），则直线 \(l\) 与曲线 \(C\) 的交点的极坐标为 \fillinblank{} \fillinblank{}.
\end{problem}

\begin{problem}
若函数 \( f(x) = |x + 1| + 2|x - a| \) 的最小值为 5，则实数 \( a = \)  \fillinblank{}.
\end{problem}

\section{解答题}

\begin{problem}
端午节吃粽子是我国的传统习俗. 设一盘中装有 10 个粽子，其中豆沙粽 2 个，肉粽 3 个，白粽 5 个，这三种粽子的外观完全相同，从中任意选取 3 个.

\begin{enumerate}
\item 求三种粽子各取到 1 个的概率；
\item 设 X 表示取到的豆沙粽个数，求 X 的分布列与数学期望.
\end{enumerate}
\end{problem}

\begin{problem}
已知函数 \( f(x) = \sin \left( \frac{\pi}{2} - x \right) \sin x - \sqrt{3} \cos^2 x \).

\begin{enumerate}
\item 求 \( f(x) \) 的最小正周期和最大值;
\item 讨论 \( f(x) \) 在 \( \left[\frac{\pi}{6}, \frac{2\pi}{3}\right] \) 上的单调性.
\end{enumerate}
\end{problem}

\begin{problem}
如图，三棱锥 \(P - ABC\) 中，\(PC \perp\) 平面 \(ABC\)，\(PC = 3\)，\(\angle ACB = \frac{\pi}{2}\)，\(D\)，\(E\) 分别为线段 \(AB\)，\(BC\) 上的点，且 \(CD = DE = \sqrt{2}\)，\(CE = 2EB = 2\).

\bitmapfigure[max width=0.34\linewidth,max totalheight=4.8cm]{img/2015/chongqing_science/q19_fig1.png}

\begin{enumerate}
\item 证明: \(DE \perp\) 平面 \(PCD\);
\item 求二面角 \(A - PD - C\) 的余弦值.
\end{enumerate}
\end{problem}

\begin{problem}
如图，椭圆 \(\frac{x^2}{a^2} + \frac{y^2}{b^2} = 1 (a > b > 0)\) 的左、右焦点分别为 \(F_1, F_2\)，过 \(F_2\) 的直线交椭圆于 \(P, Q\) 两点，且 \(PQ \perp PF_1\).

\bitmapfigure[max width=0.34\linewidth,max totalheight=4.8cm]{img/2015/chongqing_science/q21_fig1.png}

\begin{enumerate}
\item 若 \( |PF_1| = 2 + \sqrt{2} \)，\( |PF_2| = 2 - \sqrt{2} \)，求椭圆标准方程;
\item 若 \( |PF_1| = |PQ| \)，求椭圆的离心率 \( e \).
\end{enumerate}
\end{problem}

\begin{problem}
在数列 \(\{a_{n}\}\) 中，\(a_{1} = 3\)，\(a_{n + 1}a_{n} + \lambda a_{n + 1} + \mu a_{n}^{2} = 0 (n \in \mathbf{N}_{+})\).

\begin{enumerate}
\item 若 \(\lambda = 0, \mu = -2\)，求数列 \(\{a_n\}\) 的通项公式；
\item 若 \(\lambda = \frac{1}{k_0} (k_0 \in \mathbf{N}_+, k_0 \geqslant 2)\)，\(\mu = -1\)，证明: \(2 + \frac{1}{3k_0 + 1} < a_{k_0 + 1} < 2 + \frac{1}{2k_0 + 1}\).
\end{enumerate}
\end{problem}

\begin{problem}
设函数 \( f(x) = \frac{3x^2 + ax}{e^x} (a \in \R) \).

\begin{enumerate}
\item 若 \( f(x) \) 在 \( x = 0 \) 处取得极值，确定 \( a \) 的值，并求此时曲线 \( y = f(x) \) 在点 \((1, f(1))\) 处的切线方程;
\item 若 \( f(x) \) 在 \([3, +\infty)\) 上为减函数，求 \( a \) 的取值范围.
\end{enumerate}
\end{problem}
