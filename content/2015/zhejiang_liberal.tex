\chapter{2015年浙江卷（文）}

\section{单选题}

\begin{problem}
已知集合 \(P=\{x\mid x^2-2x\geqslant3\}\)，\(Q=\{x\mid 2<x<4\}\)，则 \(P\cap Q=\)（\quad）

\choices
    {\([3,4)\)}
    {\((2,3]\)}
    {\((-1,2)\)}
    {\((-1,3]\)}
\end{problem}

\begin{problem}
某几何体的三视图如图所示（单位：cm），则该几何体的体积是（\quad）

\bitmapfigure[max width=0.60\linewidth,max totalheight=5.2cm]{img/2015/zhejiang_liberal/q2_fig1.png}

\choices
    {\(8\mathrm{cm}^3\)}
    {\(12\mathrm{cm}^3\)}
    {\(\frac{32}{3}\mathrm{cm}^3\)}
    {\(\frac{40}{3}\mathrm{cm}^3\)}
\end{problem}

\begin{problem}
设 \(a,b\) 是实数，则“\(a+b>0\)”是“\(ab>0\)”的（\quad）

\choices
    {充分不必要条件}
    {必要不充分条件}
    {充分必要条件}
    {既不充分也不必要条件}
\end{problem}

\begin{problem}
设 \(\alpha,\beta\) 是两个不同的平面，\(l,m\) 是两条不同的直线，且 \(l \subset \alpha,m \subset \beta\)（\quad）

\choices
    {若 \(l \perp \beta\)，则 \(\alpha \perp \beta\)}
    {若 \(\alpha \perp \beta\)，则 \(l \perp m\)}
    {若 \(l \parallel \beta\)，则 \(\alpha \parallel \beta\)}
    {若 \(\alpha \parallel \beta\)，则 \(l \parallel m\)}
\end{problem}

\begin{problem}
函数 \(f(x)=\left(x-\frac{1}{x}\right)\cos x\left(-\pi\leqslant x\leqslant \pi\text{且}\,x\neq0\right)\) 的图象可能为（\quad）

\choices
    {\bitmapinlinefigure[0.18\linewidth][max width=0.18\linewidth,max totalheight=2.2cm]{img/2015/zhejiang_liberal/q5_opt_a.png}}
    {\bitmapinlinefigure[0.18\linewidth][max width=0.18\linewidth,max totalheight=2.2cm]{img/2015/zhejiang_liberal/q5_opt_b.png}}
    {\bitmapinlinefigure[0.18\linewidth][max width=0.18\linewidth,max totalheight=2.2cm]{img/2015/zhejiang_liberal/q5_opt_c.png}}
    {\bitmapinlinefigure[0.18\linewidth][max width=0.18\linewidth,max totalheight=2.2cm]{img/2015/zhejiang_liberal/q5_opt_d.png}}
\end{problem}

\begin{problem}
有三个房间需要粉刷，粉刷方案要求：每个房间只用一种颜色，且三个房间颜色各不相同。已知三个房间的粉刷面积（单位：\(\mathrm{m}^2\)）分别为 \(x,y,z\)，且 \(x<y<z\)，三种颜色涂料的粉刷费用（单位：元/\(\mathrm{m}^2\)）分别为 \(a,b,c\)，且 \(a<b<c\)。在不同的方案中，最低的总费用（单位：元）是（\quad）

\choices
    {\(ax+by+cz\)}
    {\(az+by+cx\)}
    {\(ay+bz+cx\)}
    {\(ay+bx+cz\)}
\end{problem}

\begin{problem}
如图，斜线段 \(AB\) 与平面 \(\alpha\) 所成的角为 \(60^\circ\)，\(B\) 为斜足，平面 \(\alpha\) 上的动点 \(P\) 满足 \(\angle PAB=30^\circ\)，则点 \(P\) 的轨迹是（\quad）

\bitmapfigure[max width=0.52\linewidth,max totalheight=4.8cm]{img/2015/zhejiang_liberal/q7_fig1.png}

\choices
    {直线}
    {抛物线}
    {椭圆}
    {双曲线的一支}
\end{problem}

\begin{problem}
设实数 \(a,b,t\) 满足 \(|a+1|=|\sin b|=t\)。则下列说法中正确的是（\quad）

\choices
    {若 \(t\) 确定，则 \(b^2\) 唯一确定}
    {若 \(t\) 确定，则 \(a^2+2a\) 唯一确定}
    {若 \(t\) 确定，则 \(\sin\frac{b}{2}\) 唯一确定}
    {若 \(t\) 确定，则 \(a^2+a\) 唯一确定}
\end{problem}

\section{填空题}

\begin{problem}
计算 \(\log_2\frac{\sqrt{2}}{2}=\)\fillinblank{}，\(2^{\log_2 3+\log_4 3}=\)\fillinblank{}。
\end{problem}

\begin{problem}
已知 \(\{a_n\}\) 是等差数列，公差 \(d\) 不为零。若 \(a_2,a_3,a_7\) 成等比数列，且 \(2a_1+a_2=1\)，则 \(a_1=\)\fillinblank{}，\(d=\)\fillinblank{}。
\end{problem}

\begin{problem}
函数 \(f(x)=\sin^2x+\sin x\cos x+1\) 的最小正周期是\fillinblank{}，最小值是\fillinblank{}。
\end{problem}

\begin{problem}
已知函数 \(f(x)=\begin{cases}x^2,&x\leqslant1,\\ x+\dfrac{6}{x}-6,&x>1.\end{cases}\) 则 \(f(f(-2))=\)\fillinblank{}，\(f(x)\) 的最小值是\fillinblank{}。
\end{problem}

\begin{problem}
已知 \(e_1,e_2\) 是平面单位向量，且 \(e_1\cdot e_2=\frac{1}{2}\)。若平面向量 \(\pmb{b}\) 满足 \(\pmb{b}\cdot e_1=\pmb{b}\cdot e_2=1\)，则 \(\lvert\pmb{b}\rvert=\)\fillinblank{}。
\end{problem}

\begin{problem}
已知实数 \(x,y\) 满足 \(x^2+y^2\leqslant1\)，则 \(|2x+y-4|+|6-x-3y|\) 的最大值是\fillinblank{}。
\end{problem}

\begin{problem}
椭圆 \(\frac{x^2}{a^2}+\frac{y^2}{b^2}=1\,(a>b>0)\) 的右焦点 \(F(c,0)\) 关于直线 \(y=\frac{b}{c}x\) 的对称点 \(Q\) 在椭圆上，则椭圆的离心率是\fillinblank{}。
\end{problem}

\section{解答题}

\begin{problem}
在 \(\triangle ABC\) 中，内角 \(A,B,C\) 所对的边分别为 \(a,b,c\)。已知 \(\tan\left(\frac{\pi}{4}+A\right)=2\)。

\begin{enumerate}
\item 求 \(\frac{\sin 2A}{\sin 2A+\cos^2 A}\) 的值；
\item 若 \(B=\frac{\pi}{4}\)，\(a=3\)，求 \(\triangle ABC\) 的面积.
\end{enumerate}
\end{problem}

\begin{problem}
已知数列 \(\{a_n\}\) 和 \(\{b_n\}\) 满足 \(a_1=2\)，\(b_1=1\)，\(a_{n+1}=2a_n\,(n\in\mathbf{N}^{*})\)，\(b_1+\frac{1}{2}b_2+\frac{1}{3}b_3+\dots+\frac{1}{n}b_n=b_{n+1}-1\,(n\in\mathbf{N}^{*})\)。

\begin{enumerate}
\item 求 \(a_n\) 与 \(b_n\)；
\item 记数列 \(\{a_n b_n\}\) 的前 \(n\) 项和为 \(T_n\)，求 \(T_n\).
\end{enumerate}
\end{problem}

\begin{problem}
如图，在三棱柱 \(ABC-A_1B_1C_1\) 中，\(\angle BAC=90^\circ\)，\(AB=AC=2\)，\(A_1A=4\)，\(A_1\) 在底面 \(ABC\) 的射影为 \(BC\) 的中点，\(D\) 是 \(B_1C_1\) 的中点。

\bitmapfigure[max width=0.55\linewidth,max totalheight=7.0cm]{img/2015/zhejiang_liberal/q18_fig1.png}

\begin{enumerate}
\item 证明：\(A_1D \perp\) 平面 \(A_1BC\)；
\item 求直线 \(A_1B\) 和平面 \(BB_1C_1C\) 所成的角的正弦值.
\end{enumerate}
\end{problem}

\begin{problem}
如图，已知抛物线 \(C_1:y=\frac{1}{4}x^2\)，圆 \(C_2:x^2+(y-1)^2=1\)，过点 \(P(t,0)\)（\(t>0\)）作不过原点 \(O\) 的直线 \(PA,PB\) 分别与抛物线 \(C_1\) 和圆 \(C_2\) 相切，\(A,B\) 为切点。

\bitmapfigure[max width=0.55\linewidth,max totalheight=5.0cm]{img/2015/zhejiang_liberal/q19_fig1.png}

\begin{enumerate}
\item 求点 \(A,B\) 的坐标；
\item 求 \(\triangle PAB\) 的面积.
\end{enumerate}

注：直线与抛物线有且只有一个公共点，且与抛物线的对称轴不平行，则称该直线与抛物线相切，称该公共点为切点。
\end{problem}

\begin{problem}
设函数 \(f(x)=x^2+ax+b\)（\(a,b\in\R\)）。

\begin{enumerate}
\item 当 \(b=\frac{a^2}{4}+1\) 时，求函数 \(f(x)\) 在 \([-1,1]\) 上的最小值 \(g(a)\) 的表达式；
\item 已知函数 \(f(x)\) 在 \([-1,1]\) 上存在零点，\(0\leqslant b-2a\leqslant 1\)。求 \(b\) 的取值范围.
\end{enumerate}
\end{problem}
