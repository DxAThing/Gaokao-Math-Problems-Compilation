\chapter{2015年浙江卷（理）}

\section{单选题}

\begin{problem}
已知集合 \(P = \{x \mid x^2 - 2x \geqslant 0\}\)，\(Q = \{x \mid 1 < x \leqslant 2\}\)，则 \((\complement_{\R} P) \cap Q =\)（\quad）

\choices
    {\([0,1)\)}
    {\((0,2]\)}
    {(1,2)}
    {[1,2]}
\end{problem}

\begin{problem}
某几何体的三视图如图所示（单位：cm），则该几何体的体积是（\quad）

\bitmapfigure[max width=0.55\linewidth,max totalheight=6.3cm]{img/2015/zhejiang_science/q2_fig1.png}

\choices
    {\(8\mathrm{cm}^{3}\)}
    {\(12\mathrm{cm}^{3}\)}
    {\(\frac{32}{3}\mathrm{cm}^{3}\)}
    {\(\frac{40}{3}\mathrm{cm}^{3}\)}
\end{problem}

\begin{problem}
已知 \(\{a_n\}\) 是等差数列，公差 \(d\) 不为零，前 \(n\) 项和是 \(S_n\)，若 \(a_3, a_4, a_8\) 成等比数列，则（\quad）

\choices
    {\(a_{1}d > 0, dS_{4} > 0\)}
    {\(a_{1}d < 0, dS_{4} < 0\)}
    {\(a_{1}d > 0, dS_{4} < 0\)}
    {\(a_{1}d < 0, dS_{4} > 0\)}
\end{problem}

\begin{problem}
命题“\(\forall n \in \mathbf{N}^*\)，\(f(n) \in \mathbf{N}^*\) 且 \(f(n) \leqslant n\)”的否定形式是（\quad）

\choices
    {\(\forall n\in \mathbf{N}^*\) ， \(f(n)\notin \mathbf{N}^*\) 且 \(f(n) > n\)}
    {\(\forall n\in \mathbf{N}^*\) ， \(f(n)\notin \mathbf{N}^*\) 或 \(f(n) > n\)}
    {\(\exists n_0\in \mathbf{N}^*\) ， \(f(n_0)\notin \mathbf{N}^*\) 且 \(f(n_0) > n_0\)}
    {\(\exists n_0\in \mathbf{N}^*\) ， \(f(n_0)\notin \mathbf{N}^*\) 或 \(f(n_0) > n_0\)}
\end{problem}

\begin{problem}
如图，设抛物线 \(y^{2} = 4x\) 的焦点为 \(F\)，不经过焦点的直线上有三个不同的点 \(A, B, C\)，其中点 \(A, B\) 在抛物线上，点 \(C\) 在 \(y\) 轴上，则 \(\triangle BCF\) 与 \(\triangle ACF\) 的面积之比是（\quad）

\bitmapfigure[max width=0.42\linewidth,max totalheight=5.0cm]{img/2015/zhejiang_science/q5_fig1.png}

\choices
    {\(\dfrac{|BF| - 1}{|AF| - 1}\)}
    {\(\dfrac{|BF|^2 - 1}{|AF|^2 - 1}\)}
    {\(\dfrac{|BF| + 1}{|AF| + 1}\)}
    {\(\dfrac{|BF|^2 + 1}{|AF|^2 + 1}\)}
\end{problem}

\begin{problem}
设 \(A, B\) 是有限集. 定义: \(d(A, B) = \operatorname{card}(A \cup B) - \operatorname{card}(A \cap B)\)，其中 \(\operatorname{card}(A)\) 表示有限集 \(A\) 中元素的个数.

命题\circled{1}：对任意有限集 \(A, B\)，“\(A \neq B\)”是“\(d(A, B) > 0\)”的充分必要条件;

命题\circled{2}：对任意有限集 \(A, B, C\)，\(d(A, C) \le d(A, B) + d(B, C)\).（\quad）

\choices
    {命题\circled{1}和命题\circled{2}都成立}
    {命题\circled{1}和命题\circled{2}都不成立}
    {命题\circled{1}成立，命题\circled{2}不成立}
    {命题\circled{1}不成立，命题\circled{2}成立}
\end{problem}

\begin{problem}
存在函数 \( f(x) \) 满足：对于任意 \( x \in \R \) 都有（\quad）

\choices
    {\(f(\sin 2x) = \sin x\)}
    {\(f(\sin 2x) = x^2 + x\)}
    {\(f(x^{2} + 1) = |x + 1|\)}
    {\(f(x^{2} + 2x) = |x + 1|\)}
\end{problem}

\begin{problem}
如图，已知 \(\triangle ABC\)，\(D\) 是 \(AB\) 的中点，沿直线 \(CD\) 将 \(\triangle ACD\) 翻折成 \(\triangle A'CD\)，所成二面角 \(A' - CD - B\) 的平面角为 \(\alpha\)，则（\quad）

\bitmapfigure[max width=0.52\linewidth,max totalheight=4.8cm]{img/2015/zhejiang_science/q8_fig1.png}

\choices
    {\(\angle ADB \leqslant \alpha\)}
    {\(\angle ADB \geqslant \alpha\)}
    {\(\angle ACB \leqslant \alpha\)}
    {\(\angle ACB \geqslant \alpha\)}
\end{problem}

\section{填空题}

\begin{problem}
双曲线 \(\frac{x^2}{2} - y^2 = 1\) 的焦距是\fillinblank{}，渐近线方程是\fillinblank{}.
\end{problem}

\begin{problem}
已知函数 \( f(x) = \begin{cases} x + \frac{2}{x} - 3, & x \geqslant 1, \\ \lg (x^2 + 1), & x < 1, \end{cases} \) 则 \( f(f(-3)) =\fillinblank{} \)，\( f(x) \) 的最小值是\fillinblank{}.
\end{problem}

\begin{problem}
函数 \( f(x) = \sin^2 x + \sin x \cos x + 1 \) 的最小正周期是\fillinblank{}，单调递减区间是\fillinblank{}.
\end{problem}

\begin{problem}
若 \(a = \log_{4} 3\)，则 \(2^a + 2^{-a} =\fillinblank{}.\)
\end{problem}

\begin{problem}
如图，在三棱锥 \(A - BCD\) 中，\(AB = AC = BD = CD = 3\)，\(AD = BC = 2\)，点 \(M, N\) 分别为 \(AD, BC\) 的中点，则异面直线 \(AN, CM\) 所成的角的余弦值是\fillinblank{}.

\bitmapfigure[max width=0.38\linewidth,max totalheight=5.0cm]{img/2015/zhejiang_science/q13_fig1.png}
\end{problem}

\begin{problem}
若实数 \(x, y\) 满足 \(x^{2} + y^{2} \leqslant 1\)，则 \(\left|2x + y - 2\right| + \left|6 - x - 3y\right|\) 的最小值是\fillinblank{}.
\end{problem}

\begin{problem}
已知 \(e_{1}, e_{2}\) 是空间单位向量，\(e_{1} \cdot e_{2} = \frac{1}{2}\)，若空间向量 \( \overrightarrow{b} \) 满足 \( \overrightarrow{b} \cdot e_{1} = 2\)，\( \overrightarrow{b} \cdot e_{2} = \frac{5}{2}\)，且对于任意 \(x, y \in \R\)，\(\left|\overrightarrow{b} - (x e_{1} + y e_{2})\right| \geqslant \left|\overrightarrow{b} - (x_{0} e_{1} + y_{0} e_{2})\right| = 1\ (x_{0}, y_{0} \in \R)\)，则 \(x_{0} =\)\fillinblank{}，\(y_{0} =\)\fillinblank{}，\(|b| =\)\fillinblank{}.
\end{problem}

\section{解答题}

\begin{problem}
在 \(\triangle ABC\) 中，内角 \(A, B, C\) 所对的边分别是 \(a, b, c\)。已知 \(A = \frac{\pi}{4}\)，\(b^2 - a^2 = \frac{1}{2} c^2\).
\begin{enumerate}[label={（\arabic*）},itemsep=0.2em]
\item 求 \(\tan C\) 的值；
\item 若 \(\triangle ABC\) 的面积为 3，求 \(b\) 的值.
\end{enumerate}
\end{problem}

\begin{problem}
如图，在三棱柱 \(ABC - A_{1}B_{1}C_{1}\) 中，\(\angle BAC = 90^{\circ}\)，\(AB = AC = 2\)，\(A_{1}A = 4\)，\(A_{1}\) 在底面 \(ABC\) 的射影为 \(BC\) 的中点，\(D\) 是 \(B_{1}C_{1}\) 的中点.
\begin{enumerate}[label={（\arabic*）},itemsep=0.2em]
\item 证明: \(A_{1}D \perp\) 平面 \(A_{1}BC\)；
\item 求二面角 \(A_{1} - BD - B_{1}\) 的平面角的余弦值.
\end{enumerate}

\bitmapfigure[max width=0.42\linewidth,max totalheight=5.5cm]{img/2015/zhejiang_science/q17_fig1.png}
\end{problem}

\begin{problem}
已知函数 \( f(x) = x^2 + ax + b \) (\( a, b \in \R \))，记 \( M(a, b) \) 是 \( |f(x)| \) 在区间 \([-1, 1]\) 上的最大值.
\begin{enumerate}[label={（\arabic*）},itemsep=0.2em]
\item 证明：当 \(|a| \geqslant 2\) 时，\(M(a, b) \geqslant 2\)；
\item 当 \(a, b\) 满足 \(M(a, b) \leqslant 2\) 时，求 \(|a| + |b|\) 的最大值.
\end{enumerate}
\end{problem}

\begin{problem}
已知椭圆 \(\frac{x^2}{2} + y^2 = 1\) 上两个不同的点 \(A, B\) 关于直线 \(y = mx + \frac{1}{2}\) 对称.
\begin{enumerate}[label={（\arabic*）},itemsep=0.2em]
\item 求实数 \(m\) 的取值范围.
\item 求 \( \triangle AOB \) 面积的最大值 (O 为坐标原点).
\end{enumerate}
\end{problem}

\begin{problem}
已知数列 \(\{a_{n}\}\) 满足 \(a_1 = \frac{1}{2}\) 且 \(a_{n + 1} = a_n - a_n^2 (n\in \mathbf{N}^*)\).
\begin{enumerate}[label={（\arabic*）},itemsep=0.2em]
\item 证明: \(1 \leqslant \frac{a_n}{a_{n+1}} \leqslant 2 (n \in \mathbf{N}^*)\)；
\item 设数列 \(\{a_n^2\}\) 的前 \(n\) 项和为 \(S_n\)，证明: \(\frac{1}{2(n+2)} \leqslant \frac{S_n}{n} \leqslant \frac{1}{2(n+1)}\) (\(n \in \mathbf{N}^*\)).
\end{enumerate}
\end{problem}
