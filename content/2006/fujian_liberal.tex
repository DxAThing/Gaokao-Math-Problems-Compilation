\chapter{2006年福建卷（文）}
\section{单选题}
\begin{problem}
已知两条直线 \( y = ax - 2 \) 和 \( y = (a + 2)x + 1 \) 互相垂直，则 \( a \) 等于（\quad）

\choices
    {2}
    {1}
    {0}
    {\(-1\)}
\end{problem}

\begin{problem}
在等差数列 \(\{a_{n}\}\) 中，已知 \(a_{1} = 2, a_{2} + a_{3} = 13\)，则 \(a_{4} + a_{5} + a_{6}\) 等于（\quad）

\choices
    {40}
    {42}
    {43}
    {45}
\end{problem}

\begin{problem}
“\(\tan \alpha = 1\)”是“\(\alpha = \frac{\pi}{4}\)”的（\quad）

\choices
    {充分而不必要条件}
    {必要而不充分条件}
    {充要条件}
    {既不充分也不必要条件}
\end{problem}

\begin{problem}
已知 \(\alpha \in \left(\frac{\pi}{2}, \pi\right)\)，\(\sin \alpha = \frac{3}{5}\)，则 \(\tan \left(\alpha + \frac{\pi}{4}\right)\) 等于（\quad）

\choices
    {\(\frac{1}{7}\)}
    {7}
    {\(-\frac{1}{7}\)}
    {\(-7\)}
\end{problem}

\begin{problem}
已知全集 \(U = \R\)，且 \(A = \{x \mid |x - 1| > 2\}\)，\(B = \{x \mid x^2 - 6x + 8 < 0\}\)，则 \((\complement_U A) \cap B\) 等于（\quad）

\choices
    {\([-1, 4)\)}
    {(2,3)}
    {(2,3]}
    {\((-1,4)\)}
\end{problem}

\begin{problem}
函数 \( y = \frac{x}{x + 1} (x \neq -1) \) 的反函数是（\quad）

\choices
    {\( y = \frac{x}{1 - x} (x \neq 1) \)}
    {\( y = \frac{x}{x - 1} (x \neq 1) \)}
    {\( y = \frac{x - 1}{x} (x \neq 0) \)}
    {\( y = \frac{1 - x}{x} (x \neq 0) \)}
\end{problem}

\begin{problem}
已知正方体外接球的体积是 \(\frac{32}{3}\pi\)，那么正方体的棱长等于（\quad）

\choices
    {\(2\sqrt{2}\)}
    {\(\frac{2\sqrt{3}}{3}\)}
    {\(\frac{4\sqrt{2}}{3}\)}
    {\(\frac{4\sqrt{3}}{3}\)}
\end{problem}

\begin{problem}
从 4 名男生和 3 名女生中选出 3 人，分别从事三项不同的工作，若这 3 人中至少有 1 名女生，则选派方案共有（\quad）

\choices
    {108 种}
    {186 种}
    {216 种}
    {270 种}
\end{problem}

\begin{problem}
已知向量 \(\bs{a}\) 与 \(\bs{b}\) 的夹角为 \(120^{\circ}\)，\(|\bs{a}| = 3\)，\(|\bs{a} + \bs{b}| = \sqrt{13}\)，则 \(|\bs{b}|\) 等于（\quad）

\choices
    {5}
    {4}
    {3}
    {1}
\end{problem}

\begin{problem}
对于平面 \(\alpha\) 和共面的直线 \(m\)、\(n\)，下列命题中真命题是（\quad）

\choices
    {若 \(m \perp \alpha, m \perp n\)，则 \(n \parallel \alpha\)}
    {若 \(m \parallel \alpha, n \parallel \alpha\)，则 \(m \parallel n\)}
    {若 \(m \subset \alpha, n \parallel \alpha\)，则 \(m \parallel n\)}
    {若 \(m\)、\(n\) 与 \(\alpha\) 所成的角相等，则 \(m \parallel n\)}
\end{problem}

\begin{problem}
已知双曲线 \(\frac{x^2}{a^2} - \frac{y^2}{b^2} = 1 (a > 0, b > 0)\) 的右焦点为 \(F\)，若过点 \(F\) 且倾斜角为 \(60^\circ\) 的直线与双曲线的右支有且只有一个交点. 则此双曲线离心率的取值范围是（\quad）

\choices
    {(1,2]}
    {(1,2)}
    {\([2, +\infty)\)}
    {\((2, +\infty)\)}
\end{problem}

\begin{problem}
已知 \( f(x) \) 是周期为 2 的奇函数，当 \( 0 < x < 1 \) 时，\( f(x) = \lg x \). 设 \( a = f\left(\frac{6}{5}\right), b = f\left(\frac{3}{2}\right), c = f\left(\frac{5}{2}\right) \)，则（\quad）

\choices
    {\(a < b < c\)}
    {\(b < a < c\)}
    {\(c < b < a\)}
    {\(c < a < b\)}
\end{problem}

\section{填空题}
\begin{problem}
\(\left(x^{2} - \frac{1}{x}\right)^{5}\) 展开式中 \(x^{4}\) 的系数是 \fillinblank{}.（用数字作答）
\end{problem}

\begin{problem}
已知直线 \(x - y - 1 = 0\) 与抛物线 \(y = ax^2\) 相切，则 \(a = \)  \fillinblank{}.
\end{problem}

\begin{problem}
已知实数 \(x\)、\(y\) 满足 \(\begin{cases} y \leqslant 1, \\ y \geqslant |x - 1|, \end{cases}\) 则 \(x + 2y\) 的最大值是 \fillinblank{}.
\end{problem}

\begin{problem}
已知函数 \( f(x) = 2\sin \omega x (\omega > 0) \) 在区间 \(\left[-\frac{\pi}{3}, \frac{\pi}{4}\right]\) 上的最小值是 \(-2\)，则 \(\omega\) 的最小值等于 \fillinblank{}.
\end{problem}

\section{解答题}
\begin{problem}
已知函数 \( f(x) = \sin^2 x + \sqrt{3}\sin x\cos x + 2\cos^2 x, x \in \R \).

\begin{enumerate}
\item 求函数 \( f(x) \) 的最小正周期和单调增区间；
\item 函数 \( f(x) \) 的图象可以由函数 \( y = \sin 2x \, (x \in \R) \) 的图象经过怎样的变换得到.
\end{enumerate}
\end{problem}

\begin{problem}
每次抛掷一枚骰子（六个面上分别标以数 \(1, 2, 3, 4, 5, 6\)）。

\begin{enumerate}
\item 连续抛掷 \(2\) 次，求向上的数不同的概率；
\item 连续抛掷 \(2\) 次，求向上的数之和为 \(6\) 的概率；
\item 连续抛掷 \(5\) 次，求向上的数为奇数恰好出现 \(3\) 次的概率.
\end{enumerate}
\end{problem}

\begin{problem}
如图，四面体 \(ABCD\) 中，\(O\)、\(E\) 分别是 \(BD\)、\(BC\) 的中点，\(CA = CB = CD = BD = 2\)，\(AB = AD = \sqrt{2}\).

\bitmapfigure[max width=0.38\linewidth,max totalheight=4.8cm]{img/2006/fujian_liberal/q19_fig1.png}

\begin{enumerate}
\item 求证：\(AO \perp\) 平面 \(BCD\)；
\item 求异面直线 \(AB\) 与 \(CD\) 所成角的大小；
\item 求点 \(E\) 到平面 \(ACD\) 的距离.
\end{enumerate}
\end{problem}

\begin{problem}
已知椭圆 \(\frac{x^2}{2} + y^2 = 1\) 的左焦点为 \(F\)，\(O\) 为坐标原点。

\bitmapfigure[max width=0.42\linewidth,max totalheight=4.8cm]{img/2006/fujian_liberal/q20_fig1.png}

\begin{enumerate}
\item 求过点 \(O\)、\(F\)，并且与椭圆的左准线 \(l\) 相切的圆的方程；
\item 设过点 \(F\) 且不与坐标轴垂直的直线交椭圆于 \(A\)、\(B\) 两点，并且线段 \(AB\) 的中点在直线 \(x + y = 0\) 上，求直线 \(AB\) 的方程.
\end{enumerate}
\end{problem}

\begin{problem}
已知 \( f(x) \) 是二次函数，不等式 \( f(x) < 0 \) 的解集是 \((0,5)\)，且 \( f(x) \) 在区间 \([-1,4]\) 上的最大值是 12.

\begin{enumerate}
\item 求 \( f(x) \) 的解析式；
\item 是否存在自然数 \(m\)，使得方程 \(f(x) + \frac{37}{x} = 0\) 在区间 \((m, m + 1)\) 内有且只有两个不等的实数根？若存在，求出所有 \(m\) 的值；若不存在，说明理由.
\end{enumerate}
\end{problem}

\begin{problem}
已知数列 \(\{a_{n}\}\) 满足 \(a_1 = 1, a_2 = 3, a_{n+2} = 3a_{n+1} - 2a_n (n \in \mathbf{N}^*)\).

\begin{enumerate}
\item 证明：数列 \(\{a_{n+1} - a_n\}\) 是等比数列；
\item 求数列 \(\{a_{n}\}\) 的通项公式；
\item 若数列 \(\{b_{n}\}\) 满足 \(4^{b_1 - 1}4^{b_2 - 1}\cdots 4^{b_n - 1} = (a_n + 1)^{b_n}\) \((n\in \mathbf{N}^*)\)，证明：\(\{b_n\}\) 是等差数列.
\end{enumerate}
\end{problem}
