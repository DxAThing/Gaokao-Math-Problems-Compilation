\chapter{2006年天津卷（文）}
\section{单选题}
\begin{problem}
已知集合 \(A = \{x \mid -3 \leqslant x \leqslant 1\}\)，\(B = \{x \mid |x| \leqslant 2\}\)，则 \(A \cap B =\)（\quad）

\choices
    {\(\{x\mid -2\leqslant x\leqslant 1\}\)}
    {\(\{x\mid 0\leqslant x\leqslant 1\}\)}
    {\(\{x\mid -3\leqslant x\leqslant 2\}\)}
    {\(\{x\mid 1\leqslant x\leqslant 2\}\)}
\end{problem}

\begin{problem}
设 \(\{\alpha_{n}\}\) 是等差数列，\(\alpha_{1} + \alpha_{3} + \alpha_{5} = 9\)，\(\alpha_{6} = 9\)，则这个数列的前 6 项和等于（\quad）

\choices
    {12}
    {24}
    {36}
    {48}
\end{problem}

\begin{problem}
设变量 \(x\)、\(y\) 满足约束条件 \(\left\{ \begin{array}{l} y \leqslant x, \\ x + y \geqslant 2, \\ y \geqslant 3x - 6, \end{array} \right.\) 则目标函数 \(z = 2x + y\) 的最小值为（\quad）

\choices
    {2}
    {3}
    {4}
    {9}
\end{problem}

\begin{problem}
设 \(P = \log_6 3, Q = \log_8 2, R = \log_9 (\log_2 2)\)，则（\quad）

\choices
    {\(R < Q < P\)}
    {\(P < R\)}
    {\(Q < R < P\)}
    {\(R < P < Q\)}
\end{problem}

\begin{problem}
设 \(\alpha, \beta \in \left(-\frac{\pi}{2}, \frac{\pi}{2}\right)\)，那么“\(\alpha < \beta\)”是“\(\tan \alpha < \tan \beta\)”的（\quad）

\choices
    {充分而不必要条件}
    {必要而不充分条件}
    {充分必要条件}
    {既不充分也不必要条件}
\end{problem}

\begin{problem}
函数 \( y = \sqrt{x^2 + 1} + 1 (x < 0) \) 的反函数是（\quad）

\choices
    {\( y = \sqrt{x^2 - 2x} (x < 0) \)}
    {\( y = -\sqrt{x^2 - 2x} (x < 0) \)}
    {\( y = \sqrt{x^2 - 2x} (x > 2) \)}
    {\( y = -\sqrt{x^2 - 2x} (x > 2) \)}
\end{problem}

\begin{problem}
若 \(l\) 为一条直线，\(\alpha, \beta, \gamma\) 为三个互不重合的平面. 给出下面三个命题: \circled{1} \( \alpha \perp \gamma, \beta \perp \gamma \Rightarrow \alpha \perp \beta; \) \circled{2} \( \alpha \perp \gamma, \beta \not\perp \gamma \Rightarrow \alpha \perp \beta; \) \circled{3} \( \beta \not\perp \alpha, \beta \perp \beta \Rightarrow \alpha \perp \beta. \) 其中结论是（\quad）

\choices
    {0个}
    {1 个}
    {2 个}
    {3 个}
\end{problem}

\begin{problem}
椭圆的中心为点 \(E(-1,0)\)，它的一个焦点为 \(F(-3,0)\)，相应于焦点 \(F\) 的准线方程为 \(x = -\frac{1}{2}\)，则这个椭圆的方程是（\quad）

\choices
    {\(\frac{2(x - 1)^2}{21} y^2 + \frac{2y^2}{3} = 1\)}
    {\(\frac{2(x + 1)^2}{21} + \frac{2y^2}{3} = 1\)}
    {\(\frac{(x - 1)^2}{5} + y^2 = 1\)}
    {\(\frac{(x + 1)^2}{5} + y^2 = 1\)}
\end{problem}

\begin{problem}
已知函数 \( f(x) = a\sin x - b\cos x \) (\( a, b \) 为常数，\( a \neq 0, x \in \R \)) 的图象关于直线 \( x = \frac{\pi}{4} \) 对称，则函数 \( y = f\left(\frac{3\pi}{4} - x\right) \) 是（\quad）

\choices
    {偶函数且它的图象关于点 \((\pi, 0)\) 对称}
    {偶函数且它的图象关于点 \(\left(\frac{3\pi}{2}, 0\right)\) 对称}
    {奇函数且它的图象关于点 \(\left(\frac{3\pi}{2}, 0\right)\) 对称}
    {奇函数且它的图象关于点 \((\pi, 0)\) 对称}
\end{problem}

\begin{problem}
如果函数 \( f(x) = x^{\alpha} (\alpha^2 - 3\alpha^2 - 1) (\alpha > 0 \text{ 且 } \alpha \neq 1) \) 在区间 \([0, +\infty)\) 上是增函数，那么实数 \(\alpha\) 的取值范围是（\quad）

\choices
    {\(\left(0, \frac{3}{2}\right]\)}
    {\(\left[\frac{\sqrt{3}}{3}, 1\right)\)}
    {\((1, \sqrt{3}]\)}
    {\(\left[\frac{3}{2}, +\infty\right)\)}
\end{problem}

\section{填空题}
\begin{problem}
\(\left(x + \frac{1}{\sqrt{x}}\right)^7\) 的二项展开式中 \(x\) 的系数是 \fillinblank{}. (用数字作答)
\end{problem}

\begin{problem}
设向量 a 与 b 的夹角为 \( \theta \) ，且 \( a = (3, 3) \) ， \( 2\theta - a = (-1, 1) \) ，则 \( \cos \theta = \)  \fillinblank{}.
\end{problem}

\begin{problem}
如图，在正三棱柱 \(ABC - A_{1}B_{1}C_{1}\) 中，\(AB = 1\). 若二面角 \(C - AB - C_{1}\) 的大小为 \(60^{\circ}\)，则点 \(C\) 到平面 \(ABC_{1}\) 的距离为 \fillinblank{} \fillinblank{}.
\end{problem}

\begin{problem}
若半径为 1 的圆分别与 y 轴的正半轴和射线 \( y = \frac{\sqrt{3}}{3} x (x \geqslant 0) \) 相切，则这个圆的方程为 \fillinblank{} \fillinblank{}.
\end{problem}

\begin{problem}
某公司一年购买某种货物 400 吨，每次都购买 x 吨. 运费为 4 万元/次，一年的总存储费用为 4x 万元. 要使一年的总运费与总存储费用之和最小，则 x = 吨 \fillinblank{}.
\end{problem}

\begin{problem}
用数字 0、1、2、3、4 组成没有重复数字的五位数，则其中数字 1、2 相邻的偶数有 个. (用数字作答)
\end{problem}

\section{解答题}
\begin{problem}
已知 \(\tan \alpha + \cot \alpha = \frac{5}{2}, \alpha \in \left( \frac{\pi}{4}, \frac{\pi}{2} \right)\)，求 \(\cos 2\alpha\) 和 \(\sin \left(2\alpha + \frac{\pi}{4}\right)\) 的值.
\end{problem}

\begin{problem}
甲、乙两台机床相互没有影响地生产某种产品，甲机床产品的正品率是 0.9，乙机床产品的正品率是 0.95. (1) 从甲机床生产的产品中任取 3 件，求其中恰有 2 件正品的概率 (用数字作答); (2) 从甲、乙两台机床生产的产品中各任取 1 件，求其中至少有 1 件正品的概率 (用数字作答).
\end{problem}

\begin{problem}
如图，在五面体 \(ABCDEF\) 中，点 \(O\) 是矩形 \(ABCD\) 的对角线的交点，面 \(CDE\) 是等边三角形，棱 \(EF // BC\) 且 \(EF = \frac{1}{2} BC\). (1) 证明： \( FO // \) 平面 CDE; (2) 设 \(BC = \sqrt{3} CD\)，证明: \(EO \perp\) 平面 \(CDF\).
\end{problem}

\begin{problem}
已知函数 \( f(x) = 4x^{3} - 3x^{2}\cos \theta + \frac{1}{32} \)，其中 \( x \in \R, \theta \) 为参数，且 \( 0 \leqslant \theta \leqslant \frac{\pi}{2} \). (1) 当 \( \cos\theta=0 \) 时，判断函数 \( f(x) \) 是否有极值； (2) 要使函数 \( f(x) \) 的极小值大于零，求参数 \( \theta \) 的取值范围; (3) 若对 (2) 中所求的取值范围性的任意参数 \(\theta\)，函数 \(f(x)\) 在区间 \((2a - 1, a)\) 内都是增函数，求实数 \(a\) 的取值范围.
\end{problem}

\begin{problem}
已知数列 \(\{x_{n}\}\) 满足 \(x_{1} = x_{2} = 1\)，并且 \(\frac{x_{n + 1}}{x_n} = \lambda \frac{x_n}{x_{n - 1}}\) (\(\lambda\) 为非零参数，\(n = 2,3,4,\dots\)). (1) 若 \(x_{1}, x_{2}, x_{3}\) 成等比数列，求参数 \(\lambda\) 的值; (2) 设 \(0 < \lambda < 1\)，常数 \(k \in \mathbf{N}^*\) 且 \(k \geqslant 3\)，证明: \(\frac{x_{1+k}}{x_1} + \frac{x_{2+k}}{x_2} + \cdots + \frac{x_{n+k}}{x_n} < \frac{\lambda^k}{1 - \lambda^k} (n \in \mathbf{N}^*)\).
\end{problem}

\begin{problem}
如图，双曲线 \(\frac{x^2}{a^2} - \frac{y^2}{b^2} = 1 (a > 0, b > 0)\) 的离心率为 \(\frac{\sqrt{5}}{2}\)，\(F_1, F_2\) 分别为左、右焦点，\(M\) 为左准线与渐近线在第二象限内的交点，且 \(\overrightarrow{F_1M} \cdot \overrightarrow{F_2M} = -\frac{1}{4}\). (1) 求双曲线的方程; (2) 设 \(A(m,0)\) 和 \(B\left(\frac{1}{m},0\right)\) \((0 < m < 1)\) 是 \(\pmb{x}\) 轴上的两点. 过点 \(A\) 作斜率不为0的直线 \(l\)，使得 \(l\) 交双曲线于 \(C\) 、 \(D\) 两点. 作直线 \(BC\) 交双曲线于另一点 \(E\). 证明: 直线 \(DE\) 垂直于 \(x\) 轴.
\end{problem}

