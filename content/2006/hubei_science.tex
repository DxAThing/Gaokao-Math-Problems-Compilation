\chapter{2006年湖北卷（理）}
\section{单选题}
\begin{problem}
已知向量 \(\bs{a} = (\sqrt{3}, 1)\)，\(\bs{b}\) 是不平行于 \(x\) 轴的单位向量，且 \(\bs{a} \cdot \bs{b} = \sqrt{3}\)，则 \(\bs{b} =\)（\quad）

\choices
    {\(\left(\frac{\sqrt{3}}{2}, \frac{1}{2}\right)\)}
    {\(\left(\frac{1}{2}, \frac{\sqrt{3}}{2}\right)\)}
    {\(\left(\frac{1}{4}, \frac{3\sqrt{3}}{4}\right)\)}
    {(1,0)}
\end{problem}

\begin{problem}
若互相不等的实数 \(a, b, c\) 成等差数列，\(c, a, b\) 成等比数列，且 \(a + 3b + c = 10\)，则 \(a =\)（\quad）

\choices
    {4}
    {2}
    {\(-2\)}
    {\(-4\)}
\end{problem}

\begin{problem}
\(\triangle ABC\) 的内角 \(A\) 满足 \(\sin 2A = \frac{2}{3}\)，则 \(\sin A + \cos A =\)（\quad）

\choices
    {\(\frac{\sqrt{15}}{3}\)}
    {\(-\frac{\sqrt{15}}{3}\)}
    {\( \frac{5}{3} \)}
    {\(-\frac{5}{3}\)}
\end{problem}

\begin{problem}
设 \( f(x) = \lg \frac{2 + x}{2 - x} \)，则 \( f\left(\frac{x}{2}\right) + f\left(\frac{2}{x}\right) \) 的定义域为（\quad）

\choices
    {\((-4,0)\cup (0,4)\)}
    {\((-4, - 1)\cup (1,4)\)}
    {\((-2, - 1)\cup (1,2)\)}
    {\((-4, - 2)\cup (2,4)\)}
\end{problem}

\begin{problem}
在 \(\left(\sqrt{2} - \frac{1}{\sqrt[3]{2}}\right)^4\) 的展开式中，\(x\) 的幂的指数是整数的项共有（\quad）

\choices
    {3项}
    {4项}
    {5 项}
    {9 项}
\end{problem}

\begin{problem}
关于直线 \(m, n\) 与平面 \(\alpha, \beta\)，有以下四个命题: \circled{1} 若 \(m \nmid \alpha, n \nmid \beta\) 且 \(\alpha \nmid \beta\)，则 \(m \nmid n\); \circled{2} 若 \(m \perp \alpha, n \perp \beta\) 且 \(\alpha \perp \beta\)，则 \(m \perp n\); \circled{3} 若 \(m \perp \alpha, n \nmid \beta\) 且 \(\alpha \nmid \beta\)，则 \(m \perp n\); \circled{4} 若 \(m \nmid \alpha, n \perp \beta\) 且 \(\alpha \perp \beta\)，则 \(m \nmid n\). 其中真命题的序号是（\quad）

\choices
    {\circled{1}\circled{2}}
    {\circled{3}\circled{4}}
    {\circled{1}\circled{4}}
    {\circled{2}\circled{3}}
\end{problem}

\begin{problem}
设过点 \(P(x, y)\) 的直线分别与 \(x\) 轴的正半轴和 \(y\) 轴的正半轴交于 \(A, B\) 两点，点 \(Q\) 与点 \(P\) 关于 \(y\) 轴对称，\(O\) 为坐标原点，若 \(\overrightarrow{BP} = 2\overrightarrow{PB}\) 且 \(\overrightarrow{OQ} \cdot \overrightarrow{AB} = 1\) ，则点 \(P\) 的轨迹方程是（\quad）

\choices
    {\(3x^{2} + \frac{3}{4} y^{2} = 1 (x > 0, y > 0)\)}
    {\(3x^{2} - \frac{3}{2} y^{2} = 1 (x > 0, y > 0)\)}
    {\(\frac{3}{2} x^{2} - 3y^{2} = 1 (x > 0, y > 0)\)}
    {\(\frac{3}{2} x^{2} + 3y^{2} = 1 (x > 0, y > 0)\)}
\end{problem}

\begin{problem}
有限集合 \(S\) 中元素的个数记作 \(\operatorname{card}(S)\). 设 \(A, B\) 都为有限集合，给出下列命题: \circled{1} \(A \cap B = \varnothing\) 的充要条件是 \(\operatorname{card}(A \cup B) = \operatorname{card}(A) + \operatorname{card}(B)\); \circled{2} \(A \subseteq B\) 的必要条件是 \(\operatorname{card}(A) \leqslant \operatorname{card}(B)\); \circled{3} \(A \nsubseteq B\) 的充分条件是 \(\operatorname{card}(A) \leqslant \operatorname{card}(B)\); \circled{4} \(A = B\) 的充要条件是 \(\operatorname{card}(A) = \operatorname{card}(B)\). 其中真命题的序号是（\quad）

\choices
    {\circled{1}\circled{2}}
    {\circled{3}\circled{4}}
    {\circled{1}\circled{4}}
    {\circled{2}\circled{3}}
\end{problem}

\begin{problem}
已知平面区域 \(D\) 由以 \(A(1,3), B(5,2), C(3,1)\) 为顶点的三角形内部和边界组成. 若在区域 \(D\) 上有无穷多个点 \((x, y)\) 可使目标函数 \(z = x + my\) 取得最小值，则 \(m =\)（\quad）

\choices
    {\(-2\)}
    {\(-1\)}
    {1}
    {4}
\end{problem}

\begin{problem}
关于 \(x\) 的方程 \((x^{2} - 1)^{2} - |x^{2} - 1| + k = 0\). 给出下列四个命题: \circled{1} 存在实数 \(k\)，使得方程恰有 2 个不同的实根； \circled{2} 存在实数 \(k\)，使得方程恰有 4 个不同的实根； \circled{3} 存在实数 \(k\)，使得方程恰有 5 个不同的实根； \circled{4} 存在实数 \(k\)，使得方程恰有 8 个不同的实根. 其中假命题的个数是（\quad）

\choices
    {0}
    {1}
    {2}
    {3}
\end{problem}

\section{填空题}
\begin{problem}
设 \(x, y\) 为实数，且 \(\frac{x}{1 - x} + \frac{y}{1 - 2i} = \frac{5}{1 - 3i}\)，则 \(x + y = \)  \fillinblank{}.
\end{problem}

\begin{problem}
接种某疫苗后，当出现发热反应的概率为 0.80，现有 5 人接种该疫苗，至少有 3 人出现发热反应的概率为 (精确到 0.01)
\end{problem}

\begin{problem}
已知直线 \(5x + 12y + a = 0\) 与圆 \(x^{2} - 2x + y^{2} = 0\) 相切，则 \(a\) 的值为 \fillinblank{} \fillinblank{}.
\end{problem}

\begin{problem}
某工程队有 6 项工程需要先后单独完成，其中工程乙必须在工程甲完成后才能进行，工程丙必须在工程乙完成后才能进行，又工程丁必须在工程丙完成后立即进行，那么安排这 6 项工程的不同排种数是 \fillinblank{}. （用数字作答）
\end{problem}

\begin{problem}
将杨辉三角中的每一个数 \(\mathrm{C}_n^n\) 都换成分数 \(\frac{1}{(n + 1)\mathrm{C}_n^n}\)，得到一个如图所示的分数三角形，成为莱布尼茨三角形，从莱布尼茨三角形可看出 \(\frac{(n + 1)\mathrm{C}_n^n}{30} + \frac{(n + 1)\mathrm{C}_n^n}{30} = \frac{1}{n\mathrm{C}_{n - 1}^n} + \frac{1}{(n + 1)\mathrm{C}_n^2} = \frac{1}{n + 1}\)，则 \(\lim_{n\to \infty}a_n = \) . 1 1 1/2 1/3 1 1/5 1/6 1/7 .. \fillinblank{}.
\end{problem}

\begin{problem}
设函数 \( f(x) = x^2 - (\sqrt[3]{x} + \sqrt[2]{x}) \)，其中向量 \( x^2 = (\sin x, -\cos x) \)，\( \sqrt[3]{x} = \) \((\sin x, -3\cos x), \sqrt[2]{x} = (-\cos x, \sin x), x \in \R\). (1) 求函数 \( f(x) \) 的最大值和最小正周期; (2) 将函数 \( f(x) \) 的图像按向量 \( \overrightarrow{d} \) 平移，使平移后得到的图像关于坐标原点成中心对称，求长度最小的 \( \overrightarrow{d} \)  \fillinblank{}.
\end{problem}

\begin{problem}
已知二次函数 \( y = f(x) \) 的图象经过坐标原点，其导函数为 \( f'(x) = 6x - 2 \)，数列 \( \{a_n\} \) 的前 \( n \) 项和为 \( S_n \)，点 \( (n, S_n) (n \in \mathbf{N}^*) \) 均在函数 \( y = f(x) \) 的图象上. (1) 求数列 \( \{a_{n}\} \) 的通项公式; (2) 设 \( b_{n} = \frac{3}{\sqrt{a_{n}a_{n+1}}} \) ， \( T_{n} \) 是数列 \( \{b_{n}\} \) 的前 n 项和，求使得 \( T_{n} < \frac{m}{20} \) 对所有 \( n \in N^{*} \) 都成立的最小正整数 m \fillinblank{}.
\end{problem}

\begin{problem}
如图，在棱长为 1 的正方体 \(ABCD - A_{1}B_{1}C_{1}D_{1}\) 中，\(P\) 是侧棱 \(CC_{1}\) 上的一点，\(CP = m\). (1) 试确定 \(m\). 使直线 \(AP\) 与平面 \(BDD_{1}D_{1}\) 所成角的正切值为 \(3\sqrt{2}\); (2) 在线段 \(A_{1}C_{1}\) 上是否存在一个定点 \(Q\)，使得对任意的 \(m, D_{1}Q\) 在平面 \(APD_{1}\) 上的射影垂直于 \(AP\)，并证明你的结论 \fillinblank{}.
\end{problem}

\begin{problem}
设 \(A\)、\(B\) 分别为椭圆 \(\frac{x^2}{a^2} + \frac{y^2}{b^2} = 1 (a, b > 0)\) 的左、右顶点，椭圆长半轴的长等于焦距，且 \(z = 4\) 为它的右准线. (1) 求椭圆的方程. (2) 设 \(P\) 为右准线不同于点 \((4,0)\) 的任意一点，若直线 \(AP, BP\) 分别与椭圆相交于异于 \(A, B\) 的点 \(M, N\)，证明点 \(B\) 在以 \(MN\) 为直径的圆内 \fillinblank{}.
\end{problem}

\begin{problem}
设 \(x = 3\) 是函数 \(f(x) = (x^2 + ax + b)\e^{3 - x}\) (\(x \in \R\)) 的一个极值点. (1) 求 \(a\) 与 \(b\) 的关系式 (用 \(a\) 表示 \(b\))，并求 \(f(x)\) 的单调区间; (2) 设 \(a > 0, g(x) = \left(a^2 + \frac{25}{x}\right)\e^x\). 若存在 \(\xi_1, \xi_2 \in [0,4]\) 使得 \(|f(\xi_1) - g(\xi_2)| < 1\) 成立，求 \(a\) 的取值范围 \fillinblank{}.
\end{problem}

\begin{problem}
在某校举行的数学竞赛中，全体参赛学生的竞赛成绩近似服从正态分布 \(N(70,100)\). 已知成绩在90分以上（含90分）的学生有12名. (1) 试问此次参赛学生总数约为多少人\(\perp\) (2) 若该校计划奖励竞赛成绩排在前 30 名的学生，试问获奖的分数线约为多少分\(\perp\) 可供查阅的（部分）标准正态分布表 \(\Phi (x_0) = P(x < x_0)\) \( x_0 \) 0 1 2 3 4 1.2 0.8849 0.8869 0.8888 0.8907 0.8925 1.3 0.9032 0.9049 0.9066 0.9082 0.9099 1.4 0.9192 0.9207 0.9222 0.9236 0.9251 1.9 0.9713 0.9719 0.9726 0.9732 0.9738 2.0 0.9772 0.9782 0.9785 0.9789 0.9793 2.1 0.9822 0.9826 0.9830 0.9834 0.9838 \( x_0 \) 5 6 7 8 9 1.2 0.8944 0.8962 0.8980 0.8997 0.9015 1.3 0.9115 0.9131 0.9147 0.9162 0.9177 1.4 0.9265 0.9278 0.9292 0.9306 0.9319 1.9 0.9744 0.9750 0.9756 0.9762 0.9767 2.0 0.9798 0.9803 0.9808 0.9812 0.9817 2.1 0.9842 0.9846 0.9850 0.9854 0.9857
\end{problem}

\begin{problem}
设 \(x = 3\) 是函数 \(f(x) = (x^2 + ax + b)\e^{3 - x}\) (\(x \in \R\)) 的一个极值点. (1) 求 \(a\) 与 \(b\) 的关系式 (用 \(a\) 表示 \(b\))，并求 \(f(x)\) 的单调区间; (2) 设 \(a > 0, g(x) = \left(a^2 + \frac{25}{x}\right)\e^x\). 若存在 \(\xi_1, \xi_2 \in [0,4]\) 使得 \(|f(\xi_1) - g(\xi_2)| < 1\) 成立，求 \(a\) 的取值范围 \fillinblank{}.
\end{problem}

