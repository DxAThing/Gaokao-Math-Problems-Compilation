\chapter{2006年四川卷（文）}
\section{单选题}
\begin{problem}
已知集合 \(A = \{x \mid x^2 - 5x + 6 \leqslant 0\}\)，集合 \(B = \{x \mid |2x - 1| > 3\}\)，则集合 \(A \cap B =\)（\quad）

\choices
    {\(\{x\mid 2\leqslant x\leqslant 3\}\)}
    {\(\{x\mid 2\leqslant x < 3\}\)}
    {\(\{x\mid 2 < x\leqslant 3\}\)}
    {\(\{x\mid -1 < x < 3\}\)}
\end{problem}

\begin{problem}
函数 \(f(x) = \ln(x - 1)\)（\(x > 1\)）的反函数是（\quad）

\choices
    {\(f^{-1}(x) = \e^{x} + 1(x\in \R)\)}
    {\(f^{-1}(x) = 10^{x} + 1(x\in \R)\)}
    {\(f^{-1}(x) = 10^{x} + 1(x > 1)\)}
    {\(f^{-1}(x) = \e^{x} + 1(x > 1)\)}
\end{problem}

\begin{problem}
曲线 \( y = 4x - x^3 \) 在点 \((-1, -3)\) 处的切线方程是（\quad）

\choices
    {\(y = 7x + 4\)}
    {\(y = 7x + 2\)}
    {\(y = x - 4\)}
    {\(y = x - 2\)}
\end{problem}

\begin{problem}
如图，已知正六边形 \(P_{1}P_{2}P_{3}P_{4}P_{5}P_{6}\). 下列向量的数量积中最大的是（\quad）

\bitmapfigure[max width=0.42\linewidth,max totalheight=4.2cm]{img/2006/sichuan_liberal/q4_fig1.png}

\choices
    {\(\overrightarrow{P_1P_2} \cdot \overrightarrow{P_1P_3}\)}
    {\(\overrightarrow{P_1P_2} \cdot \overrightarrow{P_1P_4}\)}
    {\(\overrightarrow{P_1P_2} \cdot \overrightarrow{P_1P_5}\)}
    {\(\overrightarrow{P_1P_2} \cdot \overrightarrow{P_1P_6}\)}
\end{problem}

\begin{problem}
甲校有 3600 名学生，乙校有 5400 名学生，丙校有 1800 名学生. 为统计三校学生某方面的情况，计划采用分层抽样法，抽取一个容量为 90 人的样本，在这三校分别抽取学生（\quad）

\choices
    {30 人，30 人，30 人}
    {30 人，45 人，15 人}
    {20 人，30 人，10 人}
    {30 人，50 人，10 人}
\end{problem}

\begin{problem}
下列函数中，图象的一部分如图所示的是（\quad）

\bitmapfigure[max width=0.42\linewidth,max totalheight=4.0cm]{img/2006/sichuan_liberal/q6_fig1.png}

\choices
    {\(y = \sin \left(x + \frac{\pi}{6}\right)\)}
    {\(y = \sin \left(2x - \frac{\pi}{6}\right)\)}
    {\( y = \cos \left(4x - \frac{\pi}{3}\right) \)}
    {\( y = \cos \left(2x - \frac{\pi}{6}\right) \)}
\end{problem}

\begin{problem}
已知二面角 \(\alpha - l - \beta\) 的大小为 \(60^{\circ}\)，\(m\)、\(n\) 为异面直线，且 \(m \perp \alpha\)、\(n \perp \beta\) 则 \(m\)、\(n\) 所成的角为（\quad）

\choices
    {\(30^{\circ}\)}
    {\(60^{\circ}\)}
    {\(90^{\circ}\)}
    {\(120^{\circ}\)}
\end{problem}

\begin{problem}
已知两定点 \(A(-2,0)\)、\(B(1,0)\)，如果动点 \(P\) 满足 \(|PA| = 2|PB|\)，则点 \(P\) 的轨迹所包围的图形的面积等于（\quad）

\choices
    {\(9\pi\)}
    {\(8\pi\)}
    {\(4\pi\)}
    {\(\pi\)}
\end{problem}

\begin{problem}
如图，正四棱锥 \(P - ABCD\) 底面的四个顶点 \(A\)、\(B\)、\(C\)、\(D\) 在球 \(O\) 的同一个大圆上，点 \(P\) 在球面上，如果 \(V_{P - ABCD} = \frac{16}{3}\)，则球 \(O\) 的表面积是（\quad）

\bitmapfigure[max width=0.48\linewidth,max totalheight=4.8cm]{img/2006/sichuan_liberal/q9_fig1.png}

\choices
    {\(4\pi\)}
    {\(8\pi\)}
    {\(12\pi\)}
    {\(16\pi\)}
\end{problem}

\begin{problem}
直线 \( y = x - 3 \) 与抛物线 \( y^{2} = 4x \) 交于 \( A\)、\(B\) 两点，过 \( A\)、\(B\) 两点向抛物线的准线作垂线，垂足分别为 \( P\)、\(Q\)，则梯形 \( APQB \) 的面积为（\quad）

\choices
    {36}
    {48}
    {56}
    {64}
\end{problem}

\begin{problem}
设 \(a, b, c\) 分别是 \(\triangle ABC\) 的三个内角 \(A, B, C\) 所对的边，则 \(a^2 = b(b + c)\) 是 \(A = 2B\) 的（\quad）

\choices
    {充要条件}
    {充分而不必要条件}
    {必要而不充分条件}
    {既不充分也不必要条件}
\end{problem}

\begin{problem}
从 0 到 9 这 10 个数字中任取 3 个数字组成一个没有重复数字的三位数，这个数不能被 3 整除的概率为（\quad）

\choices
    {\(\frac{41}{60}\)}
    {\(\frac{38}{54}\)}
    {\(\frac{35}{54}\)}
    {\(\frac{19}{54}\)}
\end{problem}

\section{填空题}
\begin{problem}
\((1 - 2x)^{10}\) 展开式中的 \(x^{3}\) 的系数为 （用数字作答）
\end{problem}

\begin{problem}
设 \(x\)、\(y\) 满足约束条件 \(\left\{ \begin{array}{l} x \geqslant 1, \\ y \geqslant \frac{1}{2}x, \\ 2x + y \leqslant 10, \end{array} \right.\) 则 \(z = 2x - y\) 的最小值为\fillinblank{}.
\end{problem}

\begin{problem}
如图，把椭圆 \(\frac{x^2}{25} + \frac{y^2}{16} = 1\) 的长轴 \(AB\) 分成 8 等分，过每个分点作 \(x\) 轴的垂线交椭圆的上半部分于 \(P_1\)、\(P_2\)、\(\cdots\)、\(P_7\) 七个点，\(F\) 是椭圆的一个焦点.

\bitmapfigure[max width=0.50\linewidth,max totalheight=4.8cm]{img/2006/sichuan_liberal/q15_fig1.png}

\noindent 则 \(|P_1F| + |P_2F| + \cdots + |P_7F| =\fillinblank{}.\)
\end{problem}

\begin{problem}
\(m\)、\(n\) 是空间两条不同直线，\(\alpha\)、\(\beta\) 是两个不同平面，下面有四个命题：

\begin{circlelist}
\item \(m \perp \alpha, n \parallel \beta, \alpha \parallel \beta \Rightarrow m \perp n;\)
\item \(m \perp n, \alpha \parallel \beta, m \perp \alpha \Rightarrow n \perp \beta;\)
\item \(m \perp n, \alpha \parallel \beta, m \parallel \alpha \Rightarrow n \perp \beta;\)
\item \(m \perp \alpha, m \parallel n, \alpha \parallel \beta \Rightarrow n \perp \beta.\)
\end{circlelist}

其中真命题的编号是\fillinblank{}.（写出所有真命题的编号）
\end{problem}

\section{解答题}
\begin{problem}
数列 \(\{a_{n}\}\) 的前 \(n\) 项和记为 \(S_{n}\)，\(a_{1} = 1\)，\(a_{n+1} = 2S_{n} + 1\)（\(n \geqslant 1\)）.

\begin{enumerate}
\item 求 \(\{a_{n}\}\) 的通项公式；
\item 等差数列 \(\{b_{n}\}\) 的各项为正，其前 \(n\) 项和 \(T_{n}\)，且 \(T_{3}=15\)，又 \(a_{1}+b_{1}\)、\(a_{2}+b_{2}\)、\(a_{3}+b_{3}\) 成等比数列，求 \(T_{n}\).
\end{enumerate}
\end{problem}

\begin{problem}
已知 \(A\)、\(B\)、\(C\) 是 \(\triangle ABC\) 三内角，向量 \(m = (-1, \sqrt{3})\)，\(n = (\cos A, \sin A)\)，且 \(m \cdot n = 1\).

\begin{enumerate}
\item 求角 \(A\)；
\item 若 \(\frac{1 + \sin 2B}{\cos^2 B - \sin^2 B} = -3\)，求 \(\tan C\).
\end{enumerate}
\end{problem}

\begin{problem}
某课程考核分理论和实验两部分进行，每部分考核成绩只记“合格”与“不合格”，两部分考核都“合格”则该课程考核“合格”. 甲、乙、丙三人在理论考核中合格的概率分别为 0.9、0.8、0.7；在实验考核中合格的概率分别为 0.8、0.7、0.9。所有考核是否合格互相之间没有影响.

\begin{enumerate}
\item 求甲、乙、丙三人在理论考核中至少要有两人合格的概率；
\item 求这三人该课程考核都合格的概率（结果保留三位小数）.
\end{enumerate}
\end{problem}

\begin{problem}
如图，在长方体 \(ABCD - A_{1}B_{1}C_{1}D_{1}\) 中，\(E\)、\(P\) 分别是 \(BC\)、\(A_{1}D_{1}\) 的中点，\(M\)、\(N\) 分别是 \(AE\)、\(CD_{1}\) 的中点，\(AD = AA_{1} = a\)，\(AB = 2a\).
\begin{enumerate}
\item 求证：\(MN \parallel\) 面 \(ADD_{1}A_{1}\)；
\item 求二面角 \(P - AE - D\) 的大小.
\end{enumerate}

\bitmapfigure[max width=0.55\linewidth,max totalheight=4.8cm]{img/2006/sichuan_liberal/q20_fig1.png}

\end{problem}

\begin{problem}
已知函数 \(f(x) = x^3 + 3ax - 1\)，\(g(x) = f'(x) - ax - 5\)，其中 \(f'(x)\) 是 \(f(x)\) 的导函数.

\begin{enumerate}
\item 对满足 \(-1 \leqslant a \leqslant 1\) 的一切 \(a\) 的值，都有 \(g(x) < 0\)，求实数 \(x\) 的取值范围；
\item 设 \(a = -m^2\)，当实数 \(m\) 在什么范围内变化时，函数 \(y = f(x)\) 的图像与直线 \(y = 3\) 只有一个公共点.
\end{enumerate}
\end{problem}

\begin{problem}
已知两定点 \(F_{1}(-\sqrt{2},0)\)、\(F_{2}(\sqrt{2},0)\)，满足条件 \(\left|PF_{2}\right| - \left|PF_{1}\right| = 2\) 的点 \(P\) 的轨迹是曲线 \(E\)，直线 \(y = kx - 1\) 与曲线 \(E\) 交于 \(A\)、\(B\) 两点.

\begin{enumerate}
\item 求 \(k\) 的取值范围；
\item 如果 \(\left|AB\right|=6\sqrt{3}\)，且曲线 \(E\) 上存在点 \(C\)，使 \(\overrightarrow{OA}+\overrightarrow{OB}=m\overrightarrow{OC}\)，求 \(m\) 的值和 \(\triangle ABC\) 的面积 \(S\).
\end{enumerate}
\end{problem}
