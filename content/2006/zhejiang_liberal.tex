\chapter{2006年浙江卷（文）}
\section{单选题}
\begin{problem}
设集合 \(A = \{x \mid -1 \leqslant x \leqslant 2\}\)，\(B = \{x \mid 0 \leqslant x \leqslant 4\}\)，则 \(A \cap B =\)（\quad）

\choices
    {\([0,2]\)}
    {\([1,2]\)}
    {\([0,4]\)}
    {\([1,4]\)}
\end{problem}

\begin{problem}
在二项式 \((x + 1)^4\) 的展开式中，含 \(x^2\) 的项的系数是（\quad）

\choices
    {15}
    {20}
    {30}
    {40}
\end{problem}

\begin{problem}
抛物线 \( y^{2} = 8x \) 的准线方程是（\quad）

\choices
    {\(x = -2\)}
    {\(x = -4\)}
    {\(y = -2\)}
    {\(y = -4\)}
\end{problem}

\begin{problem}
已知 \(\log_3 m < \log_4 n < 0\)，则（\quad）

\choices
    {\(n < m < 1\)}
    {\(m < n < 1\)}
    {\(1 < m < n\)}
    {\(1 < n < m\)}
\end{problem}

\begin{problem}
设向量 \(a, b, c\) 满足 \(a + b + c = 0, a \perp b, |a| = 1, |b = 2\)，则 \(|c|^2 =\)（\quad）

\choices
    {1}
    {2}
    {4}
    {5}
\end{problem}

\begin{problem}
函数 \( f(x) = x^3 - 3x^2 + 2 \) 在区间 \([-1, 1]\) 上的最大值是（\quad）

\choices
    {\(-2\)}
    {0}
    {2}
    {4}
\end{problem}

\begin{problem}
“a > 0, b > 0”是“ab > 0”的（\quad）

\choices
    {充分而不必要条件}
    {必要而不充分条件}
    {充分必要条件}
    {既不充分也不必要条件}
\end{problem}

\begin{problem}
如图，正三棱柱 \(ABC - A_{1}B_{1}C_{1}\) 的各棱长都 2, E, F 分别是 \(AB, A_{1}C_{1}\) 的中点，则 \(EF\) 的长是（\quad）

\choices
    {2}
    {\(\sqrt{3}\)}
    {\(\sqrt{5}\)}
    {\(\sqrt{7}\)}
\end{problem}

\begin{problem}
在平面直角坐标系中，不等式组 \( \left\{\begin{aligned}x+y-2&\geqslant0,\\ x-y+2&\geqslant0,\end{aligned}\right. \) 表示的平面区域的面积是（\quad）

\choices
    {\(4\sqrt{2}\)}
    {4}
    {\(2\sqrt{2}\)}
    {2}
\end{problem}

\begin{problem}
对 \(a, b \in \R\)，记 \(\max \{a, b\} = \begin{cases} a, & a \geqslant b, \\ b, & a < b, \end{cases}\) 函数 \(f(x) = \max \{|x + 1|, |x - 2|\}\) (\(x \in \R\)) 的最小值是（\quad）

\choices
    {0}
    {\(\frac{1}{2}\)}
    {\(\frac{3}{2}\)}
    {3}
\end{problem}

\section{填空题}
\begin{problem}
不等式 \(\frac{x + 1}{x - 2} > 0\) 的解集是 \fillinblank{} \fillinblank{}.
\end{problem}

\begin{problem}
函数 \( y = 2\sin x\cos x - 1, x \in \R \) 的值域是 \fillinblank{} \fillinblank{}.
\end{problem}

\begin{problem}
双曲线 \( \frac{x^{2}}{m}-y^{2}=1 \) 上的点到左焦点的距离与到左准线的距离的比是 3，则 m 等于 \fillinblank{} \fillinblank{}.
\end{problem}

\begin{problem}
如图，正四面体 \(ABCD\) 的棱长为 1，平面 \(\alpha\) 过棱 \(AB\)，且 \(CD // \alpha\)，则正四面体上的所有点在平面 \(\alpha\) 内的射影构成的图形面积是 \fillinblank{} \fillinblank{}.
\end{problem}

\section{解答题}
\begin{problem}
若 \(S_{n}\) 是公差不为 0 的等差数列 \(\{a_{n}\}\) 的前 \(n\) 项和，且 \(S_{1}, S_{2}, S_{4}\) 成等比数列. (1) 求数列 \( S_{1}, S_{2}, S_{4} \) 的公比; (2) 若 \(S_{2} = 4\)，求 \(\{a_{n}\}\) 的通项公式.
\end{problem}

\begin{problem}
如图，函数 \( y = 2\sin (\pi x + \varphi), x \in \R \)，其中 \( \left(\sin 0 \leqslant \varphi \leqslant \frac{\pi}{2}\right) \) 的图象与 \( y \) 轴交于点 (0,1). (1) 求 \(\varphi\) 的值; (2) 设 \(P\) 是图象上的最高点，\(M, N\) 是图象与 \(x\) 轴的交点，求 \(\overrightarrow{PM}\) 与 \(\overrightarrow{FN}\) 的夹角.
\end{problem}

\begin{problem}
如图，在四棱锥 \(P-ABCD\) 中，底面为直角梯形，\(AD // BC\)，\(\angle BAD = 90^{\circ}\)，\(PA \perp\) 底面 \(ABCD\)，且 \(PA = AD = AB = 2BC\)，\(M, N\) 分别为 \(PC, PB\) 的中点. (1) 求证: \( PB \perp DM; \) (2) 求 BD 与平面 ADMN 所成的角.
\end{problem}

\begin{problem}
甲，乙两袋装有大小相同的红球和白球，甲袋装有 2 个红球，2 个白球，乙袋装有 2 个红球，\(n\) 个白球. 现从甲，乙两袋中各任取 2 个球. (1) 若 n = 3，求取到的 4 个球全是红球的概率; (2) 若取到的 4 个球中至少有 2 个红球的概率为 \(\frac{3}{4}\)，求 \(n\).
\end{problem}

\begin{problem}
如图，侧面 \(\frac{x^2}{a^2} + \frac{y^2}{b} = 1 (a > b > 0)\) 与过点 \(A(2,0), B(0,1)\) 的直线有且只有一个公共点 \(T\)，且椭圆的离心率 \(\epsilon = \frac{\sqrt{3}}{2}\). (1) 求椭圆方程; (2) 设 \(F_{1}, F_{2}\) 分别为椭圆的左、右焦点，求证: \(|AT|^{2} = \frac{1}{2} |AF_{1}| \cdot |AF_{2}|\).
\end{problem}

\begin{problem}
设 \( f(x) = 3ax^2 + 2bx + c \)，若 \( a + b + c = 0 \)，\( f(0)f(1) > 0 \)，求证: (1) 方程 \( f(x)=0 \) 有实根; (2) \(-2 < \frac{b}{a} < -1\); (3) 设 \(x_{1}, x_{2}\) 是方程 \(f(x) = 0\) 的两个实根，则 \(\frac{\sqrt{3}}{3} \leqslant |x_{1} - x_{2}| < \frac{2}{3}\).
\end{problem}

